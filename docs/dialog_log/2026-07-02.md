# 2026-07-02 세션 로그

## GIS 시설 메뉴 — 남은 보강 (이어서 진행)

### 사용자 요청
- "이어서 진행" (남은 보강: 데이터 시드 / 권한·감사 / 시설 MTBF·월별 탭)

### 진행
- **시설 단위 고장 요약** 인스펙터 추가: fetchHealthSummary(12, sitename+facilitytype)
  → 총 고장·진행중·영향 설비·평균 조치시간. 실데이터, 기존 엔드포인트 재사용, 백엔드
  무변경. 검증: 행정배수지 "총 5 / 진행 4 / 영향설비 1". (slm-dashboard `16267da`)
- (사고) 편집 중 GisDetailPanel 일시 파싱 에러 → turbopack 상태 오염으로 /api/auth/* 500.
  파일은 정상, 프런트 재기동으로 복구(내 코드 이상 아님). 로그인 정상 확인.

### 판단 (남은 항목)
- commissioned_at(설치일자)은 실제 운영 데이터 → 조작 금지. 설비 CRUD 설치일 필드로
  고객이 입력. 카테고리/수명 norm 은 이미 매핑됨(교체주기 코드 검증 완료).
- CRUD 감사(audit) 로깅은 미구현 — actor 컨텍스트 확보 등 별도 설계 필요(백엔드 광범위).
- 사양 §6/§10 갱신.

## 설비 CRUD 감사 로깅 (1단계)

### 사용자 요청
- 추천안(감사 로깅) 승인 → "진행해"

### 결과
- 선행 확인: 프록시(app/api/proxy/[...path]/route.ts:89)가 JWT Authorization 전달 ✓.
- Migration 0094 `tb_audit_log`(actor·action·target·summary·detail·client_ip).
- 백엔드 endpoints/audit.py: get_actor(비파괴 JWT 추출, 토큰없음→unknown) + write_audit
  (예외 삼킴). facility_crud 설비 create/update/delete 에 actor/request + 감사 기록.
  auth_crud._decode_token 재사용. (slm `6499635`)
- 검증: 토큰 create/delete → actor=jykim/region=R01, 변경필드·client_ip; 토큰없음→unknown
  (비파괴). 테스트 설비·감사행 정리. py_compile·기동 에러 0.
- 사양 §5.3 갱신. 잔여: 시설 CRUD 확대(2단계) + 감사 조회 UI.

## 감사 2단계 (시설 CRUD 확대 + 조회 UI) — 한 번에

### 사용자 요청
- "이어서 해줘 한번에 내가 확인할꺼야"

### 결과
- **시설 CRUD 감사 확대**: facility_types_crud 배수지/가압장/감압/블록 create/update/
  delete 12개에 get_actor + write_audit(target_type 별). (slm `d3749cf`)
- **감사 조회**: GET /admin/audit-logs(페이징+대상유형·액션·작성자·기간 필터) +
  ai_server 등록. 프런트 /admin/audit-logs 페이지(필터+테이블+페이징) + audit-api +
  사이드바 M100-15 + Migration 0095(tb_menu). (slm-dashboard `7e0f491`)
- 검증: 배수지 create/delete → actor=jykim/R01; 설비 create/update/delete 3건 →
  감사 로그 화면 표시(배지·작성자·IP). 데모 데이터 정리. tsc/py_compile 에러 0.
- 사양 §5.3 갱신. GIS 시설 메뉴 고도화 1차·2차·감사(1·2단계) 전부 완료.

## 로그인 후 첫 화면 설정 (2026-07-03~09)

### 사용자 요청
- "관리화면에서 첫화면을 선택할수 있도록. 기본은 대시보드지만 GIS 관망도부터 보고 싶은 경우도 있어"

### 결과
- 백엔드 admin.py: /admin/site-settings GET/PUT 에 default_landing_page 추가
  (tb_comm_code SITE_SETTING/DEFAULT_LANDING_PAGE comm_val, 스키마 변경 없음).
  PUT 허용경로 화이트리스트 검증(/dashboard·/monitoring/gis·flow·tags·crisis/alarm-dashboard).
  (slm `27715f6`)
- 프런트: 사이트 설정에 "로그인 후 첫 화면" 셀렉터 + 로그인 페이지 리다이렉트
  (딥링크 callbackUrl 우선, 없으면 설정된 첫 화면). (slm-dashboard `8799028`)
- 검증(Playwright): 첫화면=GIS → 딥링크 없는 로그인 시 /monitoring/gis 이동, 딥링크
  있으면 그 경로 우선. 잘못된 값은 /dashboard 보정. 테스트 후 기본값 복원. tsc/py_compile 에러 0.
