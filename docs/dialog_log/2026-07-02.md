# 2026-07-02 세션 로그

## GIS 시설 메뉴 — 남은 보강 (이어서 진행)

### 사용자 요청
- "이어서 진행" (남은 보강: 데이터 시드 / 권한·감사 / 시설 MTBF·월별 탭)

### 진행
- **시설 단위 고장 요약** 인스펙터 추가: fetchHealthSummary(12, sitename+facilitytype)
  → 총 고장·진행중·영향 설비·평균 조치시간. 실데이터, 기존 엔드포인트 재사용, 백엔드
  무변경. 검증: 행정배수지 "총 5 / 진행 4 / 영향설비 1". (slm-dashboard `16267da`)
- (사고) 편집 중 GisDetailPanel 일시 파싱 에러 → turbopack 상태 오염으로 /api/auth/* 500.
  파일은 정상, 프런트 재기동으로 복구(내 코드 이상 아님). 로그인 정상 확인.

### 판단 (남은 항목)
- commissioned_at(설치일자)은 실제 운영 데이터 → 조작 금지. 설비 CRUD 설치일 필드로
  고객이 입력. 카테고리/수명 norm 은 이미 매핑됨(교체주기 코드 검증 완료).
- CRUD 감사(audit) 로깅은 미구현 — actor 컨텍스트 확보 등 별도 설계 필요(백엔드 광범위).
- 사양 §6/§10 갱신.

## 설비 CRUD 감사 로깅 (1단계)

### 사용자 요청
- 추천안(감사 로깅) 승인 → "진행해"

### 결과
- 선행 확인: 프록시(app/api/proxy/[...path]/route.ts:89)가 JWT Authorization 전달 ✓.
- Migration 0094 `tb_audit_log`(actor·action·target·summary·detail·client_ip).
- 백엔드 endpoints/audit.py: get_actor(비파괴 JWT 추출, 토큰없음→unknown) + write_audit
  (예외 삼킴). facility_crud 설비 create/update/delete 에 actor/request + 감사 기록.
  auth_crud._decode_token 재사용. (slm `6499635`)
- 검증: 토큰 create/delete → actor=jykim/region=R01, 변경필드·client_ip; 토큰없음→unknown
  (비파괴). 테스트 설비·감사행 정리. py_compile·기동 에러 0.
- 사양 §5.3 갱신. 잔여: 시설 CRUD 확대(2단계) + 감사 조회 UI.

## 감사 2단계 (시설 CRUD 확대 + 조회 UI) — 한 번에

### 사용자 요청
- "이어서 해줘 한번에 내가 확인할꺼야"

### 결과
- **시설 CRUD 감사 확대**: facility_types_crud 배수지/가압장/감압/블록 create/update/
  delete 12개에 get_actor + write_audit(target_type 별). (slm `d3749cf`)
- **감사 조회**: GET /admin/audit-logs(페이징+대상유형·액션·작성자·기간 필터) +
  ai_server 등록. 프런트 /admin/audit-logs 페이지(필터+테이블+페이징) + audit-api +
  사이드바 M100-15 + Migration 0095(tb_menu). (slm-dashboard `7e0f491`)
- 검증: 배수지 create/delete → actor=jykim/R01; 설비 create/update/delete 3건 →
  감사 로그 화면 표시(배지·작성자·IP). 데모 데이터 정리. tsc/py_compile 에러 0.
- 사양 §5.3 갱신. GIS 시설 메뉴 고도화 1차·2차·감사(1·2단계) 전부 완료.

## 로그인 후 첫 화면 설정 (2026-07-03~09)

### 사용자 요청
- "관리화면에서 첫화면을 선택할수 있도록. 기본은 대시보드지만 GIS 관망도부터 보고 싶은 경우도 있어"

### 결과
- 백엔드 admin.py: /admin/site-settings GET/PUT 에 default_landing_page 추가
  (tb_comm_code SITE_SETTING/DEFAULT_LANDING_PAGE comm_val, 스키마 변경 없음).
  PUT 허용경로 화이트리스트 검증(/dashboard·/monitoring/gis·flow·tags·crisis/alarm-dashboard).
  (slm `27715f6`)
- 프런트: 사이트 설정에 "로그인 후 첫 화면" 셀렉터 + 로그인 페이지 리다이렉트
  (딥링크 callbackUrl 우선, 없으면 설정된 첫 화면). (slm-dashboard `8799028`)
- 검증(Playwright): 첫화면=GIS → 딥링크 없는 로그인 시 /monitoring/gis 이동, 딥링크
  있으면 그 경로 우선. 잘못된 값은 /dashboard 보정. 테스트 후 기본값 복원. tsc/py_compile 에러 0.

## Tweaks(테마·브랜드·레이아웃) DB site-level 이관 (2026-07-09)

### 사용자 요청
- "설정내용을 db로 관리하여 서버 재시작시에도 적용되도록" → 확인 결과 사이트설정
  (landing/토글/AI)은 이미 DB 영속. localStorage 기반(테마/브랜드/레이아웃)만 비영속 →
  권장안(Tweaks DB 이관) 승인.

### 결과
- 백엔드 admin.py: /admin/site-settings 에 tweaks(brand_color_id/layout_mode/
  default_theme) — tb_comm_code TWEAKS_* comm_val, 화이트리스트 검증(brand 10종/
  layout sidebar·topbar/theme light·dark·system). 스키마 변경 없음. (slm `d84353d`)
- 프런트 TweaksPanel: useTweaksApplier 가 DB(조직 기본값) 로드·적용, 패널 변경 시 DB
  저장. localStorage 는 캐시로 강등. (slm-dashboard `3339d40`)
- 검증(Playwright): DB=blue/topbar → 대시보드 data-layout=topbar·--primary blue 적용.
  localStorage 비운 새 브라우저에서도 DB 값 적용·캐시 재생성. 테스트 후 기본값 원복.
- (확인) 사이트설정 재시작 지속성도 실측: landing·토글·AI 모델 재시작 후 유지.
- 사양 tweaks-layout-spec.md DB 이관 반영.

## Tweaks 개인별(per-user) 전환 (2026-07-09)

### 사용자 요청
- "개별 사용자 마다 다르게 적용되도록 하고 싶은데" (직전 조직 공통 → 개인별로)

### 결과
- Migration 0096: tb_user.preferences(jsonb). 백엔드 endpoints/user_prefs.py:
  GET/PUT /me/tweaks — actor(JWT) 기준 개인 저장, 미설정 항목은 사이트 기본값
  (SITE_SETTING/TWEAKS_*) 폴백. 인증 필수·화이트리스트·jsonb 부분 병합. ai_server 등록.
  (slm `8d557f4`)
- 프런트 TweaksPanel: /admin/site-settings → /me/tweaks 로 전환(조회·저장), 설명
  "개인 설정". (slm-dashboard `95ce3f3`)
- 검증: jykim PUT blue/topbar → 개인값(source=user), theme 미설정은 사이트폴백;
  무인증 site; 대시보드 blue/topbar 적용. 사용자 간 격리. 테스트값 정리.
- 2계층 설계(개인 우선 + 사이트 기본 폴백). 사양 tweaks-layout-spec 정정.

## 역동적 UI 기법 실제 적용 (Phase 0~3, 2026-07-09)

### 사용자 요청
- 시안 중 추천안 순차 적용. (성능 우려 확인 후 진행)

### 결과 (모두 slm-dashboard, 백엔드 무변경)
- Phase 0 모션 안전 기반: use-motion-safe(reduced-motion + 탭 숨김 정지) + globals.css
  reduced-motion 규칙. (`db65a59`)
- Phase 1 커맨드 팔레트(⌘K): 전역 단축키 + 권한 필터 메뉴 검색·키보드 이동. AppHeader/
  AppTopbar 마운트. 검증: "감사"→/admin/audit-logs 이동. (`db65a59`)
- Phase 2 배수지 수위 웨이브 게이지: WaterLevelGauge(CSS·motion-safe), 인스펙터에 현재
  수위/탱크높이. 검증: 죽동 74% 2.97m/4.0m. 라디얼 게이지는 신뢰 max 부재로 제외. (`21d11ae`)
- Phase 3 실시간 LIVE 펄스: LiveBadge(motion-safe ping), 1초 틱 금지·60초 갱신 유지.
  GIS "갱신" 옆 + 인스펙터 트렌드 헤더. (`2856ddc`)
- 성능 원칙 준수: transform/opacity·이벤트성 중심, reduced-motion·탭숨김 정지, 60초
  데이터 정렬. 편집 중 turbopack 일시 파싱에러 → 프런트 재기동으로 해소(코드 정상).
