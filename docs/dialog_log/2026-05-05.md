# 2026-05-05 세션 로그

## 요청
- "시뮬레이션이면 gis에서 보여야하는거 아닌가?" → "버튼으로 활성, 비활성할수 있는 구성이면 좋겠어"
- EPANET 시뮬 결과를 GIS 관망도에 직접 오버레이로 표출 + 토글 버튼

## 변경 요지 — EPANET Phase 2.6

### 좌표계 식별 (EPSG:5186 확정)
- 당진시 SHP 의 BBOX (156280, 465439) ~ (194621, 494896) 를 5종 한국 좌표계로 변환 시도
- EPSG:5186 (Korea 2000 / Central Belt 2010) → 126.51~126.94, 36.79~37.05 = 당진시 영역과 일치
- EPSG:5179 가 잘못된 가정이었음 (전국 통합 좌표계, 당진시 SHP 와 불일치)
- 환경변수 `EPANET_SHP_CRS` 로 변경 가능 (다른 사이트 대응)

### 백엔드 변경 (slm)
- `requirements.txt` — pyproj>=3.6.0 추가
- `epanet/simulator.py` — `_get_transformer()` lazy + `_to_lnglat(x,y)` 헬퍼
  · SimulationResult 에 `bbox_lnglat` 추가
  · junction/reservoir 에 `lng`/`lat` 필드 추가
  · pipe 에 `vertices_lnglat` 필드 추가 (UTM 좌표 vertices 와 별도 보존)
- `endpoints/epanet.py` — simulate 응답에 `bbox_lnglat` 포함

### 프런트 변경 (slm-dashboard)
- `components/gis/GisEpanetSimLayer.tsx` 신규
  · visible=true 면 가장 최근 success 시뮬 자동 fetch (artifact list → simulations → sim 상세)
  · MapLibre source/layer 동적 추가 — 노드/파이프/배수지 3 layer
  · 노드 `circle-color` = 압력 보간 (HSL 240→0), 4구간
  · 파이프 `line-color` = 정류 청록 / 역류 주황 / 거의 0 회색, `line-width` = |flow_lps| 보간
  · 배수지 = 청록 큰 원
  · visible=false 면 source/layer 모두 제거 (성능 보호)
- `monitoring/gis/page.tsx`
  · `showEpanetSim` state 추가
  · 상단 툴바에 [EPANET 시뮬] 토글 버튼 (cyan, 시설물 목록 옆)
  · `<GisEpanetSimLayer visible={showEpanetSim} />` GisMap 안에 통합

### 검증
- pyproj 변환 — bbox UTM [156280, 465458, 194003, 494896] → WGS84 [126.51, 36.79, 126.93, 37.05]
- 시뮬 #5 응답 — junction sample lng=126.59, lat=36.97 (당진시 송수관 한 점, 정확)
- 컴파일 정상, /monitoring/gis 307 (인증 미들웨어 정상)

### 사용법 (운영자)
1. /admin/site-settings 에서 EPANET 토글 ON
2. /admin/epanet 에서 INP 생성 → 산출물 행 [시뮬] 실행
3. /monitoring/gis 진입 → 상단 [EPANET 시뮬] 토글 클릭
4. 가장 최근 success 시뮬이 자동 로드되어 지도 위에 압력·유량 오버레이

### Phase 3 후속
- 표고/수요 입력 (DEM + 계량기) — 의미 있는 압력 분포
- EPS 시계열 + 시나리오 분석
- 펌프·밸브 SHP (SA100 제수밸브)
- GIS 오버레이에 sim_id 선택 드롭다운 (현재는 자동 latest)

## dialog_log 롤링 (3일)
- 05-03, 05-04 유지 (D-2, D-1)
- 05-05 신규 (오늘)
- D-4 = 05-01 — 없음
