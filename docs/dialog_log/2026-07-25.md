# 2026-07-25

## 변환룰 확장 3라운드 — 74.1% → 87.6% (사용자 "룰 불가 태그 있나?" → "진행")

- 룰 불가 유형 정량 분류 보고 (정보 부족·의미 번역·동일 desc 이중 의미·
  원본 오염 — 전체 diff의 ~40%)
- 시그니처 빈도 마이닝으로 룰 20개 추가 (49룰): 사전·언더스코어·접미 보강.
  우선순위 함정 1회(언더스코어가 F_OPEN 사전 선행 → 79.4 하락) 교정
- 잔여 336건 = 사람 판단 대상 (RE 이중 의미 82건은 override 필수). 스모크 16/16

## 룰 불가 의심 자동 표시 (사용자 "필터링 안되는건 별도 표시?")

- 기존엔 diff 한 덩어리 — 판별 신호 2종(동일 desc→다른 info 결정성 위반 /
  desc 에 없는 정보 추가)으로 hard 자동 분리: 룰 후보 24 vs 불가 의심 312
- rose 칩 "룰 불가 의심" + [제외]/[확정] 버튼 대상 확장 + 안내 문구

## 용어집·후보 단어 (사용자 "핵심 단어를 별도로 알 수 있게")

- /vocab API + 화면 카드: 핵심 표준 단어 23종(기능별 의존 근거 툴팁) +
  룰 미등록 약어 자동 발굴 9종(FAIL·KEPCO·FLOAT… 클릭→룰 초안 프리필)

## 기초정보 공백 11건 처리 (사용자 "진행해줘" — 추천 1번)

- 검증 후 3갈래 처리: ① 실존 7곳 등록 (가압장 6 booster_info + 송산2산단
  reservoir_info) ② 오타 정정 — 전의리→천의리(4태그, datainfo 근거)·
  합덕일반산단 facilitytype 배수지→소블록 ③ dev 테스트 잔재 태그 3건 삭제
  (스모크테스트·테스트현장 — 코드 참조 없음 확인)
- 검수 base_info **pass 전환** (warn 2→1, 잔여 warn 은 계측 품질 = 현장 몫).
  스모크 16/16

## UX 잔손질 2건 (추천 2번)

- 팝업 신선도 컷 24h — 첫 방문 브라우저에서 묵은 진행중 알람이 종합 카드로
  쏟아지던 문제 (벨 목록은 유지)
- 전망 "참고" 톤 — 주기 미검출+CV>15% 시설 low_confidence (검증: 난지관광
  seasonal=확정 / 신평 펌프형=참고). ⓒ 채팅 목록 서버 전환은 규모상 별도
  세션 권장 (테이블+API+마이그레이션)

## 변환룰 P2 — 신규 태그 CSV 온보딩 (추천 3번. 4번 납품 점검은 기능 묶음
## 마감 시점 주기 실행으로 합의)

- /onboard/preview·apply: 일괄 변환 + 상태 4종 + 조회 계약 lint 4종 +
  기존 tagsn 보호. 온보딩 카드 (템플릿·CSV 파서·경고 테이블·일괄 등록)
- E2E: 신규 2건 변환·등록·이력, 기존 1건 스킵, 정리. 접합 룰 앵커 개선
  (재현율 87.7%). tsc 0

## 채팅 대화 목록 서버 계정 전환 (보류 항목 착수 — "1. 진행")

- 사용자: "채팅 대화 목록 서버 계정 전환 진행"
- localStorage 전용이던 대화 그룹·메시지를 서버 정본으로 전환
  (docs/chat-history-server-spec.md, Migration 0119)
- DB: tb_ai_chat_group + tb_ai_chat_message (user/bot jsonb 통짜 —
  카드 개방 구조 보존, 메신저 tb_chat_group 과 분리)
- 백엔드: endpoints/chat_history.py — 그룹 CRUD·정렬 + 메시지 upsert +
  일회성 import(멱등). ai 메모리 세션·/ask/stream 불변, 스모크 16/16
- 프런트: chat-history-api.ts + chat-store 서버-우선(로컬 미러 폴백,
  낙관 갱신) + chat-local-migration.ts (서버 생존 확인 후 이관).
  세션 하이드레이션 전 refreshGroups 차단 (anonymous 이관 방지)
- E2E: 기존 8그룹 11메시지 자동 이관 → localStorage 전체 삭제(새 기기
  시뮬레이션)에도 목록·차트 재렌더 정상 → 신규 대화 서버 저장 확인
- 커밋: slm 1639ddc / frontend d4771fb / web (본 커밋)
