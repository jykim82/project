# 2026-09-01 세션 로그

## duckdns 접속 문의

- 포트 443(포트 표기 불필요) 안내. 컨테이너·DNS(IP 일치)·외부 검사 노드
  (TCP/HTTPS OK) 실측 — 서비스 정상. 원인은 U+ NAT 루프백(LAN 내 도메인
  접속 불가가 정상), LTE 접속 안내

## [E-060] 전체 배수지 유출유량 표 0건 수정

**사용자**: "'전체 배수지의 유출 유량을 표로 보여줘'가 왜 데이터가 없나".
- 원인: shared/timeseries 공용화 리팩토링 때 sql_executor import 에서
  query_chunks_agg·reaggregate 누락 → 이 인텐트만 NameError → except 가
  "데이터 없음"으로 위장
- 수정: import 4종 확장 + AST 미정의 이름 스캔(잔여 없음). 재검증 152행

## 배수지 압력 표 0건 — 카탈로그 미등록 (Migration 0137)

**사용자**: "한달간 (전체) 배수지 압력을 표로 보여줘도 동작 안 함".
- 원인: 코드가 아니라 데이터 — 배수지 실측 압력 태그(PRI Analog Input,
  4현장 10개)는 있는데 tb_trend_catalog 에 배수지 '압력' 트렌드 미등록.
  카탈로그 기반 인텐트라 정당한 0건이던 것
- 0137: 태그 원장 파생 INSERT...SELECT (현장 하드코딩 없음 — 타 고객 DB
  동일 동작, 멱등). 석문4·합덕3·행정2·송악1 등록
- 재검증: 두 질의 모두 310행. 값 실측 — 행정(5.1~5.7)·석문 생활유출(1.6)
  은 실측치, 나머지 다수는 센서 0.0(태그 품질 소관). 스모크 16/16

## [E-061] 야간최소유량 트렌드 오배지 (남산1 보고)

**사용자**: "남산1 소블록 야간최소유량 트렌드가 이상해 보이는데 왜 정상?".
- 실데이터: 8/8~18 원시 32.30 고착(센서 동결) 후 20대 복귀 — 사용자
  직관이 맞음. 누수 CUSUM 은 이미 8/16~18 "누수의심" 3회 검출(미확인)
- 결함: NMF 일별 집계에 범용 비교가 붙어 원시 기준선(38~91)과 비교 —
  -68% 편차에 '정상' 배지. `_nmf_series` 플래그로 비교 스킵(배지 제거),
  NMF 판정은 CUSUM 전담. 일반 트렌드 회귀 없음·스모크 16/16

## 모듈 버전·라이선스 관리 P1 (Migration 0138)

**사용자 요청**: 모듈 버전 관리 구조 검토(업데이트 버튼·롤백·버전·상태
확인) → 라이선스 제약 표현 추가 → P1 진행 승인.

- 사양 `docs/module-version-spec.md` — P1=읽기 전용 가시화. 적용 버튼·
  롤백은 P2/P3(호스트 에이전트 필요), DB 마이그레이션 롤백 자동화 금지
- 0138: tb_module_version(시드 8모듈) + tb_module_license(sku_code +
  oss_notices — SKU 활성은 tb_comm_code 정본 실시간 조인) + 메뉴 M100-17
- `GET /system/modules` — 버전+라이선스+live health(병렬 2s: db/ollama/
  node-red/frontend/vision/map-bundle)
- `/admin/system-modules` — 모듈 표(버전·health 도트·배지 3종·OSS 칩) +
  NOTICE 섹션(납품 고지 의무 답변처)
- 검증: API 8모듈 health 전부 실측 ok·라이선스 3종 상태(EPANET locked·
  vision active·나머지 included) 실데이터 표시, UI 실측(배지·NOTICE·
  OSM 고지), 콘솔 0, tsc 0, 스모크 16/16

## 모듈 업데이트 P2/P3 (Migration 0139)

**사용자 요청**: "순서대로 다 이어서, 테스트 각 5회, 확인 사항만 보고".
- P2: tar.gz 번들 규격 + 업로드→sha256·SKU 게이트·경로 안전 검증→스테이징
  (`endpoints/module_update.py`, tb_module_update 0139) + 버전 자동 스탬핑
  (백엔드 /app/VERSION 기동 시·프런트 빌드 스크립트 날짜-SHA) + UI 번들 패널
- P3: `scripts/update_agent.py` 호스트 에이전트 — 잡 스풀 처리(백업→배치→
  sha 재검증→마이그레이션→health→실패 시 자동 복원), 롤백(DB 마이그레이션
  제외), 결과 파일→DB 병합. 결함 1건 수정: 상태 전이 직전 결과 미병합으로
  롤백 거부 → 전이 전 병합
- 검증: 시나리오 6종×5회=30/30 (정상/sha 차단/SKU 차단/적용+스탬프/롤백
  복원/변조 자동복원) + UI 실측 + 스모크 5회. 테스트 데이터 전부 원복.
  container 종류(이미지 교체)는 코드만 — 실 이미지 번들 확보 시 검증

## [E-062] 전체 이상 스캔 "준비 중" 공백 제거

**사용자**: "전체 이상 스캔해줘가 준비 중이라고 나옴 — 원인 분석·조치".
- 원인: 스캔 캐시 인메모리 → 재기동마다 소실, 첫 빌드 ~51초. 당일 개발
  리로드 47회로 빈발 노출 (운영에선 배포·재부팅 직후 동일)
- 조치: 캐시 pkl 영속화 + 기동 시 즉시 로드(나이 정직·24h 상한) + 빌드
  후 원자 교체 저장. 검증: 재기동+즉시 질의 ×5 = 293행 즉답 5/5

## 운영 방침 확정

**사용자 결정**: ① 업데이트 에이전트는 **수동 실행**(상주 등록 안 함 —
승인 후 "적용 대기"가 정상 상태) ② container 실검증·pack 스크립트는 첫
실전 배포 시 ③ 네트워크 이전 수동 배치 백업(tmp_canvas_layout_backup)
삭제 — 계층 배치 확정

## 4건 배치 (P2 3종 + 스모크 보강)

**사용자 요청**: LLM 문체 서술·점검 기원 P2·미확인 상신·스모크 보강 진행.
- **스모크 17케이스** — 카탈로그 표 인텐트 추가 (E-060 커버)
- **LLM 서술 (agent-loop P2)**: narrative 필드 — 수치 검증 통과분만,
  [AI 참고 의견] violet 구분. 5/5 안정. 구현 중 **[E-063] 발견**: Ollama
  0.20.x+gemma4 thinking 기본화로 전 생성 호출 빈 응답 → think=false 명시
  (시스템 전반 AI 요약류 복구). keep-warm 핑 경고 debug 강등
- **점검 기원 P2**: 파서 확정 2종 정규화 + "고장 건 점검" → 요청 + 유발
  고장 자동 링크(확인 메시지 표시·INSERT 관통). 파싱 5/5·confirm 관통
  검증 후 원복
- **미확인 상신 (0140)**: 경고 N분 미확인 → 전체 채널 system 상신, 건당
  1회 멱등, ALARM_ESCALATION_MIN(기본 10, 0=끔), 60s 루프+수동 트리거.
  5회×(상신·멱등·메시지·기록) 전부 통과 후 원복
- 최종 스모크 17/17

## 워크스페이스 레이어 겹침 수정

**사용자**: "구축 워크스페이스 용수계통·통신망 화면 오버레이 현상".
- 원인: 숨김이 `invisible`(visibility) — React Flow 가 노드에
  visibility:visible 직접 지정해 무효화 (숨긴 레이어에서 934개 요소 노출)
- 수정: opacity-0 + z-index 로 교체 (display:none 은 캔버스 크기 0
  초기화로 부적합). 검증: 시각 누출 0

## 상황실 키오스크 모드 P1 (Migration 0141)

**사용자**: 키오스크 예시 요청 → HTML 시안 전달 → "구축 계획 세우고 진행".
- 원칙: 새 화면을 만들지 않음 — 기존 3화면(GIS·계통도·경보관리)을
  iframe 상주 로드 + opacity 순환 (재로딩 없음, 워크스페이스 겹침 수정과
  동일 원칙). `?kiosk=1` 이면 DashboardShell 이 크롬 제거
- `/kiosk` (M003-12): 하단 바(시계·화면 도트·경보 티커·일시정지·전체화면),
  순환 20s(?interval= 오버라이드), **경고 인터럽트** — 새 경고 폴링 감지
  시 경보 화면 60s 전환+red 배너, 같은 경보 재인터럽트 없음. 주의는
  티커만 (채터링 방지)
- 검증: 크롬 숨김(외부+iframe 내부)·3뷰 상주·수동/자동 순환·콘솔 0·tsc 0
- 백로그 등록: 워크스페이스 캔버스 정렬(드래그 선택·우클릭 팬·정렬 5종)
