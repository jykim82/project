# 2026-09-01 세션 로그

## duckdns 접속 문의

- 포트 443(포트 표기 불필요) 안내. 컨테이너·DNS(IP 일치)·외부 검사 노드
  (TCP/HTTPS OK) 실측 — 서비스 정상. 원인은 U+ NAT 루프백(LAN 내 도메인
  접속 불가가 정상), LTE 접속 안내

## [E-060] 전체 배수지 유출유량 표 0건 수정

**사용자**: "'전체 배수지의 유출 유량을 표로 보여줘'가 왜 데이터가 없나".
- 원인: shared/timeseries 공용화 리팩토링 때 sql_executor import 에서
  query_chunks_agg·reaggregate 누락 → 이 인텐트만 NameError → except 가
  "데이터 없음"으로 위장
- 수정: import 4종 확장 + AST 미정의 이름 스캔(잔여 없음). 재검증 152행

## 배수지 압력 표 0건 — 카탈로그 미등록 (Migration 0137)

**사용자**: "한달간 (전체) 배수지 압력을 표로 보여줘도 동작 안 함".
- 원인: 코드가 아니라 데이터 — 배수지 실측 압력 태그(PRI Analog Input,
  4현장 10개)는 있는데 tb_trend_catalog 에 배수지 '압력' 트렌드 미등록.
  카탈로그 기반 인텐트라 정당한 0건이던 것
- 0137: 태그 원장 파생 INSERT...SELECT (현장 하드코딩 없음 — 타 고객 DB
  동일 동작, 멱등). 석문4·합덕3·행정2·송악1 등록
- 재검증: 두 질의 모두 310행. 값 실측 — 행정(5.1~5.7)·석문 생활유출(1.6)
  은 실측치, 나머지 다수는 센서 0.0(태그 품질 소관). 스모크 16/16

## [E-061] 야간최소유량 트렌드 오배지 (남산1 보고)

**사용자**: "남산1 소블록 야간최소유량 트렌드가 이상해 보이는데 왜 정상?".
- 실데이터: 8/8~18 원시 32.30 고착(센서 동결) 후 20대 복귀 — 사용자
  직관이 맞음. 누수 CUSUM 은 이미 8/16~18 "누수의심" 3회 검출(미확인)
- 결함: NMF 일별 집계에 범용 비교가 붙어 원시 기준선(38~91)과 비교 —
  -68% 편차에 '정상' 배지. `_nmf_series` 플래그로 비교 스킵(배지 제거),
  NMF 판정은 CUSUM 전담. 일반 트렌드 회귀 없음·스모크 16/16

## 모듈 버전·라이선스 관리 P1 (Migration 0138)

**사용자 요청**: 모듈 버전 관리 구조 검토(업데이트 버튼·롤백·버전·상태
확인) → 라이선스 제약 표현 추가 → P1 진행 승인.

- 사양 `docs/module-version-spec.md` — P1=읽기 전용 가시화. 적용 버튼·
  롤백은 P2/P3(호스트 에이전트 필요), DB 마이그레이션 롤백 자동화 금지
- 0138: tb_module_version(시드 8모듈) + tb_module_license(sku_code +
  oss_notices — SKU 활성은 tb_comm_code 정본 실시간 조인) + 메뉴 M100-17
- `GET /system/modules` — 버전+라이선스+live health(병렬 2s: db/ollama/
  node-red/frontend/vision/map-bundle)
- `/admin/system-modules` — 모듈 표(버전·health 도트·배지 3종·OSS 칩) +
  NOTICE 섹션(납품 고지 의무 답변처)
- 검증: API 8모듈 health 전부 실측 ok·라이선스 3종 상태(EPANET locked·
  vision active·나머지 included) 실데이터 표시, UI 실측(배지·NOTICE·
  OSM 고지), 콘솔 0, tsc 0, 스모크 16/16
