# 2026-04-29 세션 로그

## 요청
- (이어서) 분류 매칭률 개선 + 통계 대시보드 UI 추가

## 변경 요지

### 1. 분류기 빈 응답 해결 — Ollama `format=json`
- `ollama_client.generate(format="json")` 1줄 수정으로 모델 교체 없이 해결
- `gemma4` 가 자유 단락은 잘 응답하나 정형 JSON 출력에 빈 응답 반환하던 문제

### 2. 분류 매칭률 향상
- 미매칭 분석: 더미 텍스트 4건 + 정보 부족 1건 + 점검 항목 2건 + 펌프 1건
- `MECH_PUMP` hint 보강 ("펌프 고장, 가압펌프 고장" 추가)
- `daily_inspection` 보고서 항목 분류 대상 제외 (batch/stats)
- **결과**: 53% (9/17) → **73% (11/15)** + 다중 코드 매칭(`COMM_MODEM`, `POWER_TRIP` 동시) 동작

### 3. 통계 대시보드 UI
- `/monitoring/equipment-health` 5번째 탭 "근본원인 통계" 추가
- `RootCauseStatsSection` 컴포넌트:
  - 헤더 + [전체 재분류] 버튼
  - 코드별 빈도 표 (그룹 색상 배지 + 분포 막대)
  - 설비 교체 후보 순위 (cause_count, total_count)
  - 설비별 코드 분포
- 화면 검증: 14건 코드 부여 / 3설비 / 8개 코드 정상 분포

### 4. SQL alias 충돌 수정
- 통계 SQL `cause` alias 가 `tb_report_item.cause` 컬럼과 충돌
- `LATERAL ... AS rc(code)` 로 수정 — by_cause 정상

## 후속
- P2 — 가중치 곱 (taxonomy.weight) + 시간 감쇠 적용
- 야간 cron 스케줄러로 자동 분류 (현재는 수동 [전체 재분류] 만)
- 미분류 항목별 사유 표시 (UI 보강)
