# 2026-04-29 세션 로그

## 요청
- (이어서) 분류 매칭률 개선 + 통계 대시보드 UI 추가

## 변경 요지

### 1. 분류기 빈 응답 해결 — Ollama `format=json`
- `ollama_client.generate(format="json")` 1줄 수정으로 모델 교체 없이 해결
- `gemma4` 가 자유 단락은 잘 응답하나 정형 JSON 출력에 빈 응답 반환하던 문제

### 2. 분류 매칭률 향상
- 미매칭 분석: 더미 텍스트 4건 + 정보 부족 1건 + 점검 항목 2건 + 펌프 1건
- `MECH_PUMP` hint 보강 ("펌프 고장, 가압펌프 고장" 추가)
- `daily_inspection` 보고서 항목 분류 대상 제외 (batch/stats)
- **결과**: 53% (9/17) → **73% (11/15)** + 다중 코드 매칭(`COMM_MODEM`, `POWER_TRIP` 동시) 동작

### 3. 통계 대시보드 UI
- `/monitoring/equipment-health` 5번째 탭 "근본원인 통계" 추가
- `RootCauseStatsSection` 컴포넌트:
  - 헤더 + [전체 재분류] 버튼
  - 코드별 빈도 표 (그룹 색상 배지 + 분포 막대)
  - 설비 교체 후보 순위 (cause_count, total_count)
  - 설비별 코드 분포
- 화면 검증: 14건 코드 부여 / 3설비 / 8개 코드 정상 분포

### 4. SQL alias 충돌 수정
- 통계 SQL `cause` alias 가 `tb_report_item.cause` 컬럼과 충돌
- `LATERAL ... AS rc(code)` 로 수정 — by_cause 정상

## 후속 (이어서 진행 — 같은 날)

### 5. P2/P3 — 가중치 + 시간 감쇠 (구현 완료)
- stats SQL CTE 로 재구성:
  · `item_codes` — 항목별 코드 + time_decay (1y=1.0, 2y=0.5, ≥2y=0.2)
  · `weighted` — equipment_id 별 SUM(weight × time_decay) + recent_1y/2y 카운트
  · `totals` — total_count
- ORDER BY weighted_score DESC NULLS LAST, cause_count DESC
- UI 교체 후보 순위 표 컬럼: 가중치 점수 (Badge ≥5 default / 2~5 secondary / <2 outline)
  + 최근 1년 (primary 컬러 강조) + 코드 매칭 + 총 보고

### 6. 야간 cron endpoint
- POST /reports/items/classify-causes-cron
- 모든 region 일괄 (only_unclassified 기본 true, limit_per_region=200)
- 응답: { regions:[{region, processed, hits}], total_processed }
- 운영: crontab 매일 03:00 호출 권장
- 인증 헤더 없음 (내부망 한정)

### 검증
- plc_3 신평 PLC: score 8.0 (1순위, 4 cause / 4 recent_1y)
- plc_50 송산1 PLC: score 3.0 (2순위, 1 cause)
- cron 호출 결과: total_processed=0 (이미 모두 분류됨, only_unclassified=true)

## 다음 단계 (P3 추가 + 운영)
- `tb_equipment_lifespan` 연식 보정 (설비별 도입 연도 반영)
- 미분류 항목별 사유 UI (어떤 텍스트가 부족했는지 표시)
- 운영 가이드: cron 등록·로그 모니터링·LLM 모델 튜닝
