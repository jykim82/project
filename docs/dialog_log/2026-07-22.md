# 2026-07-22

## 테마 번짐 미표시 계속 — 환경 진단 단계

- 사용자: 파라미터 상향 후에도 동일 증상. "리액트만 되나" / "기술적으로
  가능은 한거지"
- VT 는 브라우저 네이티브 API (리액트 무관) — 파이프라인은 계측상 정상.
  유력 원인: OS 동작 줄이기(테마 폴백+전역 freeze 로 GIS 등장도 정지) 또는
  미지원 브라우저
- theme-transition 폴백 시 사유 콘솔 로그 추가 (미지원/동작 줄이기 구분)
  → 사용자 콘솔 확인 대기

## 원인 확정 — OS 동작 줄이기 (윈도우 애니메이션 효과 꺼짐)

- 사용자 확인: 설정 켠 뒤 "이제 되네" — 테마 번짐·GIS 패널 등장 정상 표시
- 맥·윈도우 두 PC 모두 OS 애니메이션 효과가 꺼져 있어 "미구현"으로 보였던 것
- Tweaks 패널에 "모션 진단" 섹션 신설 (MotionDiagnostics.tsx): OS 동작
  줄이기 / View Transitions 지원 / 그래픽 가속(SwiftShader 검출) 3항목 +
  렌더러 표시 — 납품 현장 트러블슈팅용

## 진행 버튼 일괄 확대 + 보고서 직접 입력

- 사용자: ① ActionStateButton 일괄 확대 ② 일 점검 보고서는 개별 기재가
  기본 취지 (채팅 등록은 현장→센터 보완용) ③ 장애 조치도 마찬가지
- 확대: 메모/일정 저장, 작업(알람 제어) 등록, EPANET SHP 스캔·INP 생성,
  보고서 생성(기존) + 신규 항목 직접 입력. onAction false=무동작(검증),
  throw=오류 상태 규약 적용
- 직접 입력: POST /reports/{id}/items/manual (task_id NULL) +
  ManualItemDialog + 빈 보고서 생성 허용 (두 유형 공통)
- 실검증: 일 점검 빈 보고서 생성→직접 입력→항목 카드 표시 (#19, 검증 후
  삭제), 메모 저장 버튼 무동작/저장됨 상태 확인 (검증 메모 삭제)

## 수처리 지식 주입·재학습 검토 → 폐기 결정

- 검토: RAG 확장(효과적)/26B 파인튜닝(비현실적, M4 Pro 48GB)/4b LoRA(가능)
- 사용자 결정: 비효율 판단 — 전체 폐기, 진행하지 않음. 기존 매뉴얼 RAG·
  고장 케이스 DB는 현행 유지

## 현장 모드 일상 점검 기록 v2 — 방식 선택 시트

- 사용자: 일상 점검이 파일(사진) 선택부터 시작되는 문제 지적. ① 음성→문자
  기록 후 일 보고서에서 수정 ② 순서대로 따라가며 채우는 단계별 등록 두 경로
  필요
- FieldInspectionSheet 신설: 음성 빠른 기록(STT→채팅 점검 인텐트 인계) /
  단계별 기록(점검종류→시스템→장비→결과 칩 선택, createTask 즉시 등록) /
  사진 기록(기존 유지)
- 실검증: 신평 배수지 단계별 등록 → tb_task_master #38 저장 확인
  (점검 장비: 수위계, 이상 없음) 후 테스트 데이터 삭제. 카테고리는 관리
  메뉴 inspection_system/equipment 동적 로드

## E-038 3차 대응 + 일상점검 TC 4회 + 일정 알림 정시 검증

- E-038 재발(첫 로그인 알람 리플만 표시): 자가 복구를 "레이어 존재" 검사에서
  "0-렌더 유실" 검사로 확장 — 초기/스타일 에피소드 20초 창에서
  queryRenderedFeatures 0건+소스 피처 존재 시 리마운트 (팬 오탐 방지 시간창)
- 일상 점검 기록 TC 4회 (사용자 지시): TC1 일상+다중선택+이상없음 ✓
  TC2 정기+특이사항+내용 ✓ (DB #39·40 확인 후 삭제) TC3 특이사항+내용 없음
  → 검증 무동작 ✓ TC4 음성 경로 채팅 프리필 인계 ✓
- 일정 알림 정시 검증: 22:11 등록 → 22:11:23 팝업 발화 (30초 폴링 이내)
  → 확인 처리 ✓ (테스트 일정 삭제)

## 에러 리스트업·재발 방지 관리 (사용자 지시)

- E-038 3차 재발 대응 내역 추가 (0-렌더 유실 판정 확장)
- E-046 신규: 상시 마운트 다이얼로그 상태 잔존 + URL 1회성 가드 (재발 방지
  규범: 열릴 때 defaults 재동기화 / 파라미터 키 가드)
- E-047 신규: OS 동작 줄이기 모션 미표시 오인 + 자동화 VT 블라인드
  (진단 경로: Tweaks 모션 진단)
- 데이터 정합 2건 review-items 등재 (합덕일반 datainfo 불일치·수청2지구2
  facility_map 누락)

## AI 텍스트 기능 정비 (사용자 피드백)

- 설비 건강진단 "AI 원인 분석" 제거 — 프롬프트가 새 정보 생성을 금지해
  화면 수치 재서술만 가능한 구조 (정보 가치 0 + 수십 초 지연)
- "AI 현황 요약"은 유지·강화 판정 (위급 1건+유형 현황+정의+점검 순서 —
  비전문 운영자 해석 보조 구조가 유효)
- 요약 모순 수정: "평균과 동일한데 이상 판정" — 카테고리 무관하게 현재값/
  평균을 근거로 서술하던 프롬프트를 카테고리별 근거(감지 신호·수지 불일치·
  홀딩·값 비교)로 분리. 실검증: "남산10(블) 압력에서 comm_error 감지" 형태
  확인 + 신호 라벨 한국어 매핑

## 트렌드 적산차 전환 (사용자 승인 후 구현)

- /trend/data: 적산계(datainfo~적산) 태그를 버킷 max 인접 차분(구간 사용량)
  으로 변환해 반환. 음수(리셋·롤오버)=결측, 비교 지표는 원값 학습 정합
  문제로 적산 태그 제외, accum_diff_tags 응답 추가
- 프런트: 트렌드 메뉴 시리즈명 "(적산차)" 표기. 테이블 변경 없음·부하 증가
  사실상 0 (기존 집계에 뺄셈만)
- 실검증: 용연2통(가) 적산 태그 24h — 단조증가 소멸, 증분 시점만 값,
  음수 0건

## 알람 캘린더 분야 칩 필터 + 설비 건강성 우선순위 검토

- 캘린더: 카테고리 칩 클릭 → 해당 분야만 히트맵/추이 (실검증: 전체 3,762
  → 수위 476건, 해제 복귀). 분야별 유지관리 일정 배분 용도
- 설비 건강성 Top5 검토 (사용자 문제 제기): 근거 데이터 수집 — 실알람은
  죽동 탁도계 통신이상 15,085건이 압도적인데 Top5 는 고장보고 수기 카운트
  (신평 PLC 4건=개발 테스트 기록) 기반이라 실알람 신호 미반영 확인.
  개선안(실알람 추세 신호 추가·고장 분류 정제) 사용자 승인 대기

## AI 요약 확장 + 교체 우선순위 v2 (사용자 승인 진행)

- MNF 분석 AI 요약: /leak/cusum/explain (cusum|stddev Hybrid) + 카드 3종
  버튼. E2E: "금일 48.28㎥/hr, 평소 범위(51.53~85.63)보다 낮음" 쉬운 말
  서술 확인
- 교체 우선순위 v2: 실알람 추세 신호 + MTBF 고장·일단위 정제. 죽동
  네트워크 10,463건 상위 진입
- 트렌드 화면 갱신 재발 보고: 계측(인스턴스 ID·DOM 변이 20초) 재현 불가
  — 증상 조건 추가 확인 필요

## 펌프 호기 표시 + 이상 진단 AI 요약 + 차트 포렌식

- 사용자: "도는 펌프가 1호? 왼쪽부터 1호?" → 기존 아이콘은 대수만 표현
  (호기 무관). pump_status 에 호기별 pumps 추가, 계통도/GIS 카드 아이콘에
  호기 번호 표시. 율사 실데이터 = 1호 정지·2호 가동 (기존 표시로는 오해)
- "천의리 배수지 이상 진단해줘" AI 요약 없음 → AnomalyDetailView 에 시설
  스코프 scan-all explain 버튼 추가. E2E: 천의리 정상 요약 확인
- 트렌드 재그림 재발: 3경로 정밀 계측(질의 전 설치, 인스턴스/변이 타임라인)
  모두 재현 불가 → EChartWrapper 에 상시 링버퍼(window.__slmChartEvents)
  설치, 재발 시 F12 로 즉시 진단
