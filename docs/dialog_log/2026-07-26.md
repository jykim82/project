# 2026-07-26 세션 로그

## 캔버스 에디터 일원화 (사용자: "추천안은?" → "사양부터 잡고 하자")

- 배경: 좌표 정본 이원화 — 캔버스(tb_canvas_node_position px 104행) vs
  계통도(tb_flow_diagram_node 경위도 99행). 캔버스 편집이 계통도에 미반영
- 사양: docs/canvas-editor-unification-spec.md (2b11c44)
- P1 (slm 0a337ed / front e04be8f): canvas GET/PUT → tb_flow_diagram_node
  직결, px↔경위도 선형 변환(레벨 240px·형제 56px). full-diff 폐지 → 명시
  diff(added/removed_edges·deleted_nodes)+causal 부분 재구축. 클라이언트
  BFS 자동정렬 제거 → 서버 relayout·lint 툴바 이식. Migration 0120
  (canvas_node_position → _legacy rename)
- P2 (front 7dddd10): 노드 배치 탭 제거(2탭), /setup/canvas redirect,
  Migration 0121(M200-11 메뉴 삭제), 팔레트 드롭 기초정보 안내 toast
- 검증: px 변환 왕복·엣지 diff·관계 97건 보존, 캔버스 실드래그(가곡) →
  자동저장 → 정본 반영·복원, lint 배지 "정합 정상", 신규 자동배치 토스트,
  redirect·2탭·99노드, tsc 0, 스모크 16/16

## 캔버스 태그 탭 명칭 + 트렌드 카탈로그 구조 확인

- 사용자: "캔버스 설비 메뉴의 태그와 태그 테이블 태그 추가 차이?" →
  "모니터링 트렌드 추가 창" → "AI 채팅 트렌드 셋 같다"
- 확인: 캔버스 "태그" 탭 = tb_monitoring_catalog(모니터링 카드 구성, 9행,
  setup/trends 와 동일 다이얼로그). AI 채팅 트렌드 조회는 별도 정본
  tb_trend_catalog(333행, CRUD UI 없음 — 구축 시드) 참조. 두 카탈로그는
  다른 테이블 (다이얼로그의 "기존 트렌드 참조"로 가져오기만 연결)
- 조치: 탭 라벨 "태그"→"모니터링" (front 커밋)

## 트렌드 카탈로그 일원화 (사용자: "진행해줘 — 구축 편의성·직관성 목적")

- 정본 tb_trend_catalog 하나로 통일 (Migration 0122 — 모니터링 9행 이관,
  동명 시드 6행은 사용자 items 병합+표시 승격, legacy rename)
- 모니터링 화면=show_monitoring 큐레이션 / 채팅·이상감지=전체 행.
  시드 행 UI 편집·삭제 차단 가드
- E2E: UI 로 남산10 소블록 '압력' 카탈로그 추가 → 채팅 "압력 트렌드"
  미등록 에러 → 정상 조회 전환 (구축→채팅 개방 실증). 기존 카드 9건
  유지, 시드 보호 확인, 스모크 16/16. 프런트 무변경
- 커밋: slm (본 커밋 병기) / web (본 커밋)
