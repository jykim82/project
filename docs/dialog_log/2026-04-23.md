# 2026-04-23 대화 로그

## 주요 요청 · 변경 요지

### 1. 채팅 피드백 대시보드 집계 확장 (#159)
- 사용자: "2,3 순서로 하고, 방금한 다각화는 어떤 페이지에서 확인?"
- `GET /chat/feedback/stats` 엔드포인트 추가 (`slm@1a54b84`)
  - totals: 총/긍정/오답/처리대기/평균 리뷰 시간/성공률
  - by_intent: 인텐트별 긍정·오답·wrong_rate (Top 10)
  - weekly: 최근 N 주 피드백 추이
- 프런트 `/admin/chat-feedback` 상단 6 KPI + 12주 스택 막대 +
  인텐트 오답 순위 (`slm-dashboard@589dfe6`)
- 기존 overallStats/intentStats 필터 범위 집계 제거 → 전 기간 stats

### 2. 긴 페이지 섹션 접기/펴기 (#160)
- 사용자: "페이지를 아래로 스크롤해야하는 페이지가 많아 ... 줄임버튼"
- 재사용 `components/ui/collapsible-section.tsx` 신설:
  - `<CollapsibleSection>` 단순 Card 교체용
  - `usePersistedCollapse()` 훅 + `<CollapseToggle/>` (복잡한 Card)
  - localStorage `collapse:<key>` 로 상태 유지, 기본 펼침
- 적용 4 페이지 10 섹션:
  - `/alarm`: 요약/필터/목록 3섹션
  - `/monitoring/alarm-calendar`: 히트맵/추이 2섹션
  - `/monitoring/equipment-health` 개요탭: 월별/Top20/MTBF 3섹션
  - `/network`: 토폴로지/통신이상 2섹션
- 가시성 개선 2차: ghost + 아이콘 only → outline + "접기"/"펴기" 텍스트

### 3. 위기대응 > 경보관리 접기/펴기 확장 (#161)
- 사용자: "위기대응에 경보관리도 적용"
- `/crisis/alarm-dashboard` 현황 3 + 이력 3 = 6 섹션 추가

### 4. 접기/펴기 구현 통일 (#162)
- 사용자: "이전에 접기펴기와 현재구현한 접기펴기가 구현 내용이 다르네 통일"
- 알람/위기대응의 bg-muted SectionBar → SectionHeader(제목+CollapseToggle)
  패턴으로 교체. 6 페이지 전체 섹션 시각 일관화.

### 5. 진행중 경보 "확인" 처리 활성화 (#163)
- 사용자: "진행중 경보 분석에서 확인은 어디서 하는거야?"
- 버그 발견: `/crisis/alarm-dashboard` 현황 탭에서 `AlarmReportTable`
  에 `onConfirm` 콜백 미전달 → 확인 버튼 무반응
- 수정: `DashboardTabContent` 에 `handleConfirm` prop 주입 +
  성공 시 `refreshDashboard()` 호출
- UX: ghost 아이콘 버튼 → outline amber `[⚠ 확인]` 텍스트 버튼

### 6. 전체 확인 일괄 버튼 (#164)
- 사용자: "전체 확인 버튼도 있으면 좋겠네"
- `AlarmReportTable` 에 `onConfirmAll` prop 추가
- 미확인 경보 ≥1 시 헤더 우상단 `[✓✓ 전체 확인 (N건)]` 버튼
- window.confirm → Promise.all 병렬 처리 → 결과 토스트
- 현황/이력 탭 모두 적용
- E2E: 53건 중 15건 미확인 상태에서 버튼 정상 노출 확인

### 7. 합덕3 소블록 압력 현황 SQL 성능 조사
- 사용자: "합덕3 소블록 압력 현황은? 질의가 느린 이유는?"
- 관찰: 블록/소블록 13초, 배수지 5ms (차이 2600배)
- 조사 결과:
  - SQL: `fn_pressure_avg_summary()` (example3.json:1817-1829)
  - CTE 2개 (month_avg_data, year_avg_data) 가 tb_tag_raw_data
    92K~93K 행 전체 JOIN/집계
  - 블록은 압력 태그 5~13개로 많아 누적 I/O 폭증
  - 배수지는 압력 태그 0~1개라 즉시 종료
- 권장 수정 (미적용 — 사용자 확인 필요):
  - 옵션 A: tb_tag_info 먼저 필터링 후 tb_tag_raw_data JOIN
    (예상 385ms → 2~3ms, 99% 감소)
  - 옵션 B: 복합 인덱스 INCLUDE (tagsn, logtime DESC) INCLUDE (val)

## 커밋 시퀀스

**slm**: `1a54b84` (stats endpoint)

**slm-dashboard**: `589dfe6` → `e07f1ed` → `c3664af` → `30c6a17` →
  `67ac9da` → `b896cae` → `968c6c5`

**web**: `a133d0b` → `e13c44f` → `86bcc31` → `ac88587` → `1755800` →
  `425394f` → `10d81c5`

## 데이터 상태 복원 필요 (테스트 흔적)
- `tb_equipment_alarm_report` 진행중 53건 중 15건을 "전체 확인" 버튼
  테스트용으로 `alarm_confirm_yn=NULL` 로 복원해둠.
  필요 시 사용자가 UI 에서 "전체 확인" 눌러 정상화.
