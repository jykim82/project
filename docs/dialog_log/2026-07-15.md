# 2026-07-15 세션 대화 로그

## 인텐트 아키텍처 검토 → 1단계 (메타 단일화) 완료

### 사용자 질문·결정
- "AI_SERVER에서 JSON 질의 응답이 늘어가면 점점 늘어나는 구조인데 현재 프로그램
  구조가 맞나?" → 진단 보고: 선언적 코어(example3.json)는 맞음, 한계는 **분산
  등록**(인텐트 1개 추가에 최대 6곳 수정 — 실회귀 4건 전부 이 패턴). 레지스트리+훅
  3단계 전환 제안 → "응 진행해줘"

### 1단계 변경 (`slm`)
- `example3.json`: 인텐트 정의에 선택 메타 필드 도입
  - `dynamic_sql: true` — 16개 마킹 (기존 하드코딩 12 + _SUPPLY 4)
  - `classify_keywords.stage1` — 6그룹 27키워드 (교차검증/설비장애/센서스캔/
    물수지/누수/표준편차 — 회귀 이력으로 하드코딩에 추가됐던 것들을 인텐트 정의로)
- `intent_matching.py`: `dynamic_sql_intents()` / `stage1_keywords_from_definitions()` 파생 함수
- `ai_server.py`: SSE 인라인 하드코딩 세트 제거 → 모듈 로드 시 파생 1회
- `intent_classifier.py`: 하드코딩 리스트에 인텐트 무관 공통 신호만 남기고
  파생 키워드 합산 (lazy import + 캐시)

### 검증
- 파생 dynamic 세트 == 기존 하드코딩+_SUPPLY (16개 완전 일치)
- 파생 stage1 ⊇ 이관 27키워드 (누락 0)
- 스모크 16/16 통과 + 파생 키워드 실매칭 로그 확인 ('교차 검증'/'물 수지'/'설비 장애')

### 문서·메모리
- `docs/intent-architecture-spec.md` 신설 (1단계 완료 + 2단계 핸들러 레지스트리·
  3단계 card_type 계획) + CLAUDE.md 인벤토리 등록
- 메모리 `feedback_intent_category_keyword_sync` 개정 — 새 규칙: 키워드는
  example3.json 한 곳 선언 (두 곳 동시 갱신 규범 폐지)

## 2단계 — 핸들러 레지스트리 완료: 42/42 분기 이관 (사용자: "계속 이관하고 완료되면 테스트")

### 커밋 체인 (4배치, 배치마다 게이트)
- 1차 `2b9adb7`: 프레임(intent_handlers/base — IntentContext·레지스트리·서비스
  getter 주입) + 교차검증/설비장애/물수지/tagtype 5개
- 2차 `82d5f36`: prepare 훅 신설 + 17개 (CUSUM·SUPPLY 기간/ANOMALY 필터/PIE/
  ONGOING/ALARM_ABNORMAL/트렌드·nmf·stddev 템플릿/CATALOG/HUNTING/CAUSE/NETWORK)
- 3차 `99a9b6f`: early-return 지원(final_response/progress_message) + 13개
  (SCAN_ALL 캐시/STDDEV·SUMMARY·TAG_DAILY early/CUSUM·NIGHT_STATUS·TIMESERIES 조달)
  + from_ts==to_ts 보정을 훅 앞으로 이동 (조달 순서 보존)
- 4차 `3a35c41`: post_sql/post_process 훅 + 7개 (알람 7일 폴백/SCAN_ALL 교차검증
  SQL 폴백 경로/트렌드 변수/stddev·CUSUM response_extras)

### 결과
- **event_generator 인텐트 분기 0개** — 651줄 순수 파이프라인
  (분류→추출→prepare→pre_sql→실행→post_sql→process→post_process→extras→응답)
- ai_server 4,374(2단계 전)→3,507줄. 도메인 모듈 6개(anomaly/alarm/trend/
  night_min_flow/reservoir/tag_query)

### 최종 테스트
- 스모크 16/16 (배치마다 + 최종)
- /ask 68케이스 구조 비교: 최종 불일치 0건 (중간 배치 차이 2건은 시간 민감
  태그·기준선 일시 실패로 규명)
- 실 UI: 트렌드 질의 — 핸들러 템플릿 요약·평소 대비/향후 전망·차트·추천질의 렌더 확인
- 사양 §2단계 완료 갱신 (훅 5개 표 + 커밋 체인)

## 3단계 — card_type 선언화 (완료, 사용자: "이어서 진행해줘")

### 변경
- 백엔드 `slm@9156d70`: example3.json card_type 11종 선언 +
  build_success_response 자동 주입 (인텐트→card_type 1회 캐시, 빌드 지점 전체 적용)
- 프런트 `slm-dashboard@3cc666c`: mapper ANOMALY 게이트 / BotMessage isCard()·
  평문 억제 3종 / VisualRenderer alarm 특례 — 전부 card_type 우선
- **하위호환 불변식**: 히스토리 저장 응답엔 card_type 없음 → 인텐트 폴백 유지 (제거 금지)

### 검증
- SSE probe: alarm_history/cross_facility 주입, 카드 없는 인텐트 미주입
- 스모크 16/16 / 신규 응답 카드+평문 억제 정상 / 과거 대화(07-12 이상 이력)
  재렌더 카드 정상 (폴백 경로)
- 남은 후속: Tier 3 카드 갤러리, Tier 2 Playwright 스모크

## Tier 2·3 — 프런트 스모크 + 카드 갤러리 (완료, 사용자: "이어서 진행해줘")

### Tier 2 (`slm-dashboard@30974de`)
- @playwright/test 기반 e2e: playwright.config.ts + auth.setup(로그인
  storageState) + chat-smoke.spec 4케이스 (오라우팅/장애기록 라우팅/카드+평문
  억제 2종). `npm run test:e2e` — 5/5 통과 (7.6s).

### Tier 3 (`slm-dashboard@875a2b6`, migration 0097)
- /admin/chat-gallery — 대표 질의 12종 실행→실제 BotMessage 렌더 (card_type·
  응답시간 라벨, 개별/전체 실행). 사이드바 M100-16 + tb_menu/tb_auth_menu.
- 검증: 12/12 성공 (카드 7 + 비카드 5), 오류 0. 갤러리 스크린샷 확인.

## 후속 정리 — 사전집계 갱신 확인 + 비상연락처 6.4s 해소 (사용자: "웅")

- 야간최소유량 사전집계 자동 갱신: **이미 해결돼 있었음** (07-10
  _night_min_flow_agg_loop, 24h 주기). 실증(max=07-14 최신) 후 review-items ✅ 표기.
- 비상연락처 분류 6.4s → 0.1s (`slm@HEAD`): Stage1 키워드 누락 → SLM 폴백이
  원인. EMERGENCY_CONTACT_QUERY 에 classify_keywords.stage1 JSON 선언으로 해소
  (1단계 아키텍처 경로). 스모크 16/16, 스위트 총 7s.
- 주의: example3.json 만 변경 시 uvicorn(.py 감시)이 리로드하지 않음 —
  운영에서 JSON 키워드 변경 후엔 백엔드 재기동 필요.
