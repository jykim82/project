# 2026-07-28

## QR 제외 + A-4 경보 확인 책임 추적 (Migration 0131)

**요청:** "QR 은 사실 적용되어 있는게 없어" → 선택지 제시 → **제외 (재제안
금지)** 결정, memory 저장 (project_no_qr_labels.md). "이어서 진행해줘"

### 압축 마감 확인
새벽 3시 정책 job(1008) Success — **120 GB → 14 GB** (23/26 청크, 최근
청크만 비압축 정상). 압축 건 종결.

### A-4 경보 확인 책임 추적 — docs/alarm-confirm-audit-spec.md
- 문제: alarm_confirm_yn='Y' 만 있어 "누가 언제" 부재 — 감사 질문에 답
  불가, 3교대에서 확인 책임 증발. 확인율 0.3% 개선의 전제
- Migration 0131: confirmed_by/confirmed_at. **기존 확인 건은 NULL 유지**
  (소급 추정 금지 — 감사 컬럼에 추정값 넣으면 컬럼 전체를 못 믿음)
- 기록 규칙: **최초 확인만** (재확인이 덮으면 감사 무의미 — CASE/COALESCE).
  confirm + resolve(비전 해제도 확인의 한 형태) 양쪽. 자동해제 루프는 기록
  안 함 — 시스템 해제와 사람 인지는 다른 사실
- user_id 는 세션에서 명시 전달 (코드베이스 패턴. api→store→page 관통)
- 표시: 확인 컬럼에 확인자 id + 툴팁 "jykim 확인 · 시각". 과거 건은
  "기록 도입 전" 툴팁
- 검증: API 확인 → jykim 기록 / other_user 재확인 → **jykim 유지** /
  UI 클릭 → DB 기록 + 이력 표시 렌더 확인 / 테스트 확인 2건 원복 /
  tsc 0 · 콘솔 에러 0
- P2 예고: 미확인 메신저 상신, 확인율 추이 통계

---

## A-3 임계 도달 예측 (사전 표출)

**요청:** "응" (A-3 진행)

- 대상 조사: tagsn 명명 규칙으로 LEI(수위 측정)↔LEC(HH/LL 설정) 페어링 —
  55개 중 **41개 페어 성립**. 압력은 임계 공백(목표값 부재)이라 제외
- 계산: 최근 60분 **원시** 선형회귀(regr_slope) → (임계-현재)/기울기.
  cagg 5min 은 refresh 지연으로 60분 내 2~3버킷뿐이라 부적합 실측 →
  원시 직접 집계 (logtime 하한, E-056 원칙. 41태그 89ms)
- 게이트: R²>0.5 (출렁임 오탐 차단) · |기울기|>0.001m/분 (정체 제외) ·
  horizon 240분 (수위 일주기라 먼 외삽 무의미)
- 같은 설비 복수 임계는 "먼저 도달하는" 것 선택 (사전 신호는 보수적으로)
- **예측은 경보가 아니다** — tb_equipment_alarm_report 에 안 씀 (감사·통계
  오염 방지). 조회 시점 계산, cron 없음
- `GET /alarm/approach` + 경보관리 현황 탭 "임계 도달 예측" 패널 (5분 갱신).
  0건일 때 "41태그 감시 중, 접근 없음" — 침묵과 구분
- Chronos 향후 전망과 목적 구분을 사양 §5 에 명문화 (합치지 않는다)
- 검증: 실데이터 난지마을 수위 하강(R²=1.00) LL까지 412분 검출(horizon 밖
  이라 기본 화면엔 미표출 — 의도), 패널 렌더·콘솔 에러 0

---

## 사이클 마감 정리 (이어서 진행)

**요청:** "응 이어서 해줘"

- **work-history.md** 에 사이클 섹션 추가 (압축+E-056+A-1~A-4 — 밀려 있던
  기록 의무 이행) + **memory 영속 저장** (project_alarm_trust_cycle.md —
  잠식 방지 접기 패턴·예측은 알람 테이블에 안 씀 원칙 포함)
- **카탈로그 원고 갱신** (catalog-draft.md)
  · p.8 "경보 신뢰 관리" 신설 — 반복 경보 정리(실측 80% 잠식 정상화)·
    임계 도달 예측·확인 책임 추적·교대 인수인계
  · p.14 "저사양 서버 최적화" 신설 — 압축 120→14GB·1년 조회 25초→2초·
    GPU 없는 최소 사양 동작. 저장소 각주 추가
  · brochure-draft.md 차별화 ② 에 경보 신뢰 한 줄
  · **HTML 판 동기화는 보류** — 원고(md)가 정본, HTML 은 확정 필요 항목
    반영 시점에 일괄 (제작 직전)
- **로드맵 부록 B — 미결정 권고안** (결정 아님, 착수 시 출발점)
  · A 도구 주체=룰 라우터 (LLM as announcer 정합) · B 충돌=안전 신호
    불가침 · B enum=4종(연락 체계는 비상연락처와 중복 제외) · C 초기=
    30건 미만 가중치 유보+채터링 우선 노출 · E=PWA (폐쇄망 앱스토어 부재)
    · 순서=C→D→B→A→E

---

## 로드맵 C — 오탐 피드백 루프 P1 (Migration 0132)

**요청:** "진행해줘" (로드맵 C 착수 승인)

- **3분법 판정** real/false/check — 기존 is_false_alarm('N' 기본값)은
  "실제 확인"과 "미판정"을 구분 못해 오탐률 분모가 안 섰음
- 정본=anomaly_label, is_false_alarm 은 파생 동기화 (false→Y, 그 외·해제→N.
  해제도 N — 검증 중 발견한 비일관성 수정: 해제인데 파생 Y 잔존)
- 백필 1,457건 ('Y'→'false'), labeled_by/at NULL (판정자 소급 금지 — 0131 원칙)
- 판정은 확인(0131)과 달리 **정정 허용** — 판단이므로. 마지막 판정자·시각 보존
- UI: 이력 목록 [실제][오탐][점검] 1클릭 (재클릭 해제, 진행중 목록엔 미제공)
  + 경보관리 4번째 탭 "오탐 분석" — 주별 추이 차트(판정 0건 주는 null 단선)
  + 분류별 표 + 가중치 유보 배지 (30건 미만, 부록 B.4)
- 주 기준 = 경보 발생 시각 ("그 주 경보가 얼마나 오탐이었나"가 실적)
- 판정률 5% 미만 배너 — 반복 경보 상위부터 판정 유도 (축적 가속)
- 가중치 자동 조정은 P2 (소표본 과적합) · 채팅 오답 피드백과 별도 체계 유지
- 검증: API 왕복(real→false→해제, 파생 동기화) / UI 판정→강조→해제 /
  통계 탭 렌더(총 11,083·라벨 55·유보 배지) / 테스트 라벨 원복 / tsc 0 ·
  콘솔 에러 0

---

## 도달 예측 정확도 검증 (사용자 질문: "향후 전망은 AI가 더 정확하지 않나")

- 답의 구분: 트렌드 "향후 전망"은 **이미 Chronos-Bolt** (+46.9% 실측).
  임계 도달 예측은 질문이 달라 선형 유지 — Chronos 는 "가장 그럴듯한
  미래"라 개입 실패(경보가 필요한 상황)를 구조적으로 가림
- **백테스트 실측** (90일 수위 도달 사건): 30분 전 ±16.8분(16건) /
  60분 전 중앙 ±20.0분·p90 ±55.6분(247건) → **분 단위 표시는 거짓 정밀**
- 조치: ETA 를 구간 표시로 변경 (30분 내/1시간 내/1~2시간/2~4시간),
  정밀값·오차는 툴팁. 사양 §2.1(실측)·§2.2(왜 AI 아닌가) 신설

---

## 네트워크 토폴로지 계층형 우선

**요청:** "force와 계층형 중에 계층형이 먼저 나타나도록 순서를 바꿔줘"
→ 토글 버튼 순서 [계층형][Force] + **기본 뷰도 계층형**으로. 검증: 첫
진입에 계층형 레인(CORE/배수지/가압장/소블록) 렌더, 콘솔 에러 0
