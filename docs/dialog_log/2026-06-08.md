# 2026-06-08 대화 로그

## 1. 서비스 기동 + Vision Agent 진단 테스트
- **요청**: 서비스 기동, Vision Agent 용도 설명, 채팅에 사진 첨부 후 비전 진단 테스트
- **확인**: docker compose 6개 컨테이너 + 호스트 네이티브 Ollama(:11434) + Vision Agent(:8100)
  기동 (venv python 사용). `gemma4:26b-a4b-it-q4_K_M` 모델 로드.
- **테스트**: `/chat` 페이지에서 LS XGT PLC 사진 + "이 장비가 뭐야? 상태 진단해줘" 전송.
  약 82초 후 응답 — 장비 추정 (LS XGT PLC, 신뢰도 100%) + 10가지 관찰 상태 +
  `[AI 참고 의견]` 접두어 + 면책 고지.
- **발견 이슈**: `vision_agent` 가 호스트 네이티브 실행인데 DB 호스트명을 `timescaledb`
  (Docker hostname) 으로 시도 → RAG/FAULT_CASE 인덱스 로드 실패. 매뉴얼·고장 케이스
  RAG 사용 못함. 추후 수정 필요.

## 2. GIS 관망도 클러스터 표시 확인 (미해결)
- **요청**: GIS 관망도에서 지점개소를 원형으로 개소 숫자 표시가 다시 롤백되어 있는지 확인
- **조사**: `GisFacilityMarkers.tsx` (lines 132-158) 클러스터 원형 + 흰색 숫자 코드
  살아 있음. 화면에서도 표시되지만 매우 작아 식별 어려움. 실제 "롤백"으로 보이는
  변경은 commit log에서 찾지 못함. **사용자 추가 정보 필요** (스크린샷 등).

## 3. 관리 메뉴 — GIS/EPANET 토글 가시성 제어 추가 (완료)
- **요청**: GIS 관망도의 [물흐름 표시], [EPANET 시뮬], [실측 유량 차이] 토글을
  관리 페이지에서 숨길 수 있는 옵션 추가.
- **분석**:
  - `gis-flow` (EPANET 시뮬) → `/admin/epanet` 에서 이미 토글 가능 ✓
  - `flow-deviation` (실측 유량 차이) → 이미 토글 가능 ✓
  - **`물흐름 표시`** → 항상 노출 (2026-05-10 사양 "마스터 OFF 와 무관") → 신규 작업 필요
- **사양 충돌 검토**: 마스터 토글 독립성 원칙 유지 + 자체 menu_key 로 별도 제어 가능
  (기존 사양과 호환).
- **구현**:
  - 백엔드: `endpoints/epanet.py` `_MENU_REQUIREMENTS` 에 `gis-flow-arrow` 추가
    (required/recommended 빈 배열 → 데이터 품질 게이트 없음)
  - DB: Migration `0074_epanet_gis_flow_arrow_menu.sql` — region별 row seed
  - 프론트: `monitoring/gis/page.tsx` [물흐름 표시] 버튼을 `isEpanetEnabled("gis-flow-arrow")`
    로 감싸기 + `DataQualityCard.tsx` MENU_LABEL 추가
- **검증 (Playwright)**:
  - `/admin/epanet` → "물흐름 표시 (GIS)" 토글 노출 (활성 상태)
  - 토글 OFF → `/monitoring/gis` 에서 [물흐름 표시] 버튼 사라짐 (button.length === 0)
  - 토글 ON 복원 → 버튼 재노출
- **문서**: `docs/epanet-menu-spec.md` 변경 이력 §2026-06-08 추가

## 4. NextAuth 멀티 호스트 지원 — 화면 깜빡임 fix
- **증상**: 사용자가 `https://192.168.219.105:3000/admin/menus` 에 머물면 약
  70~80초 주기로 전체 화면 깜빡임 + 스크롤 위치 초기화.
- **원인**:
  - 컨테이너 `NEXTAUTH_URL=https://localhost:3000` 고정
  - 사용자 실제 접속: LAN IP `192.168.219.105:3000`
  - 쿠키 `__Secure-next-auth.callback-url=https%3A%2F%2Flocalhost%3A3000` 으로
    callback URL 과 요청 origin 미스매치 → SessionProvider 가 주기적으로
    재발급 시도하면서 컴포넌트 reflow 발생.
- **조사 경로**:
  - Playwright 로 dashboard·GIS·admin/menus 각각 2~3분 모니터링 → reload 0회
    (localhost 접속이라 미스매치 미발생). 코드만 봐서는 단서 없음.
  - 사용자 DevTools Network 캡처 — `data-quality?region=R01` 14회/16분 호출.
    Initiator `api-client.ts:42`. 호출 간격 약 70초.
  - Request Headers 확인 → `cookie: __Secure-next-auth.callback-url=...localhost:3000`
    이 결정적 단서. NEXTAUTH_URL 환경 변수가 localhost 로 고정된 것 확인.
- **해결**:
  - `src/app/api/auth/[...nextauth]/route.ts` — request host 헤더로 매 요청마다
    `process.env.NEXTAUTH_URL` 런타임 오버라이드. `x-forwarded-proto` 우선,
    없으면 localhost 만 http 로 다운그레이드.
  - `src/lib/auth.ts` — `useSecureCookies: true` 명시. 멀티 호스트 모두 HTTPS
    가정이라 host-only 자동 스코핑으로 호스트별 별도 세션.
- **사용자 액션 필요**: LAN IP 에서 1회 로그아웃 → 재로그인 (기존 stale
  callback-url 쿠키 갱신용).

## 5. LAN IP 깜빡임 — 진짜 근본 원인 발견 + 수정 (E-034)
- **재현 지속**: 사용자 로그아웃·재로그인 후에도 동일 증상 + Edge 에서도 동일
  → 브라우저 무관 / NextAuth 멀티 호스트 fix 만으로 부족.
- **결정적 단서 (Edge DevTools 스크린샷)**:
  - `webpack-hmr` websocket 이 3회 재연결 (web-socket.ts:5...)
  - Next.js 콘솔: **"Blocked cross-origin request from 127.0.0.1 to /_next/*
    resource. To allow this, configure allowedDevOrigins"**
  - "Finish: 5.08s" + DOMContentLoaded 라벨 → 페이지가 방금 reload 됨
- **원인 2 중 복합**:
  1. `next.config.ts` `allowedDevOrigins: ["*.trycloudflare.com"]` 뿐 →
     LAN IP origin 의 `/_next/*` 요청 차단 → HMR ws reconnect 반복 →
     Turbopack stale chunk 감지 시 페이지 reload
  2. cert SAN 에 `localhost / 127.0.0.1 / ::1` 만 → LAN IP wss 검증 불안정
- **해결**:
  1. `next.config.ts` `allowedDevOrigins` 에 사설망 와일드카드 추가 —
     `127.0.0.1 / 192.168.*.* / 10.*.*.* / 172.16~31.*.*`
  2. `certs/localhost.pem` 재발급 — 호스트의 모든 LAN IP 를 SAN 에 포함
     (`192.168.219.105` 외 4개 인터페이스)
  3. frontend 컨테이너 재시작 → 새 cert + config 즉시 적용
- **검증**: 사용자가 `https://192.168.219.105:3000/admin/menus` 에서 정상
  확인.
- **추가 산출물**:
  - `certs/README.md` — 신규 머신 셋업 + cert 재발급 가이드 + E-034 참조
  - `docs/error-management.md` E-034 신규
  - `.gitignore` 에 `/certs/*.pem` `/certs/*.exe` 추가 (private key 보호)

## 6. 별건 발견 — /admin/menus "메뉴 트리" 탭의 숨김 토글이 localStorage 기반
- **사용자 보고**: "엔진 재시작 후 메뉴 숨김이 초기화" → DB 저장 안 됨 확인
- **현재 구현 분석**:
  - `sidebar-menus.ts` `HIDDEN_MENUS_KEY = "slm-hidden-menus"` 로 localStorage
    에만 저장 (per-browser)
  - `MenuTreeView.tsx` 의 표시/숨김 토글이 `toggleMenuHidden` 호출 →
    DB 미반영
- **이미 존재하는 DB 컬럼**: `tb_menu.use_yn` (default 'Y') — 사이드바 메뉴
  use_yn='N' 처리 인프라 가능
- **TODO** (별도 작업으로 후속): /admin/menus "메뉴 트리" 탭의 숨김 토글을
  DB 기반으로 마이그레이션 — 백엔드 PUT 엔드포인트 + useSidebarMenus 에서
  use_yn='Y' 메뉴만 노출.
- **관련**:
  - `tb_epanet_menu_setting` 은 이미 DB 기반 (EPANET 토글 11개)
  - `tb_auth_menu` 권한 매트릭스도 DB 기반

## 7. /admin/menus 메뉴 숨김 — localStorage → DB 전환 (사용자 후속 요구)
- **사용자 요구**: "엔진 재시작해도 메뉴 숨김 유지" — DB 영구 저장 필요.
- **인프라 점검**:
  - 백엔드 `PUT /api/auth/menu-visibility` 이미 존재 (auth_crud.py:818-838) —
    body `{menu_idn, use_yn}` 로 `tb_menu.use_yn` 업데이트.
  - 백엔드 `/auth/me` 의 메뉴 쿼리는 이미 `AND m.use_yn='Y'` 필터 (line 431) →
    use_yn='N' 메뉴는 사이드바에 노출 안 됨.
  - 프론트 `lib/api/admin-menu-api.ts:84` `updateMenuVisibility` 함수 존재.
  - **누락된 연결**: `MenuTreeView.handleToggleVisibility` 가 localStorage
    `toggleMenuHidden` 만 호출, 백엔드 호출 없었음.
- **수정** (`src/components/admin/MenuTreeView.tsx`):
  - mount 시 `fetchMenuPermissions()` → `use_yn='N'` 메뉴 IDs 로 `hiddenIds`
    초기화 + localStorage 도 DB 기준으로 정합화.
  - 토글: localStorage 즉시 갱신 (UI 반응) + `updateMenuVisibility` 호출 (DB
    영구). DB 실패 시 localStorage 되돌리기 (불일치 방지).
  - 토스트 메시지 "(새로고침 후 적용)" 제거.
- **검증**:
  - `UPDATE tb_menu SET use_yn='N' WHERE menu_idn='M200-14'` → `/auth/me`
    응답 메뉴 수 58→57 (M200-14 제외 확인).
  - `docker restart slm-backend` 후에도 DB 그대로 → `/auth/me` 응답 동일.
  - 사용자 환경에서 토글 → DB 영구 저장 → 엔진 재기동 후 유지 확정.
- **별건 발견**: 정적 `sidebarMenus` 에 있지만 `tb_menu` 에 누락된 메뉴 존재
  (예: `M200-15 고장 진단 케이스`). CLAUDE.md §메뉴 구조에 "신규 페이지 추가
  시 sidebar-menus.ts + tb_menu INSERT 둘 다" 명시되어 있으나 누락. 추후 정합
  스크립트 추가 권고 — 본 작업 범위 외.
- **MASTER self-lockout 경고**: 만약 MASTER 가 `/admin/menus` (M100-2) 자체를
  hide 하면 사이드바에서 못 들어감. `/auth/me` 의 use_yn 필터가 모든 사용자
  공통이라 발생. URL 직접 입력 (`/admin/menus`) 으로 진입 가능하므로 복구 가능.

## 8. 정책 통일: hide = 안 보임 (MASTER 포함)
- **사용자 의견**: "메뉴를 hide 하면 안 보여야 하는 게 맞다" — MASTER 우대
  정책 불필요.
- **현재 동작 점검**:
  - 정상 인증 — 사이드바 (/auth/me 응답): MASTER 포함 모두 use_yn='N' 숨김 ✓
  - 미인증/API 실패 fallback (`buildStaticMenus`): "MASTER 는 숨김 무시"
    예외 코드 있음 — 정책 불일치.
  - `/admin/menus` 관리 섹션: hide 된 메뉴도 표시 (재토글 복구 경로 — 의도적)
- **수정** (`src/hooks/use-sidebar-menus.ts`):
  - `buildStaticMenus` 의 `authIdn !== "MASTER"` 가드 제거. 모든 사용자에게
    hiddenIds 필터 적용. 정책 코멘트 갱신.
- **MASTER lockout 복구**: 사이드바에서 `/admin/menus` 가 hidden 이어도
  URL 직접 입력으로 진입 가능 — 관리 섹션은 정적 sidebarMenus 전체 표시
  (의도적). 완전 lockout 위험 없음.

## 9. 기능 SKU·feature flag 사양 v1 — 관망수리분석(EPANET) 마스터 토글 (B1)
- **요구**: EPANET 관련 메뉴를 모듈 단위로 일괄 ON/OFF. 데이터 미구축 사이트는
  의미 없으므로 **기본 OFF** (master 명시 활성화 시만 노출).
- **사양 신규**: `docs/feature-sku-spec.md` v1 — 두 레벨 정책 (SKU vs 운영
  hide), 모듈 지도 (B1~B5), EPANET 마스터 토글 구조, 후속 모듈 예고.
- **DB**: Migration 0075 — `tb_comm_code(SITE_SETTING.EPANET_ENABLED)` 전 region
  `use_yn='N'` reset (R01 → N).
- **백엔드**:
  - `endpoints/epanet.py` `_check_data_quality` — 마스터 OFF 시 11 menu_key
    모두 `menus_disabled` 자동 포함 (gis-flow-arrow 제외 — 마스터 독립).
  - `endpoints/auth_crud.py` `/auth/me` 응답에 `features: { epanet: bool }`
    추가. 후속 모듈도 동일 패턴.
- **프론트**:
  - `/admin/site-settings` 카드 라벨 "EPANET 수리 시뮬레이션" → **"관망수리분석"**
    + 설명 보강 (영향 메뉴 + 기본 OFF 명시).
  - 토글 액션 후 `invalidateEpanetDataQuality(R01)` 호출 → 사이드바·GIS
    토글 즉시 갱신.
  - 사이드바·GIS 토글 분기 코드 변경 없음 — 기존 `dataQualityKey` + `statusOf`
    인프라가 백엔드 `menus_disabled` 응답을 자동 흡수.
- **검증 (Playwright)**:
  - OFF (기본): 사이드바 EPANET 11 메뉴 + GIS 토글 7개 모두 hidden,
    `gis-flow-arrow`·시설물 목록 유지. `/admin/epanet` 복구 경로 노출.
  - ON 토글 → DB use_yn='Y' + 사이드바/GIS 즉시 노출.
  - OFF 복원 → `docker restart slm-backend` → DB 'N' 유지 + 사이드바/GIS
    재차 hidden.
- **MASTER lockout**: M100-12 `/admin/epanet` 는 dataQualityKey 없으므로
  마스터 OFF 여도 항상 사이드바 노출 (복구 경로 자동 보장).
- **CLAUDE.md 인벤토리**: feature-sku-spec.md 항목 추가.
- **후속 모듈 (B2~B5)**: 동일 패턴 — Vision Agent / 매뉴얼 RAG / 트렌드 비교 /
  보고서 자동 생성. 사용자 결정 후 Phase 2 진행.

## 10. M100-12 (/admin/epanet) hide 확장 — 사용자 결정 2026-06-08
- **요구**: 관리 메뉴의 EPANET 시뮬레이션 (M100-12) 도 마스터 OFF 시 사이드바 hide.
- **변경**:
  - 백엔드 `_MENU_REQUIREMENTS` 에 `epanet-admin` 키 추가 (마스터 OFF 시 자동
    menus_disabled 포함).
  - Migration 0076 — `tb_epanet_menu_setting` 에 `epanet-admin` row 시드.
  - 프론트 `MENU_DATA_QUALITY_KEY['M100-12']='epanet-admin'` 매핑 + 정적
    sidebar 정의에도 동일 dataQualityKey 부여.
- **복구 경로 변경**: `/admin/epanet` 도 hide 되므로 `/admin/site-settings` 가
  유일 복구 경로 (URL 직접 진입 가능, 사이드바에도 항상 노출).
- **검증**: 마스터 OFF → 사이드바·관리 dropdown 에서 /admin/epanet 사라짐 ✓,
  /admin/site-settings·/admin/menus 노출 유지 ✓ (복구 경로 보장).
- **사양**: docs/feature-sku-spec.md §3.2 admin 페이지 항목 변경 + §3.4 복구
  경로 갱신.

## 11. 비상연락처 인프라 점검 + AI 채팅 인텐트 신규 (A+B+C)
- **사용자 보고**: 구축 > 비상연락처 페이지가 비어있음. AI 채팅에서 UPS 고장
  등 업체 전화번호 활용 의도.
- **점검 결과**:
  - DB `tb_alarm_contact` 10건 (UPS 3 / 정전 3 / 네트워크 2 / 밸브 1) 존재
  - **region 미스매치**: 다른 테이블 모두 'R01' 인데 tb_alarm_contact 만
    'water' (레거시) → 페이지 빈 화면
  - 프론트 API 가 `http://localhost:8000` 직접 호출 (apiClient 미사용) →
    LAN IP 환경에서 CORS·인증 위험
  - AI 채팅에 비상연락처 활용 인텐트 없음
- **A (UI fix)**:
  - Migration 0077 — `UPDATE tb_alarm_contact SET region='R01' WHERE region='water'` (10건)
  - 백엔드 `endpoints/alarm_contacts.py` default region 'water'→'R01' (전체 4 occurrence)
  - 프론트 `src/lib/api/alarm-contacts-api.ts` 전면 재작성 — apiClient 사용 + default 'R01'
  - Playwright 검증 → /setup/alarm-contacts 표 10건 정상 표시
- **B (AI 채팅 인텐트)**:
  - `example3.json` 에 `EMERGENCY_CONTACT_QUERY` 추가 (75번째, graph_type=table)
  - `intent_classifier.py` Stage 2 키워드 단축 — 비상연락/긴급 연락/연락처/
    연락망/어디로 전화/누구한테 전화 등 → 카테고리 무관 최우선 분기
  - SQL: 단일 region 환경 가정 (region WHERE 절 제거, sql_executor 의 region
    placeholder 인프라 부재. 멀티 region 지원 시 추가 작업 권고)
  - Playwright 검증: "비상연락처 알려줘" → 표 10건 응답 + summary + reference 정상
- **C (사양)**:
  - `docs/emergency-contact-spec.md` v1 신규 — 데이터 모델 + 관리 UI + 채팅 인텐트 +
    향후 확장 (카테고리 필터링·알람 자동 연계·작업 등록 통합)
  - CLAUDE.md 인벤토리 등록
- **후속 (v2 권고)**: 카테고리 자동 추출 (UPS 고장 → category='UPS' WHERE),
  알람 ↔ 연락처 매칭 자동화, 작업 등록 시 contact 외래키.

## 12. region 코드 UI 표시 제거 — 단일 지자체 운영 정책
- **사용자 결정**: "지자체 한 곳만 쓰일 것이라 R01만 으로 충분, 외부에 R01 표현
  불필요. WEB에서 보여주지 않도록."
- **현황 파악**:
  - `tb_region` 마스터 테이블 자체 없음 (코드/시드 하드코딩 단일 region)
  - region 컬럼 가진 테이블 27개, 백엔드 'R01' 하드코딩 91 위치, 프론트 5+ 페이지
  - 실제 운영 데이터 모두 R01 단일
  - UI 표시 위치 3곳: AppHeader 우측 Badge, AppTopbar 동일 Badge, 로그인 페이지
    "지역" Select dropdown
- **변경**:
  - `AppHeader.tsx` / `AppTopbar.tsx` — region Badge 제거 (조건부 렌더 코드 삭제 + 주석)
  - `(auth)/login/page.tsx` — "지역" Select dropdown 제거, region='R01' 고정 상수화,
    Select 컴포넌트 import 정리
- **유지**:
  - 백엔드 region 격리 로직 동일 (모든 WHERE region=%s)
  - JWT 토큰 region 클레임 동일
  - `/auth/me` 응답 region 포함 (프론트 내부 활용)
- **복원 경로** (멀티 region 도입 시):
  - AppHeader / AppTopbar Badge 복원
  - 로그인 dropdown 복원 + region useState 전환 + `tb_region` 마스터 테이블 신설
- **검증 (Playwright)**: dashboard·로그인 페이지 모두 R01 / 지역 라벨 미노출 확인.

## 13. 후속 작업 1+2+3 — 한 번에 진행
### 13.1 작업 1: Vision Agent RAG DB 호스트명 fix
- 문제: vision_agent.py DB_HOST default `'timescaledb'` (Docker 호스트명).
  호스트 네이티브 실행이라 RAG 인덱스 로드 실패 (could not translate host name).
- 수정: default `'localhost'`, `DB_PORT=5433` (호스트 환경 우선). Docker 배포
  시 env override 가능.
- 검증 (6회): `/health` + `/vision/fault-cases/reload` 6회 — 모두 `loaded:5`
  안정 응답. RAG 에러 0건.

### 13.2 작업 2: EMERGENCY_CONTACT_QUERY v2 — 카테고리 자동 분기
- example3.json 에 4개 카테고리별 인텐트 추가 — `EMERGENCY_CONTACT_UPS` /
  `_POWER` / `_NETWORK` / `_VALVE`. 각 SQL 에 `WHERE category=…` 하드코딩.
- intent_classifier 의 비상연락 트리거 매칭 후 카테고리 키워드 분기:
  UPS / 정전·단전·전원 이상 / 네트워크·통신·모뎀·LTE / 밸브 → 카테고리별 인텐트.
  매칭 없으면 전체 (`EMERGENCY_CONTACT_QUERY`).
- 트리거 보강: "어디로 연락" / "어디로 전화" / "어디 전화" 추가 (기존 "어디 연락" 만으로 부족).
- 검증 (6 변형): UPS / 정전 / 네트워크 / 모뎀 / 밸브 / 전체 — 6/6 PASS.
  SQL 응답: UPS 3건, 정전 3건, 네트워크 2건, 밸브 1건, 전체 10건.

### 13.3 작업 3: B3 매뉴얼·고장 케이스 RAG SKU 모듈 — Phase 1
- 신규 마스터 키: `MANUAL_RAG_ENABLED` (default 'N' — feature-sku 정책).
- Migration 0078: `tb_comm_code(SITE_SETTING.MANUAL_RAG_ENABLED)` 시드.
- vision_agent: `is_manual_rag_enabled()` 헬퍼 + 60초 캐시. `_retrieve_*`
  호출 직전 가드 (False 시 skip).
- backend `/auth/me`: `features.manual_rag` 추가 (epanet 과 같은 객체에 포함).
- backend `/admin/site-settings` GET/PUT: manual_rag_enabled 처리 추가.
- 프론트 `/admin/site-settings`: "매뉴얼·고장 케이스 RAG" 카드 (purple Waves).
  ON 시 "고장 케이스 관리 페이지로 이동" 빠른 링크.
- 사양 `docs/feature-sku-spec.md` §5 신규 (Phase 1 구현 상세).
- 검증 (6회 ON/OFF 토글): 6/6 PASS — DB use_yn ↔ /auth/me features.manual_rag
  양방향 정합. 마지막 OFF 복원 (기본 정책).
