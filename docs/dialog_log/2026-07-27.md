# 2026-07-27 세션 로그

## P2-b 구축 워크스페이스 셸 (사용자: "응 진행해줘")

- `/setup/workspace` 신설 — 좌 트리(시설→설비→태그) · 중앙 레이어 캔버스
  (용수 계통 = CanvasEditor / 통신망 = NetworkLinkEditor)
- **신규 API 0개**: 트리는 캔버스가 쓰던 `/canvas/layout`·`/canvas/node-detail`·
  `/canvas/equipment-tags` 재사용, 하위는 펼칠 때 지연 로드
- 두 에디터에 focus prop 추가 → 트리 선택 시 노드 선택 + fitView 이동.
  레이어는 hidden 으로 유지(재조회·fitView 방지, spec §2.2)
- 검증 중 발견·수정: 외부 포커스가 store `selectedNodeId` 만 바꿔 인스펙터는
  갱신되는데 **노드 하이라이트가 안 됨** → 하이라이트 기준은 React Flow
  `node.selected` 이므로 setNodes 로 함께 갱신 (루프는 focusKey ref 로 차단)
- Migration 0127 메뉴 등록 — tb_menu PK 가 (region, menu_idn) 이라 region 별
  복제 + tb_auth_menu 권한 복제(M200-17 기준, 2/2)
- 검증: 트리 99시설 · 검색 "송산" 4건 · 시설 클릭 → `송산1__소블록` 선택 +
  인스펙터 탭 동기화 · 설비 클릭 → 태그 3건 · 통신망 전환 → plc_50 포커스 ·
  복귀 후 상태 유지. tsc 0
- 남은 것: P2-c(통신 탭 포함 인스펙터 공통화), P3(시설 등록 마법사)
