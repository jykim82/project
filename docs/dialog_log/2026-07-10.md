# 2026-07-10 세션 대화 로그

## 채팅 인텐트 분류 — 짧은 follow-up 지표 상속 오답 수정

### 사용자 요청
- "오늘 전체 배수지 유출량은?" 직후 "배수지 총유량은?" 질문의 답이
  **앞선 질의 답변으로 대체**되어 나온다. 분류가 잘되는지 확인 요청.

### 원인
- `session_manager.is_short_followup()` 의 `len(q) < 10` 규칙이
  10자 미만 질문을 **무조건 직전 인텐트로 상속**.
- "배수지 총유량은?"(9자)이 직전 `TODAY_OUTFLOW_ALL_STATUS`(유출량)를
  상속 → 실제 의도 `TODAY_FLOW_ACCUMULATION`(총유량) 대신 유출량 답 재사용.
- 두 인텐트는 example3.json 에 각각 정확히 정의됨 → **분류기 자체는 정상**,
  세션 상태 상속이 원인.

### 조치
- `is_short_followup()` 에 `_standalone_metric_nouns` 가드 추가.
  질문이 유량/유출량/총유량/수위/압력/공급량/적산/순시 등 **지표를 스스로
  명시**하면 (a)<10자·(b)표지+<20자 밴드 모두 상속 거부 → 재분류.
- 지표 없는 조각("오늘 것도", "가압장은?", "그럼 어제 것도?")만 기존대로 상속.
- 단위 검증 8/8 통과. 백엔드 재기동으로 반영.
- 사양: `docs/review-items.md §멀티턴 (a-fix)` 갱신.

### 파일
- `slm/session_manager.py` — 지표 가드 + docstring 갱신
- `docs/review-items.md` — (a-fix) 항목 + 히스토리

## 채팅 로딩 스피너 — 동작 줄이기(reduced-motion)에서 멈춤 수정

### 사용자 요청
- 병목 질의 중 진행 표지 동그라미가 멈춘 것처럼 보임. 계속 돌게 해달라.

### 원인·조치
- globals.css 전역 `prefers-reduced-motion` freeze 규칙이 `animate-spin`/
  `animate-pulse` 를 1회 후 정지 → OS "동작 줄이기" 켠 PC 에서 재현.
- 로딩 스피너는 기능적 요소이므로 WaterLevelGauge(.slm-wl-*) 와 동일하게
  클래스 특이도+!important 로 reduced-motion 예외 복원
  (`.slm-live-spin`/`.slm-live-pulse`, ChatMessageArea.tsx).
- 검증: 활성 freeze 하에서도 300ms 간 약 113도 회전 확인.

## 야간최소유량 트렌드 응답 28초 → 5.7초 [E-035]

### 사용자 요청
- "신평 배수지 야간최소유량 트렌드" 응답이 너무 느린 이유·해결책.
- (선행) 그래프 내용·표출 시간이 야간최소유량 정의와 맞는지 확인.

### 확인·조치
- **정확성**: 사전집계 `tb_night_min_flow_daily` = 00~06시 60분 이동평균
  일별 MIN. x축은 일자(월 단위 표출). 라벨과 일치 — **오류 없음**. (야간
  시간대는 각 점에 반영, 축에는 시계 시각이 아닌 날짜 표시가 정상.)
- **성능**: SSE 경로가 사전집계 미적용(23.6초 함수) + rows 초기화 덮임으로
  선언 SQL 재실행(3300행). SSE 를 사전집계 fast-path 로 통일 + 조회를
  rows 초기화 이후로 이동. 사전집계 테이블 정체(2026-03-21) → backfill.
- 결과: 서버 28초→5.7초, wall-clock 27.5초→11.2초, rows 3300→157.

### 파일
- `slm/ai_server.py` — SSE NMF 사전집계 fast-path + 순서 수정
- `slm-dashboard .../chat/ChatMessageArea.tsx` — 스피너 reduced-motion 예외
- `docs/error-management.md` — E-035

## 태그 모니터링 이상 필터 빈 화면 (데이터홀딩·물수지 불균형)

### 사용자 요청
- 태그 모니터링에서 데이터홀딩·물수지 불균형 필터가 하나도 안 보임.

### 원인 (코드 버그 아님)
- 반복 백엔드 재기동으로 인메모리 이상감지 스캔 캐시(_ANOMALY_SCAN_CACHE)가
  비워짐. 전체 스캔(SQL+IForest+교차검증) 재빌드에 **약 200초** 소요되며,
  그 사이 모든 이상 필터 뷰가 "이상 분석 준비 중" 빈 화면 표시(정상 동작).
- 캐시 완료(21:31:56, 302행) 후 데이터홀딩·물수지 불균형 모두 정상 복구
  (물수지 50행 확인). 별도 코드 수정 불필요.
- 참고: 세션 중 수정 적재를 위한 반복 재기동마다 스캔(~200초)이 재실행돼
  "이상 분석 준비 중"이 반복 노출됨. 재기동 종료 후 캐시 유지.

## 야간최소유량 사전집계 테이블 자동 갱신 루프 (재발방지)

### 사용자 요청
- E-035 사전집계 정체 재발방지 — 추천안(백엔드 백그라운드 루프)대로 진행.

### 조치
- `ai_server._night_min_flow_agg_loop` 신규 (서버 시작 200초 후 + 24h 주기).
  `max(log_date)+1 ~ 어제` 구간만 `backfill_night_min_flow` 로 채워 자기치유
  (다운 기간 gap 자동 보정). upsert 라 재실행 안전.
- 검증: gap 로직(현재 최신 → 0일), self-heal 왕복(1일 삭제→backfill→86행 복원).
- `docs/review-items.md` [Perf] 항목 ✅ 해결(b) 로 갱신.

### 파일
- `slm/ai_server.py` — `_refresh_night_min_flow_daily` + `_night_min_flow_agg_loop` + 등록

## 상단 메뉴/브레드크럼 반응형 + 팝업 크게보기

### 사용자 요청·조치
- 탑바 좁은 폭에서 메뉴 라벨 2줄 접힘 → 한 줄 고정+폰트 축소+가로 스크롤
  (whitespace-nowrap/shrink-0, text-xs↔xl:text-sm, nav min-w-0).
- "빠른 이동" 버튼 미축소 → xl 미만 아이콘만.
- 브레드크럼 폰트 미축소 + 아이패드 2줄 접힘 → 반응형 폰트 + flex-nowrap.
- 팝업 스크롤 시 "크게보기" 버튼 → 공용 DialogContent expandable prop
  (96vw×94vh near-fullscreen, 스크롤 제거). 트렌드/분석/위기대응/설비이력 팝업 적용.

## 리프레시(자동 리로드) 근본 해결 — E-034 재발 → 프로덕션 빌드 경로

### 사용자 요청
- 계통도 확대 중 화면이 리프레시되며 초기화. 어느 페이지든 특정 시간마다 발생.
- 이전 개선 이력 확인 + IP 납품 시 변경돼도 근본 해결(매번 IP 추가 불가) 요청.

### 원인·조치
- **E-034 재발**: 호스트 IP 가 DHCP 로 192.168.50.84 로 변경됐으나 cert SAN 에
  없어, 아이패드/PC 의 LAN IP 접속 시 `next dev` HMR wss 재검증 실패 →
  ~70~90초 주기 전체 리로드. (localhost 접속엔 무영향)
- 임시: cert 재발급(현재 IP 포함) + frontend 재기동.
- **근본 해결(사용자 선택)**: 프로덕션 빌드 경로 구축. `next dev`(HMR) →
  `next build`+`next start` 는 HMR 웹소켓이 없어 IP/인증서와 무관하게 리로드
  트리거가 없음. Caddy 리버스 프록시로 HTTPS 종단. 이미지 빌드·기동 검증 완료.

### 파일
- `slm-dashboard .../Dockerfile.prod` (신규), `docker-compose.prod.yml` (신규),
  `certs/Caddyfile` (신규), `docs/deploy-production.md` (신규),
  `docs/error-management.md` E-034 재발·근본해결 추가, CLAUDE.md 인벤토리.
- `certs/localhost.pem` 재발급(192.168.50.84/192.168.10.11 SAN) — git 미관리.

### 전환·검증 (DDNS 실측)
- 접속처 확인: `dnhigh98.asuscomm.com:3000` (ASUS 라우터 DDNS, 아이패드).
- **무중단 프런트 교체 적용**: db/backend(캐시 포함) 유지, dev 프런트 중지 →
  `slm-frontend-prod`(next start) + `slm-caddy`(HTTPS:3000) 기동. Caddy→frontend→
  backend 프록시 정상(login 200, proxy 401). 프로덕션 HTML 에 HMR 클라이언트
  없음 / `/_next/webpack-hmr`=404 확인 → **리로드 원천 제거**. 사용자 확인 완료.
- 재현·재빌드 스크립트 신규: `scripts/switch-frontend-prod.sh`(재빌드=재실행),
  `scripts/switch-frontend-dev.sh`(HMR 복귀). deploy-production.md §무중단 교체 추가.
- 메모리: `project_prod_frontend_reload_fix.md`.
- **자동 재빌드 루틴 요청**: 프런트 수정 후 자동으로 switch-frontend-prod.sh 재실행.
  `scripts/watch-frontend-prod.sh` 신규(src mtime 폴링+20s 디바운스, fswatch 불요).
  메모리 `feedback_frontend_prod_autorebuild.md`, deploy-production.md 반영.

## 알람 밀도 캘린더 — dynamic-ux 시안 17 실구현

### 사용자 요청
- "알람발생 캘린더 히트맵에 역동적 개선(시안에서 제공하던 거) 반영됐나?"

### 확인·조치
- 확인 결과 시안 17(GitHub 컨트리뷰션 스타일 일자별 밀도 캘린더)은 인덱스엔
  '적용' 표기였으나 **실구현 없음**. 기존 페이지는 요일×시간대 ECharts 히트맵뿐.
- `AlarmDensityCalendar` 신규 구현 — by_day_hour 일자별 합산 → 주×요일 색농도
  그리드(고정 15px 셀, GitHub 스타일). 호버 scale(1.35)+흰 테두리+플로팅 툴팁,
  5단계 색 램프, 월 라벨·범례·통계(기간 총건/최다일/일평균/무알람일).
  alarm-calendar 페이지 최상단 섹션 추가. 긴 기간은 가로 스크롤.
- 검증: 15×15 셀, grid-auto-flow column·7행, 통계 실값(3,931건 등), 호버 전환 확인.
  프로덕션 재빌드(switch-frontend-prod.sh)로 반영.

## 설비 건강성 KPI 전부 0 — 커넥션 누수 [E-036]

### 사용자 요청
- "설비 건강성 개요가 숫자가 다 0이네"

### 원인·조치
- 데이터·API 정상(summary=21건)인데 UI 0. mtbf 엔드포인트 500(풀 고갈) →
  Promise.all 전체 reject → KPI 미설정. 풀 고갈은 `alarm_contacts.py` 5개
  핸들러 `finally: conn.close()` 누락으로 비상연락처 SELECT 가 idle-in-txn
  으로 6개 누수(풀 max=10) 때문.
- 수정: alarm_contacts 5개 핸들러 `finally: conn.close()` 추가(uvicorn reload
  반영, idle-in-txn 6→0), equipment-health `load()` Promise.allSettled 로 견고화.
- 검증: mtbf 500→200, KPI 총장애 21/진행중 19/완료 2/고장 12/이상 7 정상.

### 파일
- `slm/endpoints/alarm_contacts.py`, `.../monitoring/equipment-health/page.tsx`,
  `docs/error-management.md` E-036, CLAUDE.md 인덱스 E-036.

## 향후 전망(수위) 값 폭주 — 물리 한계 클램프

### 사용자 요청
- "행정 배수지 수위 트렌드 향후 전망 6시간 한계 접근 결과 값이 터무니없게 올라가"

### 원인·조치
- `_linear_forecast` 는 경계 없는 선형 외삽. 수위(유계 신호)를 상승 기울기로
  연장하면 만수위(4.2m)를 크게 초과. `compute_comparison` 에 trend_kind 별
  물리 상·하한 클램프 추가 — level 상한 만수위(threshold/0.9)·하한 0,
  유량/압력/수질 하한 0, 그 외 관측범위+여유. 한계에서 평탄화.
- 검증(행정 LEI_N001): forecast_max 4.2 클램프, 말단 [4.2,4.2,4.2],
  "6시간 후 한계 접근" 정상. `docs/trend-comparison-spec.md` 갱신.
- 파일: `slm/trend_comparison.py`.

## 모바일 화면 최적화 검토 + 개선 3종

### 사용자 요청
- 모바일 버전 화면 최적화 검토 (sidebar/topbar 두 모드 각각 확인) → 순서대로 개선.

### 검토 결과 (iPhone 390px, 6개 페이지)
- 전 페이지 가로 오버플로우 0. sidebar/topbar 모두 모바일에선 햄버거+드로어로
  수렴(topbar 가로 메뉴는 반응형 숨김). 구조적 문제 없음.

### 개선 (순서대로)
1. **터치 타깃**(WCAG 2.5.5): 모바일 헤더 아이콘/토글 버튼 hit area ≥44px
   (Toggle Sidebar 28→44, 아이콘 36→44), 이상 필터 pill ≥34px. globals.css.
2. **GIS 컨트롤바**: 좌측 버튼군 가로 스크롤(.slm-gis-toolbar) + 모바일 중복
   제목·장황한 상태텍스트 숨김 → 좌측 툴바 26→269px, 비좁음 해소.
3. **태그 테이블**: 모바일(<md) 부차 컬럼(태그유형·시설유형·장비유형) 숨김
   → 1156→909px, 핵심 7컬럼만.

### 파일
- `slm-dashboard .../app/globals.css`, `.../monitoring/gis/page.tsx`,
  `.../monitoring/AnomalyFilterChips.tsx`, `.../monitoring/TagMonitoringTable.tsx`.
