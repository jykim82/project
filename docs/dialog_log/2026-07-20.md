# 2026-07-20 세션

## 외부 접속 진단 + 모바일 지원

- DDNS(dnhigh98.asuscomm.com:3000) 접속 불가 문의 → 서버·포트포워딩·외부
  도달성 모두 정상 실증 (해외 3노드 TCP 연결 성공). 원인은 클라이언트 측
  (mkcert 개발 인증서 경고 or http 스킴 or 모바일 회선)
- 모바일 전용 실패 → cloudflared quick tunnel 기동 (정식 인증서·443)
- 모바일에서 메신저 대화가 목록에 가려 안 보임 → 1열 전환 레이아웃
  (목록 ↔ 스레드, 뒤로가기 버튼. lg+ 데스크톱은 기존 2열 유지)


## P3 실통화 무음 진단 + 모바일 메신저 1열

- 실통화(모바일 jykim ↔ PC kwater) 무음 신고 — 시그널링은 정상(수락·통화
  기록 확인), 미디어 P2P 미성립 의심 (STUN/TURN 없음 → 같은 네트워크 필수)
- 보강: connectionState 가시화("음성 연결 중…" ↔ 경과시간), 8s 미연결
  안내 토스트, 자동재생 차단 "소리 켜기" 폴백
- 모바일 메신저: 목록↔스레드 1열 전환 + 뒤로가기 (채팅 가림 해소)


## 연결음 + 외부망 통화 옵션 (coturn) + 부하 확인

- 통화 연결음: WebAudio 톤 (발신 425Hz 링백 / 수신 740+880Hz 벨) +
  끄기 토글 (수신 모달·발신 바, localStorage)
- 서버 부하 실측: 폴링 전체 ~1.2 req/s (2 브라우저), backend CPU 17%/1코어,
  DB 0.03% — 여유. 사용자 승인 후 coturn 진행
- coturn: compose profile "turn" + /call/turn-credentials (시간제한 HMAC,
  TURN_ENABLED 옵션 — 납품 기본 off) + 프런트 iceServers 주입.
  로컬 릴레이 할당 검증 완료. 실 LTE 통화는 공유기 UDP 포트포워딩 필요


## 연결음 개선 + 외부망 통화 UI 토글

- 연결음 차임벨 스타일 교체 (벨 엔벨로프+배음. 수신 G5→C6→E6 / 발신 A4→C5)
- "어디서 켜고 끄나" → 관리 > 사이트 설정에 외부망 통화 토글 신설
  (Migration 0109 CALL_TURN_ENABLED). env(인프라) AND 토글로 발급 게이트,
  OFF→enabled:false 실측 검증


## 발신 연결음 무음 수정

- 링백 시작이 제스처 밖이라 모바일 AudioContext 차단 → 통화 버튼 클릭
  시점 선잠금(unlockCallAudio, iOS 무음 버퍼) + suspended 예약 무음 수정


## P4 영상 통화 + 외부망 통화 진단

- P4: call_type 시그널링(0110) + 영상 패널(원격+PiP+카메라 토글) +
  카메라 없음 음성 폴백. 발신 오류 문구 정밀화(권한/장치/중복/기타)
- 외부망 통화 실패 원인 확정: 공유기 TURN 포트포워딩 미설정 —
  외부 노드에서 3478 타임아웃 실측. 서버·토글·자격증명은 정상


## 통화 유령 세션 해결 + 무응답 자동 종료

- "이미 진행 중인 통화" 원인: accepted 세션이 정리 규칙 없이 잔존
  (브라우저 종료 등) → 즉시 DB 정리 + 3중 방어 구축
- 벨 무응답 20s 부재중 (사용자 요청 60→20s) + 수신 모달 자동 닫힘
- accepted 하트비트: status 폴링 30s 중단 시 서버 자동 종료 (0111)
- 미디어 미연결 20s 자동 종료 + pagehide sendBeacon 정리
- 클라우드플레어 터널 테스트 한계 안내: HTTP 만 통과 — 미디어(UDP)는
  TURN 포트포워딩 필수


## 아수스 포트포워딩 완료 — 외부망 통화 경로 개통

- 사용자 라우터 설정 후 실측: 외부 3노드 3478 연결 성공 + 공인 주소
  TURN 인증·릴레이 할당·패킷 중계 성공 (손실 0%)
- 남은 확인: LTE 실통화 (릴레이 대역 49160-49200 최종 검증)


## LTE 외부망 통화 성공 + 수락 중복 클릭 수정

- LTE↔PC 실통화 성공 확인 (사용자). 연결 실패 다발+통화중 표시 소실
  원인 = 수락 중복 클릭 (2번째 시도의 cleanup 이 성립된 통화 표시 제거)
- 수락 버튼 처리중 비활성(연결 중…) + 재진입 가드 + 통화 성립 후
  잔여 오류 무시. relay 후보 조기 마감으로 수락 지연 단축


## 영상 통화 수신측 검은 화면 수정

- 아이폰→PC 영상이 패널만 뜨고 안 보임: 수락 직후 ref 바인딩이 렌더
  전이라 무효 → 렌더 후 effect 바인딩 + play() 재시도로 수정
- 벨 자동 닫힘은 로직 정상 (DB 폴링 흔적 확인) — 수신측 화면이 구빌드로
  남아있던 것으로 추정, 새로고침 안내


## 카메라 전환 + PiP 정리

- 전면/후면 전환 🔄 (facingMode+replaceTrack, iOS stop-후-획득 패턴.
  아이폰·갤럭시 공통 지원 확인)
- 카메라 없는 쪽 검은 PiP·카메라 토글 숨김


## 알람 팝업 사양 3건

- 팝업 표시 중 알람 해제 시 초록 카드/뱃지/안내 전환 (그룹 행 포함)
- "작업 수정" → "알람 제어" 명칭 변경
- "이 알람 중지" 버튼 → 알람 제어창 자동 오픈 + 시설/유형/내용/중지 알람
  유형(키워드 매칭) 프리필. 명시 등록 원칙 유지


## 3~4인 회의 통화 구현 (풀메시)

- 방 생성→초대 벨→수락자 합류 흐름 (사용자 확인 UX). Migration 0112
- 쌍별 SDP 사서함 + 하트비트, 신규 합류자 offerer 규칙. 프로토콜 curl
  전 경로 검증 (생성/벨/합류 peers/시그널 소비/이탈/403)
- UI: 메신저 회의 버튼+참가자 선택 다이얼로그, 참가자 바(상태 점·음소거·나가기)


## 그룹 채팅 (다중 초대 대화방)

- Migration 0113: tb_chat_group/member, room_id grp:N — 기존 메시지·읽음
  인프라 재사용. 제목 자동(참가자 이름). 비멤버 403
- 새 대화 다이얼로그: 1명=DM / 2명+=그룹(+이름 입력). 그룹 아이콘 구분


## 메모 달력 보기

- 목록/달력 토글 추가 (작성일 기준 월 달력 + 선택일 목록 + 클릭 상세).
  memo-schedule-spec §4 반영


## 공개 도메인 HTTPS (Let's Encrypt)

- Caddy 에 PUBLIC_DOMAIN 옵션 (.env) — 설정 시 443/80 종단 + LE 자동
  발급/갱신 (certs/Caddyfile.public 치환 생성, slm-caddy-data 볼륨 영속)
- 폐쇄망 납품과 무관한 dev/데모 전용 — prod compose 는 고객 인증서 유지
- 발급 대기: 공유기 TCP 80/443 포워딩 필요 (challenge timeout 실측)
