# 2026-07-09 세션 로그

## 역동적 UX 기법 제안 → 검증 → 순차 적용 (Phase 0~5)

### 사용자 요청 (요지)
- 역동적 기법 시안을 자립형 HTML 목업으로 제시 → "10개로 제한하지말고 추천 항목도
  더 해서" (20종으로 확장).
- "현재 페이지에 적용 안 된 기법 맞냐" → 정직한 감사 요청.
- "적용 시 속도·자원 문제 발생하는지 확인해줘" → 성능/자원 리스크 분석.
- "너의 추천안은" → 우선순위 추천.
- "응 순차적으로 이어서 해줘" / "끝까지 진행" → 실제 적용.

### 진행
- **시안 갤러리**: `prototype/dynamic-ux/` 20종(01~20)+index. 각 시안에 현재 적용
  상태 태그(신규/부분/적용). 감사 결과 약 6종은 이미 적용 상태로 판명. (커밋 6e78d73, 8cec67a)
- **성능 분석**: 20종을 비용별 분류(🟢 CSS transform/opacity·이벤트 구동 / 🟡 canvas·1s
  interval / 🔴 water-hero O(n²)). 가드레일: reduced-motion, 탭 숨김 시 정지, 60초
  데이터 주기 정렬, 동시 애니 상한, transform/opacity 한정, React cleanup.
- **Phase 0 (기반)**: `src/hooks/use-motion-safe.ts` — usePrefersReducedMotion /
  useDocumentVisible / useMotionActive. globals.css 에 reduced-motion 전역 규칙(빌드
  보존 확인).
- **Phase 1**: `CommandPalette.tsx` — 전역 ⌘K/Ctrl+K 팔레트, useSidebarMenus() 권한
  필터 메뉴 flatten, 키보드 내비. AppHeader/AppTopbar 마운트. (54메뉴, "감사"→감사로그)
- **Phase 2**: `WaterLevelGauge.tsx` — CSS 파동 탱크(motion-safe swell). GisDetailPanel
  배수지에 렌더(죽동 74% / 2.97m / 4.0m 검증). tankMax 는 reservoir zone 높이 fetch.
- **Phase 3**: `LiveBadge.tsx` — motion-safe 하트비트 핑. gis/page 갱신시각 옆 + 트렌드 헤더.
- **Phase 4**: `count-up.tsx` — motion-safe KPI 카운트업(이전→새값 이징, reduced/숨김 즉시).
  대시보드 종합현황판 KPI 7종 적용(값 렌더 검증).
- **Phase 5**: 알람 벨 경고(severity) 행 red inset ring 강조(경고 한정). 애니 글로우는
  Tailwind v4 가 커스텀 keyframe 클래스를 빌드에서 누락 → 신뢰성 있는 Tailwind ring
  유틸(모션 절제)로 전환. globals.css 죽은 글로우 CSS 제거(reduced-motion 유지).

### 판단 (의도적 보류)
- 드래그 위젯 / 비교 슬라이더 / 워크플로 스테퍼 / 파이프 흐름 애니 — 운영자 화면 모션
  절제 · 이미 유사 존재 · 고비용 이유로 보류.
- 애니 글로우 대신 정적 ring — Tailwind v4 커스텀 클래스 drop 회피 + 읽는 드롭다운
  특성상 정적 강조가 더 적절.

### 교훈 (기술)
- **Tailwind v4 는 globals.css 의 커스텀 클래스 선택자를 tree-shake 로 누락**시킴
  (universal `*` 규칙·기존 규칙은 보존). 향후 커스텀 애니 CSS 는 `<style jsx global>`
  (컴포넌트 스코프) 또는 Tailwind 유틸로 작성.

## 역동적 UX 잔여 마무리 — Phase 6 (파이프 흐름 애니)

### 사용자 요청
- "다음 할일은?" → 방향 질문 → **UX 잔여 기법 마무리** 선택.

### 진행
- **Phase 6**: Sankey 용수흐름도(`FlowDiagramGraph`) 링크에 `.s-flow` 오버레이 path
  (stroke-dashoffset 애니, source→target). motion-safe: useMotionActive 게이트 +
  `@media (prefers-reduced-motion)` 이중 억제. dimmed 링크 흐름 숨김. `<style>` 주입.
  검증: 97링크 오버레이 렌더·dashoffset 이동·에러 0. (slm-dashboard `06f1871`)
- 사양 동기화: `flow-diagram-mode-spec.md §3.1`.

### 커밋
- slm-dashboard: Phase 4/5 `cc9bfbc`, Phase 6 `06f1871`.
- 부모 web: submodule bump.

## 보류 항목 전량 구현 — 각 항목 4회 검증

### 사용자 요청
- "이어서 진행" → "각 항목을 진행하고 테스트는 4회씩 확인하면서" (보류 5건 실구현 + 항목별 4회 검증)

### 진행 (항목별 Playwright 4회 검증·개별 커밋)
- **시안 18 GIS 알람 리플** (`fb7dced`): 활성 알람 시설 맥동 리플(react-map-gl Marker+CSS).
  검증 ①18리플 ②gis-ripple 실이동 ③pointer-events none ④줌 지도추적.
- **시안 05 라디얼 게이지** (`3c5d40e`): 임계 미설정이라 '오늘 관측범위 내 현재값'으로 정직
  표현(no-fake-numbers). GisDetailPanel Q/H/P 요약. 검증 ①렌더 ②실값위치 ③채움애니 ④멀티지표.
- **시안 08 인라인 스파크라인** (`b52d77d`): 태그 모니터링 현재값 6h 미니추세.
  E-030(트렌드 15태그 제한)→12개 청크 병렬. 검증 ①18/18 ②실시계열 ③정렬재조회 ④시각.
- **시안 15 인스펙터 드로어** (`25a744c`): GIS 인스펙터 슬라이드 인+섹션 스태거(tw-animate).
  검증 ①enter 애니 ②지연 0~0.28s ③재오픈 재생 ④시각.
- **시안 19 드래그 위젯** (`a5cd84d`): 대시보드 상위 3섹션 드래그 재정렬+per-user localStorage.
  검증 ①3위젯·핸들 ②재정렬+저장 ③새로고침 영속 ④기본 복원.

### 결과
- 20종 최종: **적용 18 / 제외 1(16 비교슬라이더 EPANET) / 부분 1(09 스크롤리빌 랜딩만)**.
- 갤러리 index.html 동기화. E-030 신규 기록. turbopack 전환오류 3회(page.tsx 편집)→재기동 복구.

### 커밋
- slm-dashboard: `fb7dced`·`3c5d40e`·`b52d77d`·`25a744c`·`a5cd84d`.
- 부모 web: 갤러리·E-030·dialog_log + submodule bump.

## 역동적 UX 잔여 마무리 2 — 시안 13/20 + 파도 버그픽스

### 사용자 요청
- "비교 슬라이더(EPANET) 제외하고 남은 UI 개선 항목 완료 후 알려줘"
- "EPANET 관련은 장기 후보로 남기고 나머지 UI 개선만"
- "물이 출렁거리지 않는데?" (수위 게이지 버그)
- "시안 13도 적용 안 되어 있네" (채팅 스트리밍)

### 진행 (20종 정직 재감사 → 추려서 구현)
- **시안 20 워크플로 스텝퍼**: `FaultWorkflowStepper` 신규 — 발생→진단→조치→완료
  단방향, TaskListDialog "조치 진행" 컬럼에 실데이터 파생. 현재 단계 맥동(motion-safe).
  검증 19스텝퍼·활성맥동. (slm-dashboard `af44206`)
- **시안 13 채팅 타자기**: `useTypewriter` 신규 — 백엔드 progress→result(토큰이벤트
  없음)라 수신 전체를 프런트 점진 공개. 최신 답변 1회(typewrittenSeqs 가드)·캐럿·
  motion-safe·내용 100% 보존. 검증 len 0→13→24. (my audit 오분류 정정: '적용'이 아니었음)
- **버그픽스 수위 파도**: 파도색=물색이라 수평 이동이 안 보이던 문제 → 이중 시차
  파도(앞 2.4s 역+뒤 3.6s)+진폭↑+흰 크레스트 하이라이트. 남산 배수지 실이동 검증.

### 판단 (추려서 = 정직 큐레이션)
- **보류 5**: 05 라디얼(임계데이터 없음→범위 지어내면 no-fake-numbers 위배), 08
  스파크라인(MetricTrendPanel 중복), 15 인스펙터 드로어(GisDetailPanel 견고), 18 맵
  리플(MapLibre GL 레이어라 별도 스코프), 19 드래그위젯(per-user 영속 대형 기능).
- **제외 1**: 16 비교슬라이더(EPANET, 사용자 지시). **부분 1**: 09 스크롤리빌(랜딩만).
- 결과: 20종 중 **적용 13 / 보류 5 / 제외 1 / 부분 1**. 갤러리 index.html 태그 동기화.

### 커밋
- slm-dashboard: `af44206` (시안13+20+파도).
- 부모 web: prototype/dynamic-ux/index.html 상태 동기화 + submodule bump.
