# 2026-07-09 세션 로그

## 역동적 UX 기법 제안 → 검증 → 순차 적용 (Phase 0~5)

### 사용자 요청 (요지)
- 역동적 기법 시안을 자립형 HTML 목업으로 제시 → "10개로 제한하지말고 추천 항목도
  더 해서" (20종으로 확장).
- "현재 페이지에 적용 안 된 기법 맞냐" → 정직한 감사 요청.
- "적용 시 속도·자원 문제 발생하는지 확인해줘" → 성능/자원 리스크 분석.
- "너의 추천안은" → 우선순위 추천.
- "응 순차적으로 이어서 해줘" / "끝까지 진행" → 실제 적용.

### 진행
- **시안 갤러리**: `prototype/dynamic-ux/` 20종(01~20)+index. 각 시안에 현재 적용
  상태 태그(신규/부분/적용). 감사 결과 약 6종은 이미 적용 상태로 판명. (커밋 6e78d73, 8cec67a)
- **성능 분석**: 20종을 비용별 분류(🟢 CSS transform/opacity·이벤트 구동 / 🟡 canvas·1s
  interval / 🔴 water-hero O(n²)). 가드레일: reduced-motion, 탭 숨김 시 정지, 60초
  데이터 주기 정렬, 동시 애니 상한, transform/opacity 한정, React cleanup.
- **Phase 0 (기반)**: `src/hooks/use-motion-safe.ts` — usePrefersReducedMotion /
  useDocumentVisible / useMotionActive. globals.css 에 reduced-motion 전역 규칙(빌드
  보존 확인).
- **Phase 1**: `CommandPalette.tsx` — 전역 ⌘K/Ctrl+K 팔레트, useSidebarMenus() 권한
  필터 메뉴 flatten, 키보드 내비. AppHeader/AppTopbar 마운트. (54메뉴, "감사"→감사로그)
- **Phase 2**: `WaterLevelGauge.tsx` — CSS 파동 탱크(motion-safe swell). GisDetailPanel
  배수지에 렌더(죽동 74% / 2.97m / 4.0m 검증). tankMax 는 reservoir zone 높이 fetch.
- **Phase 3**: `LiveBadge.tsx` — motion-safe 하트비트 핑. gis/page 갱신시각 옆 + 트렌드 헤더.
- **Phase 4**: `count-up.tsx` — motion-safe KPI 카운트업(이전→새값 이징, reduced/숨김 즉시).
  대시보드 종합현황판 KPI 7종 적용(값 렌더 검증).
- **Phase 5**: 알람 벨 경고(severity) 행 red inset ring 강조(경고 한정). 애니 글로우는
  Tailwind v4 가 커스텀 keyframe 클래스를 빌드에서 누락 → 신뢰성 있는 Tailwind ring
  유틸(모션 절제)로 전환. globals.css 죽은 글로우 CSS 제거(reduced-motion 유지).

### 판단 (의도적 보류)
- 드래그 위젯 / 비교 슬라이더 / 워크플로 스테퍼 / 파이프 흐름 애니 — 운영자 화면 모션
  절제 · 이미 유사 존재 · 고비용 이유로 보류.
- 애니 글로우 대신 정적 ring — Tailwind v4 커스텀 클래스 drop 회피 + 읽는 드롭다운
  특성상 정적 강조가 더 적절.

### 교훈 (기술)
- **Tailwind v4 는 globals.css 의 커스텀 클래스 선택자를 tree-shake 로 누락**시킴
  (universal `*` 규칙·기존 규칙은 보존). 향후 커스텀 애니 CSS 는 `<style jsx global>`
  (컴포넌트 스코프) 또는 Tailwind 유틸로 작성.

### 커밋
- slm-dashboard: Phase 0~3 (이전) + Phase 4/5 `cc9bfbc`.
- 부모 web: submodule bump.
