# 2026-07-17 세션

## [E-038] GIS 첫 로그인 마커 미표시 (사용자 스크린샷 리포트)

- 증상: 알람 리플만 보이고 시설 마커/클러스터 미표시, 나중에 갑자기 표시
- 원인: 리플=DOM(생존) vs 시설=WebGL 레이어(스타일 로드 경합 시 유실,
  복구 장치 없음) + visible 조기 return 훅 위반
- 수정: idle 시 레이어 존재 확인→리마운트 자가 복구(≤3회, warn 로그)
- 부수 발견: 베이스맵 cartocdn 외부 CDN — 폐쇄망 납품 시 지도 자체가
  안 뜸 → review-items [납품 필수] 오프라인 타일 번들 등재

## 현장 모드 v3 — 일상점검 + 알람 없는 설비 (사용자 피드백 2건)

- "일상점검 사진 올리는 경우 반영" + "알람 기준만으로는 문제 (기계식
  유량계·전력 부품)" → 시설 직접 선택 섹션 신설
- 시설 검색(coords JSON 103시설) → 선택 → 액션 2×2: 일상 점검 기록
  (사진→draft 점검/일상 자동 분기)·사진 진단·고장 기록·매뉴얼
- 갭 수정: isFaultRecordIntent 에 점검 패턴 추가 (점검+기록+시설유형)
- 종단 검증: 신평 배수지 선택→점검 사진 업로드→"일상 점검 기록해줘. "
  프리필+첨부 1장 / 백엔드 draft: fault_category=점검·inspection_type=일상

## 트렌드 차트 이중 렌더 수정 (사용자: "그려지고 다시 리프레시되고 그려짐")

- 원인 ①: EChartWrapper 가 next-themes resolvedTheme 확정 전(undefined)에
  차트를 초기화 → hydration 후 dark 확정 시 ECharts 전체 재초기화 (채팅·
  트렌드 공통). 테마 확정까지 1프레임 대기 게이트 추가
- 원인 ②: TrendChart 활성 태그를 effect 로 결정 → 데이터 도착 렌더 후
  오버레이 적용 2차 전체 리드로우. 렌더 중 파생 계산(useMemo)으로 전환

## GIS 오프라인 지도 번들 (백로그 추천 1순위 — "폐쇄망에서 안 도는" 마지막 항목)

- 당진 pmtiles 19MB 추출(protomaps) + 글리프 14MB/스프라이트 →
  public/map/ (git 제외, Docker 빌드 포함)
- gis-offline-style.ts: protomaps-themes-base(ko) + pmtiles 프로토콜(기존
  등록 재사용). 기본 오프라인, NEXT_PUBLIC_MAP_CDN=1 opt-in
- GisMap mapStyle useMemo (객체 identity — setStyle 반복 방지)
- docs/operations/offline-map-bundle.md 신설 + review-items CDN 리스크 해소

## 납품 체크리스트 통합 (추천 2순위)

- docs/operations/delivery-checklist.md — 오프라인 자산(웨이트·지도·Ollama)/
  dev 요소 제거/배포 구성/검수(스모크 3층+인터넷 차단 확인)/인수인계 단일 진입점

## 지도 관할 교체 config화 (사용자: "광주로 해야할 수 있잖아")

- 타일 파일명 region.pmtiles 고정 — 고객사 변경 = 파일 교체만
- 추출 파라미터 스크립트 scripts/extract-map-region.sh "<bbox>" (광주 예시 포함)
- 지도 중심/줌 NEXT_PUBLIC_GIS_CENTER / NEXT_PUBLIC_GIS_ZOOM env (기본 당진)
- offline-map-bundle.md 에 "고객사(관할) 변경 절차 — 코드 수정 없음" 표 추가
- 검증: region.pmtiles 206 ·외부 0건
