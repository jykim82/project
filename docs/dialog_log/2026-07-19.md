# 2026-07-19 세션

## E-041: 블록 fill 확대 줌 조각남 (사용자 "확대하면 경계가 깨지거나 색상이 빠진곳이 존재")

- 소블록경계 SHP 는 POLYLINE(경계선) — LineString 인 채 fill 렌더 시
  MapLibre 가 타일 경계에서 선을 재폐합하지 않아 **확대 줌에서만**
  직선 절단·색 누락 (축소 줌은 블록이 한 타일에 들어가 정상으로 보임)
- 임포트 정규화 `_normalize_boundary_feats` 추가: 폐합선→Polygon,
  미폐합선 제외(경고), 완전 동일 중복 33건 제거, 자기교차 buffer(0) 보정
  (남산3), 동명·상이 지오메트리 경고(합덕2·합덕3). 소블록 73→40, 중블록 11→10
- 남산 일대 z14 재검증: 균일 파스텔 + 경계선 정상, invalid 0

## 경계면 색·굵기·채도 "전과 같은지" 검증 (사용자 요청)

- 렌더 코드(팔레트 20색·선굵기 1.5·표현식)는 도입 이래 불변 확인 (git 이력)
- **채도 보정**: "전"은 중복 2겹 렌더라 체감 fill≈0.33·line≈0.9 였음 —
  중복 제거 후 동일 체감 유지 위해 0.18→0.33 / 0.7→0.9 조정
- **E-042 발견**: 검증 중 브라우저가 구본 geojson 을 계속 렌더 —
  /api/gis/layer 가 max-age=86400 고정 캐시. ETag+no-cache 로 교체
  (serveGeojsonWithEtag 공용화, pipelines 라우트 포함). 이후 다색 파스텔
  패치워크가 데이터 참값과 일치 확인
- 블록별 개별 색 배정은 전과 다를 수 있음 — 전은 중복 2색 혼합 + 73개
  넘버링이라 정확 재현 불가(버그 산물), 팔레트 자체는 동일

## 트렌드 재진입 시 차트 재갱신 (사용자 "행정배수지 첫 화면 오픈시 갱신")

- E-039 추가 사례 ③: 전역 store 캐시로 즉시 그려진 뒤 마운트 자동 재조회
  도착 → notMerge 리드로우 + 애니메이션 재시작
- trend-store `silentUpdate` (동일 태그·기간 재조회 판별) → TrendChart
  해당 갱신 animation:false. 사용자 조작(태그·기간 변경)은 플래그 해제로
  정상 애니메이션 유지
- 검증: 재진입 직후/2초 후 프레임 동일 (완성 상태 즉시 표시, 재시작 없음)

## GIS 알람 리플 정지 (사용자 "움직이는 모양이었는데")

- 코드 회귀 아님 — 리플이 reduce-motion 에서 정적 링으로 전환되도록 설계돼
  있었고, 사용자의 열람 환경이 동작 줄이기 상태라 멈춰 보임
- 진행중 알람 맥동은 **기능성 표지**로 판단 (로딩 스피너·유량 흐름 예외와
  동일 취지) → reduce-motion 에서도 유지, 탭 숨김 시에만 정지로 변경
  (useMotionActive → useDocumentVisible + !important 예외 복원)
- 검증: prefers-reduced 환경에서 gis-ripple 2.2s infinite + transform 변화 실측

## GIS 인스펙터 펌프 가동 표시 (사용자 "가압장 같은 경우는 펌프가 있으니까")

- GisDetailPanel 상단에 펌프 가동 카드 — pump_status 있는 시설(가압장)에
  "N대 중 M대 가동" + 팬 아이콘(가동=적색 회전). 지도 팝업 카드와 동일
  데이터(flow-map/realtime)·표현 규칙 재사용
- 갈산 가압장으로 검증 (3대 중 1대 가동 표시)

## 관리 메뉴 정비 (사용자 요청 5건)

- 명칭: 사용자→사용자 관리 / 메뉴→메뉴 관리 / FAQ→FAQ 관리
- 이동: 시설 약칭 관리→구축(지도 설정 다음), 설비 신뢰성 관리→모니터링
  (설비 건강성 다음)
- Migration 0102 (tb_menu UPDATE, 롤백 절차 포함) + sidebar-menus.ts 동기.
  세 그룹 모두 브라우저 렌더 검증

## 구축 메뉴 개편 (사용자 요청 11건, Migration 0103)

- 통합 페이지 3종 신설 (기존 페이지를 탭으로 재사용, SetupTabbedPage 공용 셸,
  ?tab= 딥링크 · 원 라우트 유지):
  시설정보 구축(배수지/가압장/감압시설/블록) · 계통도 설정(용수 흐름/캔버스)
  · GIS 설정(GIS 관리/지도 설정)
- 명칭: 설비→장비 설정, 모니터링 설정→트렌드 설정, 네트워크→네트워크 설정,
  인과 규칙→인과규칙 확인, 비상연락처→비상연락처 설정, 고장 진단 케이스→
  고장 진단 케이스 설정 (M200-15 는 tb_menu 미등록이었어서 신규 INSERT)
- 잠금 관리 검토 → **미구현 확인 후 삭제**: mock UI·백엔드 참조 0·
  tb_field_lock 0행. 페이지+컴포넌트 4종+mock 제거, 사양에 상태 표기
- 검증: 통합 3페이지 탭 전환·딥링크 + 구축 드롭다운 11항목 렌더
- 후속: 시설 약칭 → 시설 약칭 설정 (Migration 0104)

## 캔버스 에디터 크기 틀어짐 (사용자 보고) + GIS 관리 동류 수정

- 원인: 캔버스는 -m-4/lg:-m-6 네거티브 마진 + h-full 로 페이지를 채우는
  앱형 레이아웃 — 탭 셸이 높이 컨텍스트를 안 줘 틀어짐
- SetupTabbedPage 에 fill 탭 모드 (h-full flex-1 + 패딩으로 네거티브 마진
  상쇄). GIS 관리 루트도 100vh 고정 → h-full 로 (단독/탭 양쪽 검증)

## 동적 UI 숫자 카운트업 "취소됐나" (사용자 문의)

- 취소 아님 — CountUp 이 reduce-motion 가드로 즉시 표시되던 것 (리플과
  동일 원인). 데이터 갱신 전달용 기능성 모션으로 판단해 예외 편입,
  탭 숨김 시에만 정지

## 누수 의심 알림 상단 통계 (사용자 "그래프로 통계나 직관적 표현")

- LeakAlertStats 신설: KPI 3종(CountUp) + 최근 14일 발생 추이 스택 바 +
  CUSUM 임계 초과율 Top5 (블록별 최대 배율 집계, 미확인 red)
- 전체 알림 기준 별도 fetch — 탭 필터 무관. 기존 시설별 뱃지 카드 대체
- leak-alert-spec §상단 통계 추가

## 배수지 모니터링 첫 트렌드 재갱신 (사용자 "재연되고 있음" — E-039 사례 ④)

- /trend 와 다른 경로: useAutoRefresh 가 enabled 전환 시 즉시 1회 실행 →
  초기 selectSite 로드와 **이중 로드 레이스** (lastUpdated null 이라 가드 통과)
- 훅에 immediate 옵션(모니터링 페이지 false) + isLoading/isRefreshing
  재진입 가드. 재진입 15초 실측 — 트렌드 호출 1회로 정상화

## 카운트업 확산 (사용자 "종합 현황만 반영된 것 같다")

- CountUp 적용 확대: 경보관리 요약 4카드(AlarmSummaryCards — 위기대응·알람
  페이지 공용) / 분류별 경보 현황 건수·스탯(AlarmCategorySummary) /
  네트워크 KPI(SummaryCard) / 설비 건강성 KPI(KpiCard, 숫자일 때만)

## /trend 재재현 (사용자 "지금도 동일해" — E-039 사례 ⑤) + 재발 방지 총정리

- 잔여 경로: silentUpdate 는 fetch 후에만 세워져 **재진입 마운트 첫 렌더**
  (캐시 데이터)가 애니메이션을 다시 시작 → 직후 백그라운드 갱신이 끊음
- TrendChart animationKey — 같은 조회 조건은 세션 동안 최초 1회만
  애니메이션 (모듈 싱글턴 Set, SPA 라우팅 간 유지). 재진입 t0/t+2s
  픽셀 동일 검증
- 재발 방지: chart-rendering-policy §이중 렌더 방지 4·5항 + **진단
  체크리스트(5단계)** 신설, E-039⑤ 기록, 메모리
  feedback_chart_first_load_rerender 영속화

## 설비 상태 전이 애니 위치 (사용자 질의)

- 시안 23 은 현재 **GIS 관망도 지도 카드 상태 배지 1곳** (gis.css
  .gis-card-badge — 정상↔주의↔경고 0.6s 색 morph)만 적용. 프로토타입의
  파이프라인 pill(정상→이상→고장→조치) 형태는 미적용 — 확장 후보:
  계통도 노드 상태색·태그 모니터링 상태 pill·설비 장애 뱃지

## 배수지 "아직도 재현" (E-039 사례 ⑥) + diff 플래시 확인

- 실측: 행정 선택 23초 뒤 **30초 자동 갱신**이 400ms 모프로 전 시리즈를
  다시 그림 (notMerge=false 여도 update 애니 동작) — 이것이 체감 "갱신"
- monitoring-view-store loadTrendData({silent:true}) → 자동 갱신 무음 반영.
  30s 틱 전후 스크린샷 픽셀 diff 0 검증
- diff 플래시(시안 22): 대시보드 KPI 한 곳만 적용 확인 + useMotionActive
  가드가 reduce-motion 에서 꺼지던 것 → useDocumentVisible 로 완화
  (카운트업·리플과 동일 기능성 모션 기준)

## 트렌드>배수지(행정) 재검증 — 3회 계측 (MutationObserver SVG 변이 타임라인)

- R1: 초기 draw 애니 1회 + 30s 틱 인스턴트 스왑 (단일 500ms 버킷)
- R2: 행정 클릭 즉시 로드→draw 1회 (중간 16s 공백은 도구 조작 지연으로 판명)
- R3 (인페이지 자동 클릭, 마크 정밀): 선택 0.5s 후 draw 1회(2s 애니),
  30s 자동 갱신 +3996 변이가 단일 버킷 = 인스턴트 무음 스왑.
  이전 라운드 픽셀 diff 0 재확인
- 결론: 현 배포본은 "그려졌다 다시 그려짐" 없음. 사용자 재현 시
  구번들 실행 중일 가능성 — 강력 새로고침 후 재확인 요청

## 트렌드/배수지 재갱신 최종 종결 (E-039⑦) — 영상 프레임 포렌식

- 사용자 녹화 영상 프레임 분해: 행정 draw 25%→5% 후퇴 재시작 + "마지막
  갱신" 2회 표기 실증. 로컬 다크모드에서 5/5 재현
- 포렌식: ECharts 인스턴스 ID 0.6s 뒤 전체 교체 (dispose 미경유 —
  next/dynamic 경계 간헐 리마운트). 원인 레이어 통제 불가 판단
- **animationKey 를 MonitoringTrendBlock 에 확장** — 재init 불문 같은
  카탈로그는 1회만 애니메이션. 검증: 재선택 후 SVG 애니 변이 0건
- 부수: 다크모드에서만 재현되던 이유 포함 계측 방법론(MutationObserver
  버스트·인스턴스 ID·fiber 체인) 확립

## 경보관리 현황 요약카드 카운트업 (사용자 지적)

- 누락 컴포넌트는 AlarmDashboardCards (진행중 전체/경고/주의/정상 4카드)
  — CountUp 적용. 앞서 반영한 AlarmSummaryCards 와 별개 컴포넌트였음

## GIS 타임라인 스크러버 사양 확립 (시안 14)

- docs/gis-timeline-scrubber-spec.md 신설 + 인벤토리 등록 — 24h 되감기/
  재생, 알람 이벤트 도트, useChartPlayback 재사용, Phase 1 신규 API 없음

## GIS 타임라인 스크러버 Phase 1 구현 (사용자 "구현 착수")

- use-gis-timeline(24h·144pt 로드/재생 엔진/t 스냅샷) +
  GisTimelineScrubber(하단 오버레이 — 재생·배속·트랙·알람 도트·실시간 복귀)
- 타임라인 중 실시간 폴링 정지, 현재-시점 전용 정보(알람 리플 등)는
  스냅샷에서 제거 (Phase 1.5 t 연동 예정)
- 검증: 토글→로드→재생(시각 라벨 진행, 트렌드 축과 동일 표기)→실시간 복귀

## 타임라인 데이터 패널 (사용자 "표 형태로 변화 표현" 제안)

- GisTimelinePanel: 우측 오버레이 표 — 시설|지표|t값|Δ(직전 10분),
  |Δ| 큰 순 정렬(재생 중 움직이는 시설이 상단으로), ▲/▼ 색 구분,
  시설명 검색, 스크러버 표 버튼 토글, 타임라인 진입 시 자동 오픈
- 검증: 재생 중 값·Δ 갱신과 정렬 동작 실화면 확인

## 타임라인 패널 개선 (사용자 "유량·압력·수위·펌프 구성 + 정렬 고정")

- 컬럼: 시설 | 유량 | 수위 | 압력 — 대표 1지표 → 시설 보유 지표 전체 로드
  (지도 스냅샷도 전 지표 t 값 반영으로 개선)
- 정렬 유형→시설명 **고정** (|Δ| 정렬 폐지) — 재생 중 스크롤 추적 유지,
  변화는 셀 하이라이트·▲/▼ 로만
- 펌프 동작 컬럼: 실시간 API 에 RUN 시계열 태그 부재 → Phase 2
  (서버 스냅샷 API) 제공으로 사양 명시

## 타임라인 Phase 1.5 — t 시점 알람 리플 + 알람 도트 실동작

- 백엔드 /alarm/list 에 endedAt(alarm_end_time) 추가 (--reload 즉시 반영)
- t 시점 진행중 판정(occurredAt ≤ t < endedAt) → snapshot alarm_severity
  주입 — 리플이 과거 시점 기준으로 맥동. 검증: 도트 점프 시 송악2 경고 리플
- 버그 2건 수정: level='all' 이 백엔드 문자열 비교로 0건 (빈 값=전체 규약),
  알람(KST)↔트렌드 축(UTC 나이브) 좌표계 차이 → 변환 통일. 도트 87개 렌더
- 발견: trend 축이 UTC 나이브 표기 (전역) → review-items 등록 (KST 통일 권고)

## 화이트 모드 품질 라운드 (사용자 지적 7건 일괄)

- CompareSlider(평소 vs 실측 비교 슬라이더) 삭제 — 사용자 결정
- "2024년" 첫 글자 색 분리 버그: 숫자 정규식 \d{1,3} 이 4자리를 쪼갬 → 교정
- 채팅 값 강조색: 라이트 text-blue-600 / 다크 cyan-400 유지
- 경보분석 로직 다이어그램: -200 밝은 글자 → 라이트 -800/다크 -200 듀얼
- MetricTrendPanel(GIS 인스펙터·용수흐름): 다크 고정 카드 → 테마 연동
  (라이트 토큰 세트 + invalid light: 접두 제거)
- 트렌드 차트 라이트 축 라벨/그리드/축선 진하게 (plot-chart)
- FAQ 카테고리 뱃지 라이트 -700 글자
- 검증: 채팅 일반현황(2024년 단일색)·경보분석 다이어그램·GIS 인스펙터
  라이트 스크린샷 확인

## 채팅 트렌드 재갱신 재검증 (사용자 "채팅만 남아있다")

- PlotChart 에 EChartWrapper 공통 animationKey(데이터 정체성 키) 적용 +
  등록 시점을 onChartReady 로 (dynamic 로딩 패스 선등록 시 첫 draw 까지
  무애니 되던 부작용 수정)
- 최신 빌드 계측: 초기 draw 1회(+218 변이) 후 30초간 재갱신 0 — 해소.
  이전 빌드로 열린 탭은 새로고침 필요

## 화이트 모드 라운드 2차 + 기능 버그 3건

- 평소 대비/향후 전망 배지: 라이트 -700/다크 -300 듀얼 (인과 카드 purple 포함)
- 답변 시작 "■" 검정 네모: example3.json summary 7건 접두 제거 (백엔드 커밋)
- "상세 원인 진단" 무동작: chat:intent 이벤트 **리스너 부재** —
  채팅 페이지에 리스너 신설(question 동반 dispatch → submitCorrection 강제
  인텐트 실행), 트렌드 메뉴 쪽은 기본 /chat?q= 라우팅으로 정리
- 계통도 숨김 노드가 라인 뒤로 보임: dimmed 를 그룹 투명(opacity .18) →
  **불투명 배경 rect + 내용만 30%** 로 변경 (라이트/다크 각 배경색)
- 추가 가시성: 스트리밍 단계 칩·ANALOG/DIGITAL 통계 뱃지·GIS 인스펙터
  "시설 정보"/공급시간·인과 규칙 요약 숫자/뱃지 — 라이트 진한 계열 듀얼

## GIS 시설 마커 미표시 재발 (E-038 재발)

- 로그인 직후 관망도에 알람 리플만 보이고 시설 도형·클러스터 미표시 재발
- 원인: 자가 복구 상한 3회가 세션 누적 — 초기 pmtiles 로딩 churn 에서
  소진되면 이후 영구 방치
- 수정: 상한을 스타일 로드 에피소드당으로 변경 (style.load 마다 리셋 +
  300ms 후 즉시 재검사 + 상한 도달 console.error 관측)


## 업무 메모 + 일정 알림 구현 (신규 기능)

- 요청: 메모(제목/내용, 날짜·제목·내용·작성자 검색) + 일정 알림(달력 등록 →
  정시 팝업) DB 영속. 채팅·영상·음성 대화는 사양 검토만
- 구현: Migration 0105 + backend memo/user_schedule + /reports/memo·schedule +
  전역 ScheduleAlarmPopup(30s 폴링). 보고서 메뉴 그룹에 등록
- 사양: memo-schedule-spec.md 신설, realtime-comm-spec.md 검토안(결정 4항 대기)
- E-043: 커넥션 풀 미반환 연쇄 500 — finally close 패턴 기록
