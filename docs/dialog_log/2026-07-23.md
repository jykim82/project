# 2026-07-23

## 향후 전망 오류(E-048) + 채팅 트렌드 적산차 + 라벨 마무리

- 난지마을 수위 전망 급락 보고 → 원인: 컨테이너에 chronos 미설치로 전망
  전체가 선형 폴백 고정 (E-048). 설치·복구 후 method=chronos_bolt,
  주기 범위 내 완만 예측 실검증. 이미지 재빌드 필요 주의 기록
- 사용자 진단대로 채팅 트렌드는 적산 원값 그대로였음 → ai_server plot
  조립 지점에 트렌드 메뉴와 동일한 적산차 변환+라벨 적용
  (convert_accum_rows_to_diff)
- 트렌드 메뉴 시리즈명 "(적산차)" 표기 경로 수정 (displayTags)

## E-049 — 전망 주기 왜곡 2중 결함 수정 (사용자 재보고 "지금도 안 맞아")

- 직접 검증: Chronos 원호출은 주기 재현(진폭 1.4)하는데 카드 전망만 납작
  → ① 스텝 스케일 불일치(30분 그리드 vs 6분 입력 단위) ② 수위 상한 캡이
  하한 임계에 오적용돼 고점 절단
- 수정 후 동일 태그: 진폭 0.31 → 1.48 (1.49~2.97), 실측 일주기와 일치

## E-049 추가 — 주기·연속성 (사용자 재검증 "진동폭 다르고 안 이어짐")

- 30분 컨텍스트 다운샘플 48스텝 원샷 전환 + seasonal_24h 경로(r>=0.6) +
  연속성 앵커(오프셋 선형 점감). 난지마을은 주기 불안정(r<=0.55) 판명 —
  중앙값 평탄+밴드가 정직한 표현임을 확인, 연속 격차 0.0 검증

## E-049 최종 — 지배 주기 자동 탐지 (사용자: "옆 패턴과 흡사해야")

- 시간 격자 ACF 재계산: 난지마을 = 14h 주기 r=0.81 (24h 아님, 위치 기반
  배열의 시간축 압축이 검출 실패 원인)
- seasonal_{N}h 경로: 8~36h 스캔 최적 주기 → 직전 주기 반복 + 연속성 앵커.
  검증: seasonal_14h, 진폭 1.48, 연속 격차 0.0 — 실측 파형 동형
- 프런트 method 라벨 동적 처리 (반복 주기 패턴 N시간)

## 달력 항목 직접 클릭 편집 (구글 캘린더식)

- 일정·메모 달력의 셀 내 항목을 직접 클릭 → 보기/수정 다이얼로그
  (기존: 날짜 선택 → 옆 목록 클릭 2단계). 실검증: 일정 "팝업 검증 일정"·
  메모 "스모크 테스트 메모" 모두 수정 창 오픈 확인

## E-050 — /chat 스택 오버플로 (사용자 오류 화면 보고)

- 원인: 어제 라벨 치환 스크립트가 forecastMethodLabel 내부 조회까지 자기
  호출로 바꿔 무한 재귀 (전망 포함 카드 전체 크래시)
- 복원 + 외부 도메인 트렌드 질의 E2E 재검증 (렌더 정상·예외 0)
