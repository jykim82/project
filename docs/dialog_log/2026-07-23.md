# 2026-07-23

## 향후 전망 오류(E-048) + 채팅 트렌드 적산차 + 라벨 마무리

- 난지마을 수위 전망 급락 보고 → 원인: 컨테이너에 chronos 미설치로 전망
  전체가 선형 폴백 고정 (E-048). 설치·복구 후 method=chronos_bolt,
  주기 범위 내 완만 예측 실검증. 이미지 재빌드 필요 주의 기록
- 사용자 진단대로 채팅 트렌드는 적산 원값 그대로였음 → ai_server plot
  조립 지점에 트렌드 메뉴와 동일한 적산차 변환+라벨 적용
  (convert_accum_rows_to_diff)
- 트렌드 메뉴 시리즈명 "(적산차)" 표기 경로 수정 (displayTags)

## E-049 — 전망 주기 왜곡 2중 결함 수정 (사용자 재보고 "지금도 안 맞아")

- 직접 검증: Chronos 원호출은 주기 재현(진폭 1.4)하는데 카드 전망만 납작
  → ① 스텝 스케일 불일치(30분 그리드 vs 6분 입력 단위) ② 수위 상한 캡이
  하한 임계에 오적용돼 고점 절단
- 수정 후 동일 태그: 진폭 0.31 → 1.48 (1.49~2.97), 실측 일주기와 일치

## E-049 추가 — 주기·연속성 (사용자 재검증 "진동폭 다르고 안 이어짐")

- 30분 컨텍스트 다운샘플 48스텝 원샷 전환 + seasonal_24h 경로(r>=0.6) +
  연속성 앵커(오프셋 선형 점감). 난지마을은 주기 불안정(r<=0.55) 판명 —
  중앙값 평탄+밴드가 정직한 표현임을 확인, 연속 격차 0.0 검증

## E-049 최종 — 지배 주기 자동 탐지 (사용자: "옆 패턴과 흡사해야")

- 시간 격자 ACF 재계산: 난지마을 = 14h 주기 r=0.81 (24h 아님, 위치 기반
  배열의 시간축 압축이 검출 실패 원인)
- seasonal_{N}h 경로: 8~36h 스캔 최적 주기 → 직전 주기 반복 + 연속성 앵커.
  검증: seasonal_14h, 진폭 1.48, 연속 격차 0.0 — 실측 파형 동형
- 프런트 method 라벨 동적 처리 (반복 주기 패턴 N시간)

## 달력 항목 직접 클릭 편집 (구글 캘린더식)

- 일정·메모 달력의 셀 내 항목을 직접 클릭 → 보기/수정 다이얼로그
  (기존: 날짜 선택 → 옆 목록 클릭 2단계). 실검증: 일정 "팝업 검증 일정"·
  메모 "스모크 테스트 메모" 모두 수정 창 오픈 확인

## E-050 — /chat 스택 오버플로 (사용자 오류 화면 보고)

- 원인: 어제 라벨 치환 스크립트가 forecastMethodLabel 내부 조회까지 자기
  호출로 바꿔 무한 재귀 (전망 포함 카드 전체 크래시)
- 복원 + 외부 도메인 트렌드 질의 E2E 재검증 (렌더 정상·예외 0)

## E-049 4차 — 전망 고점 초과 (앵커 램프 단축)

- 오프셋 전 구간 점감 → 패턴 고점 들림 (전망 3.3 > 실측 3.01). 램프 2h
  로 단축 — 실측 max 3.01 / 전망 max 2.99, 연속 격차 0.0 검증

## 사양 동기화 (사용자 지시)

- trend-comparison-spec §12 전망 v2(엔진 우선순위·시간 격자·주기 탐지·앵커
  램프·Chronos 규약·클램프·검증 4종) + §13 적산차
- leak-alert-spec AI 요약 범위 보강, flow-diagram-mode-spec 펌프 호기 표시

## 일정 달력 더블클릭 등록

- 날짜 셀 더블클릭 → 해당 날짜 프리필 신규 등록 (E2E: 25일 더블클릭 →
  2026-07-25 프리필 확인). 항목 위는 제외(단일 클릭=수정 유지)

## 메모 달력 더블클릭 동일 적용

- 날짜 셀 더블클릭 → 새 메모 작성 창 (E2E 확인). 작성일 자동이라 날짜
  프리필 없음

## E-051 — 이상 진단 +1257% (기준선 오염) + 전망 기울기 앵커

- 계측 개시 이전 노이즈가 30일 평균 오염 → tag_scale(최근 3일 중앙값×10%)
  상대 임계. 검증: 0.18→2.35, +1257%→+2.6%
- 전망 앵커에 기울기 연속성(초반 4h 블렌드) — 톱니 신호 역방향 시작 방지

## 계층형 토폴로지 노드 라벨 대비 개선 (사용자: "박스 안 글자가 배경색과 비슷")

- 다크모드 IP/시리얼 라벨 `#2d4a6e`(배경과 동색) → `#94a3b8`, 장비명
  `#cbd5e1`→`#e2e8f0`, 사이트명 라벨 투명도 .6→.85. 위계(장비명>IP>
  미감시 시리얼) 유지 — hierarchical-topology.ts
- 실화면 확대 검증: L2 스위치/NAC/SSLVPN 장비명·IP 판독 가능 확인

## 평소 대비 패턴 불일치 (사용자: "24시간 전 기준인가?")

- 기준은 24h 전이 아니라 GBT 정상 기대값(폴백: 같은 요일×시각 14일 평균)
- 실원인: **baseline 주1회 재학습 cron 미등록** — 모델이 6/16 학습본 그대로
  (IForest 는 서버 내장 루프라 정상, baseline 만 외부 cron 의존)
- 조치: 즉시 재학습(20260723_074609, 831태그) + launchd 주간 cron 등록
  (일 03:30). 모델 평가 API 반영 확인
- 홀드아웃 개선율 -3.6% (hm 열세) — 게이트 도입 검토 항목 등록

## 학습 지연 경고 배지 + hm 게이트 구현 (사용자 "1,2 둘다 구현")

- TrainFreshnessBadge (baseline 주7일/IForest 1일, 주기+유예 초과 amber·
  2배 red) — model-eval 두 뷰 공통
- 태그별 hm 게이트 (Migration 0114): holdout 에서 hourly_mean 이 3% 이상
  우세한 태그는 추론 시 hm 폴백. 재학습 결과 725/831 게이트, 신평 태그
  폴백 동작 검증. 평가 화면 "게이트→평균" 배지 + KPI 부제 + 용어 2건

## 성상1 유출압력 이상 (사용자 육안 발견)

- 7/21~22 이틀 계측 상한 10 포화 → 7/23 정상 복귀. 설정압력 7.8 불변·
  펌프 패턴 불변 → 트랜스미터 고착 의심. 압력 임계 태그 부재로 알람 0,
  기준선 오염으로 정상 복귀를 "주의↓" 역판정 — review-items 선택지 3종 기록

## 센서 포화·고착·DI 반전 감지 구현 (사용자 "이부분을 개선하여 반영해줘")

- 성상1 유출압력 이력 전수: 7일 창에선 전례 없어 보였으나 **6/1 주부터
  포화 간헐 재발 7주째** (주간 ≥9.5 샘플 최대 6,424건) — 현장 점검 대상
- anomaly_scan 신규 2종: ① `_detect_sensor_saturation` (센서포화의심 —
  6h 80%+ 가 90일 관측 상한 고정 / 신호고착의심 — 6h flat + 직전 7일 변동)
  ② DI 상시 ON 게이트 (7일 on 95%+ → 설비 장애 제외, "DI상시ON의심" 강등)
- 검증: 성상1 7/21 시점 시뮬 pinned 100% 발화 / 현재 전수 23건(오탐 0) /
  DI 32건 게이트·실장애 36건 잔존. 프런트 DQ 색상·아이콘·정렬 3종 추가

## 죽동 power_fault 실발생 검증 (사용자 요청)

- DI "죽동(배) 정전 발생" 상시 val=1 (30일간 0 없음) — AI 요약은 데이터
  충실하나 신호 자체가 고착/반전 의심. 동일 패턴 사이트 10곳+ 전수 확인,
  SCADA 알람 이력 0건. review-items 에 게이트 선택지 기록
