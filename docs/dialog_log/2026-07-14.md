# 2026-07-14 세션 대화 로그

## 코드 최적화 시점 검토 → 순차 수행 승인

### 사용자 요청
- "일정 기간후에 코드 최적화를 수행중에 있어. 지금 코드 최적화를 다시 수행하는게 맞는지 검토해줘"
- 검토 결과(마지막 정리 4월 이후 3개월/+39K줄 드리프트, 중복 비용 실증, 깨끗한 체크포인트) 보고 → "응 순차적으로 수행해줘"

### 승인된 순서
1. GBT baseline 40s 최적화 (시 버킷 예측)
2. supply_time/Metric 표시 로직 공용화 (4곳 중복)
3. endpoints/epanet.py 3,223줄 패키지 분리
4. ai_server.py Phase 4 분리

## 1. GBT baseline 시(hour) 버킷 최적화 (완료)

### 원인·해결
`gbt_baseline()` 이 타임스탬프마다(7일 1분 버킷=10,080행) 피처 생성 + `_roll()` 전체
스캔. 모든 피처가 시(hour) 단위에서만 변하므로(floor('h') + KST 고정 오프셋) 고유 시
버킷(~169행)만 예측 후 매핑 — **완전 동치 변환** (`trend_baseline.py`).

### 검증
- 고정 시간창 구/신 동일성: 10,080개 값 완전 일치 (불일치 0건)
- 벤치: 4태그 78.1s → 3.06s (25×)
- 실 UI(트렌드 페이지): 4태그 7일 /trend/data 636ms, 평소 대비·향후 전망 배지 정상
- 사양: trend-comparison-spec §6.4 ③ 과제 → 완료로 갱신

## 2. supply_time 표시 로직 공용화 (완료)

### 변경
- 신규 `src/lib/supply-time.ts` — `getSupplyTimeDisplay`(충전 배지/24h+ 캡/미구축) +
  `getLevelStatus`/`LOW_LEVEL_PCT`/`HIGH_LEVEL_PCT` 단일 소스
- 소비처 5곳 교체: GisDetailPanel / GisFacilityCard / GisFacilityPopup /
  FlowMonitoringGraph(툴팁+오버레이) / monitoring/flow 사이드 리스트
- WaterLevelGauge 게이지 색 임계도 공용 상수로 통일

### 공용화 효과 실증
공용화 작업 중 **5번째 누락 지점 발견** — 용수 흐름 페이지 "배수지 공급가능시간"
사이드 리스트가 충전 상태에서 여전히 24.0h 표기(07-12 수정 누락). 단일 소스 전환으로
해소, 이후 규칙 변경은 supply-time.ts 한 곳만.

### 검증 (실제 브라우저)
- GIS 난지관광: "충전 중 ↑ / 소진 없음 / ⚠ 수위 10% 저수위 주의" (회귀 없음)
- 용수 흐름 사이드: 충전 배수지 10곳 "충전 중 ↑", 정상 계산(행정 9.4h 등)은 숫자 유지
- tsc: 수정 파일 신규 오류 0건 (기존 프로젝트 오류만 잔존)

## 3. endpoints/epanet.py 3,223줄 → 패키지 분리 (완료)

### 변경
`endpoints/epanet/` 패키지 12 모듈로 분리 — common(라우터·게이트·공용 헬퍼) /
points_crud / flow_map / deviation / menu_settings / leak_headloss / whatif /
assessment / data_quality / artifacts / simulations / __init__(라우터 re-export).
본문은 원본 라인 그대로 복사(동작 무변경), 공유 헬퍼 import 는 참조 스캔으로 자동
결정. `from endpoints.epanet import router` 경로 유지 → ai_server 무변경.

### 검증
- 라우트 48개 분리 전/후 (method, path) 완전 일치 (diff 0)
- EPANET 임시 활성화(use_yn Y) 후 GET 20개 전체 본문 200 스모크 → 원복(N) 확인
- 비활성 게이트 503 동작 유지 (분리 전과 동일)
- 사양: gis_plan.md 모듈 구조 갱신 (히스토리성 기록은 유지)
