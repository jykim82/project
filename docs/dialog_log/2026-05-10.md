# 2026-05-10 세션 로그

## 요청 흐름
- "관리 메뉴의 EPANET 시뮬레이션 메뉴에서 전체 활성/비활성 토글 추가"
- "GIS 관망도에서 물흐름 방향 표시기능만 별도로 물흐름표시 라는 버튼"
- "비활성화 토글 → 시뮬·누수 의심·…·교체 후보까지 숨김"
- "정말 동작하는지 테스트도 해줘"
- "마스터 비활성시에도 물흐름은 나타나도록"
- "비활성시에는 모니터링 메뉴에서도 누수의심구간/헤드손실 이상구간도 숨겨줘"
- "비활성화 된 것도 db에서 관리해야돼 재시작해도 남아야 하니까"
- "분석 서브메뉴가 안보이는건 비활성화 한게 있어서 그런가?"
- "epanet 항목을 비활성화 할때 연관된 상위 메뉴도 비활성화 되어야해"

## 변경 요지

### 1. EpanetMenuToggles 마스터 스위치
- 백엔드 PUT `/admin/epanet/menu-settings/bulk` (region/enabled bool)
- 프런트 [전체 활성] / [전체 비활성] 버튼 (10 메뉴 일괄)
- DB 영구 저장 (`tb_epanet_menu_setting`) — backend 재시작 후에도 유지

### 2. 물흐름 화살표 별도 토글
- 신규 `GisFlowArrowLayer` — 옅은 회색 line + 정/역 색상 화살표만 (압력 색상 X)
- 기존 `GisEpanetSimLayer` 의 화살표 layer 제거 (분리 명확)
- GIS 페이지 [물흐름 표시] 토글 (cyan)
- **마스터 비활성에 영향 안 받음** — EPANET 메뉴와 별도의 단순 시각화

### 3. data-quality 캐시 → Zustand store
- `useStore` (byRegion / loadingRegions / fetched) + `invalidateEpanetDataQuality(region)` export
- 토글 변경 후 모든 mount 컴포넌트 즉시 갱신 (사이드바·탑바·GIS 토글)

### 4. AppTopbar statusOf 필터 추가
- 사이드바와 동일 로직 — 탑바 모드도 disabled 메뉴 hidden

### 5. M003-5 GIS 관망도 dataQualityKey 제거
- 기존 메뉴이므로 EPANET 마스터 토글 영향 안 받게
- GIS 페이지 안의 `gis-flow` 토글은 별도 (변경 X)

### 6. 자식 모두 disabled → 상위 그룹 hidden
- AppSidebar / AppTopbar 양쪽 적용
- 분석 그룹 (M008) 자식 3개 모두 EPANET → 마스터 비활성 시 그룹 사라짐
- 위기대응·모니터링은 EPANET 외 자식 있어 그룹 유지 (자식만 hidden)

## 검증 (Playwright)

| 상태 | 표시 그룹 |
|------|----------|
| 마스터 비활성 | 위기대응 / 모니터링 / 트렌드 / 보고서 / 관리 / 구축 (6 — **분석 사라짐**) |
| 마스터 활성 | + 분석 (7) |

**GIS 페이지**:
- 마스터 OFF → [물흐름 표시] 1개만
- 마스터 ON → 7개 토글 (물흐름 / 시뮬 / 누수 / 헤드손실 / 밸브 / 파손 / 교체)

**DB 영구성**:
- backend 컨테이너 재시작 → tb_epanet_menu_setting.enabled 'N' 그대로 유지
- API 응답 `menus_disabled: 10` 정상

**콘솔 에러** 0.

## dialog_log 롤링 (3일)
- 05-08, 05-09 유지 (D-2, D-1)
- 05-10 신규 (오늘)
- 05-07 삭제 (D-3)

## 후속 — 성능 검토 + 채팅 슬롯 누수 fix
사용자 질문 흐름: "EPANET 별도 분리 / 비활성 성능 영향?" → "1,2 둘 다 진행" → "구조적 검토" → "경보 누적 TOP 10 답 안 나옴"

### EPANET 성능 분리 — 구조적 검토
- 현재 구성: 같은 backend 컨테이너, 같은 uvicorn 프로세스
- **이미 Lazy import 구조** 확인됨:
  · `epanet/simulator.py` line 32 `from pyproj import Transformer` — 함수 내부
  · line 119, 303 `import wntr` — 함수 내부
  · `epanet/shp_reader.py` `import shapefile` — 함수 내부
  · `epanet/inp_converter.py` line 429 `import wntr` — 함수 내부
- 메모리 측정 (실제):
  | 시점 | RSS |
  |------|-----|
  | backend 시작 직후 (wntr 미로드) | 310 MB |
  | /status 호출 (wntr 미사용) | 391 MB |
  | /sim/cron 호출 (wntr 첫 로드) | **2.72 GB** (+2.3GB) |
- 비활성·미사용 시: CPU/IO 0, 메모리 0 ✓
- 사용 시 점유: wntr+numpy+pandas+scipy 모듈 자체 ~500MB + 큰 INP (12k link) 시뮬 결과 1.8GB
- Python 모듈 unload 불가 — 절대 격리는 사이드카 분리 필요

### EPANET 시뮬 GC 추가 (즉시 적용)
- `simulator.py` `run_steady_state` / `run_what_if` 종료 시 `del wn; del results; gc.collect()`
- 목적: 이전 wn 객체 즉시 회수 → 누적 호출 시 메모리 폭증 방지
- import 모듈 자체는 unload 안 됨 (Python 한계) — 첫 호출 후 ~500MB 점유 유지

### 별도 분리 옵션 비교
| 방법 | 작업량 | 메모리 격리 | CPU 격리 | 권장 |
|------|------|----|----|------|
| 현재 (Lazy + GC) | XS | 부분 | X | 단일 사이트, 1일 1~2회 cron |
| 사이드카 (slm-epanet) | 중 (3~4h) | ✓ | ✓ | 다중 사이트, 잦은 시뮬, 메모리 제약 |
| 워커 큐 (Celery+Redis) | 큼 | ✓ | ✓ | 시나리오 batch 100+ |

### 채팅 슬롯 컨텍스트 누수 fix
사용자 보고: "경보 발생 누적건수 높은 순서대로 10개만 보여줘" → 답 0건

원인:
- 인텐트 `FACILITY_ALARM_TOP_COUNT` 매칭은 정상 (132ms keyword)
- 그러나 이전 대화의 sitename='석문' / facilitytype='배수지' 가 세션 컨텍스트로 자동 채워짐
- SQL: `WHERE sitename='석문' AND facilitytype='배수지'` → 0건

수정 (`ai_server.py` 2곳, line 3293 /ask + line 4843 /ask/stream):
```python
_ALARM_PIE_INTENTS = {"FACILITY_ALARM_CAUSE_DIAGNOSIS_RANK", "FACILITY_ALARM_TOP_COUNT"}
if intent in _ALARM_PIE_INTENTS:
    _u_sitename = extract_sitename(user_question or "")
    _u_facilitytype = extract_facility_type_from_question(user_question or "")
    if not _u_sitename or ...:
        params["sitename"] = ""
    if not _u_facilitytype or ...:
        params["facilitytype"] = ""
```

검증 (DB SQL 직접): sitename='', facilitytype='' → 10건 정상
`죽동(배) 탁도계 통신이상 4397건` / `죽동(배) 수위#1 HH 알람 234건` 등.

커밋: `slm@fa4c1e6` (chat fix + EPANET GC)

## 후속 — GIS 물흐름 분리 검토 → B-1·B-2 사양·구현
사용자 흐름: "물흐름은 shp 기준이야 아니면 현재 유량데이터 기준?" → "gis 기반 vs
실측 기반으로 버튼 나누면 도움?" → "b-1, b-2 둘다 추천?" → "사양 두개부터
작성하고 이어서 구현"

### 사양 작성
- `docs/epanet-flow-injection-spec.md` (B-1) v1 — 시설 ↔ 실측 outflow 매핑 →
  INP demand IDW 보간 자동 주입. 단위 환산 (cmh/lps/m3h/lpm/m3s → LPS),
  자동 제안 (`tb_tag_info.datainfo` 패턴 매칭), manual 점 ±10m 우선순위.
- `docs/epanet-flow-deviation-spec.md` (B-2) v1 — 매핑된 시설별 시뮬 vs
  실측 유량 비교. KNN link 매칭, direction (observed_higher/sim_higher/match),
  threshold_pct 임계, 24h 시계열 차트, GIS 오버레이.

### B-1 구현
- Migration `db/migrations/0071_epanet_facility_flow_map.sql` — 신규 테이블
- backend `slm/endpoints/epanet.py` E3.5 섹션 추가:
  - GET/POST/DELETE `/admin/epanet/facility-flow-map`
  - POST `/auto-suggest` — 패턴 매칭으로 94건 후보 발견 (당진 R01)
  - `_compute_live_demands(region, hours)` 헬퍼 (가압장·블록 outflow → LPS)
  - `_to_lps()` 단위 환산
- INP 변환 `inject_live_demand` + `inject_live_hours` 옵션 추가
- 데이터 품질 게이트 `HAS_LIVE_FLOW` (≥5건) — flow-deviation 메뉴 활성 조건
- frontend `EpanetFacilityFlowMap.tsx` 신규 카드 (자동 제안 다이얼로그 +
  단건 입력 + 목록), `/admin/epanet` 페이지에 추가
- INP 생성 다이얼로그에 [실측 유량 자동 주입] 체크박스 + 시간 윈도우 셀렉터

### B-2 구현
- Migration `db/migrations/0072_epanet_flow_deviation_menu.sql` — M008-4 +
  `tb_epanet_menu_setting` flow-deviation 등록
- backend GET `/admin/epanet/flow-deviation` — 시설별 비교 + KNN link
- backend GET `/admin/epanet/flow-deviation/timeseries` — 24h 차트용
- frontend `FlowDeviationAnalysis.tsx` — 슬라이더 (hours/threshold_pct/min_flow)
  + KPI 4 + 표 + 24h 시계열 다이얼로그 (ECharts)
- frontend `GisFlowDeviationLayer.tsx` — circle 레이어 (의심 펄스), direction
  별 색상 (cyan/orange/gray), 좌하단 범례
- 페이지 `monitoring/flow-deviation/page.tsx` (M008-4)
- 사이드바 `sidebar-menus.ts` + `use-sidebar-menus.ts` (dataQualityKey 매핑)
- GIS 페이지 8번째 토글 [실측 유량 차이] 추가 (pink) — 마스터 게이트 적용
- `DataQualityCard.tsx` 라벨 사전에 `HAS_LIVE_FLOW` / `flow-deviation` 등록

### 검증 (Playwright)
- /admin/epanet — "실측 유량 매핑 (B-1)" 카드 노출, 자동 제안 / 단건 입력 OK
- 메뉴 토글 "11/11 활성" (기존 10 → 11) ✓
- /monitoring/flow-deviation — 게이트 미충족 시 안내 카드 표시 ✓
- /monitoring/gis — 8개 토글 (기존 7 + B-2) 모두 노출 ✓

3-repo 커밋: web (사양 2 + migration 2), slm (endpoints/epanet.py 확장),
slm-dashboard (컴포넌트 3 + 페이지 1 + 사이드바)

## 후속 — 자동 제안 강화 (tb_facility_flow_map join + 좌표 lookup)
사용자 지적: "tb_facility_flow_map 에 상하관계 + tb_tag_info 면 매핑 자동 가능
하지 않나?" → 정확한 지적. 자동 제안 정확도 강화 진행.

### 백엔드 강화 (`endpoints/epanet.py:auto_suggest_facility_flow_map`)
- tb_facility_flow_map 의 상하류 관계 join → 짝 검증
- 신뢰도 점수 산출:
  · **verified**: tb_facility_flow_map 등장 + 짝 매칭 (downstream_inflow 또는
    upstream_outflow 가 함께 발견)
  · **probable**: tb_facility_flow_map 등장 (단방향만)
  · **weak**: 미등장 (datainfo 패턴만)
- 시설별 그룹화 → 같은 (sitename, facilitytype, role) 중복 후보 1개로 축약
  (74건 → 이전 94건 대비 정밀화)
- `unmapped_facilities` 필드 추가 — 수계망에 등장하지만 datainfo 매칭 안 된
  시설 목록 (운영자 수동 입력 안내)
- 정렬: confidence 우선 (verified → probable → weak)
- 응답에 `stats: {verified, probable, weak}` 추가

### 프런트 강화 (`EpanetFacilityFlowMap.tsx`)
- `gis-facility-coords.json` import + EPSG:4326 → 5186 간이 변환 함수
  (당진권 ±5m 정밀도, proj4 의존 없이)
- 자동 제안 받은 후 `lookupFacilityXY(sitename, facilitytype)` 로 좌표 자동
  채움 — 좌표 필요 0건 (당진 R01 검증 완료)
- 기본 선택: verified + probable + 좌표 채워진 항목 (weak 제외)
- 다이얼로그 UI:
  · 신뢰도 뱃지 (✓verified emerald / probable cyan / weak gray)
  · 행 배경색 (verified emerald-tint / weak amber-tint)
  · 검증 컬럼 (downstream_sample / upstream_sample / "수계망 등록")
  · [검증된 항목만] 단축 버튼
  · [누락 시설] 섹션 — chip 리스트 + 안내

### 검증 (Playwright)
- 자동 제안 버튼 클릭 → 74건 후보 노출
- toast: "verified 0 / probable 70 / weak 4"
- 좌표 채움: **74건 모두 자동 채움** (좌표 필요 0건)
- 누락 시설 섹션 노출 (37건 chip 표시)

### 운영 효과
- 이전: 94건 후보 × 좌표 수동 보정 → 30분 이상 소요
- 현재: 74건 정밀 후보 × 좌표 자동 → [선택 등록] 1클릭에 70+ 매핑 등록.
  weak 4건만 검토하면 끝.

3-repo 커밋: slm (auto_suggest 강화), slm-dashboard
(EpanetFacilityFlowMap 확장 + 좌표 변환 헬퍼), web (dialog_log).


