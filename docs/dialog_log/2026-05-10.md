# 2026-05-10 세션 로그

## 요청 흐름
- "관리 메뉴의 EPANET 시뮬레이션 메뉴에서 전체 활성/비활성 토글 추가"
- "GIS 관망도에서 물흐름 방향 표시기능만 별도로 물흐름표시 라는 버튼"
- "비활성화 토글 → 시뮬·누수 의심·…·교체 후보까지 숨김"
- "정말 동작하는지 테스트도 해줘"
- "마스터 비활성시에도 물흐름은 나타나도록"
- "비활성시에는 모니터링 메뉴에서도 누수의심구간/헤드손실 이상구간도 숨겨줘"
- "비활성화 된 것도 db에서 관리해야돼 재시작해도 남아야 하니까"
- "분석 서브메뉴가 안보이는건 비활성화 한게 있어서 그런가?"
- "epanet 항목을 비활성화 할때 연관된 상위 메뉴도 비활성화 되어야해"

## 변경 요지

### 1. EpanetMenuToggles 마스터 스위치
- 백엔드 PUT `/admin/epanet/menu-settings/bulk` (region/enabled bool)
- 프런트 [전체 활성] / [전체 비활성] 버튼 (10 메뉴 일괄)
- DB 영구 저장 (`tb_epanet_menu_setting`) — backend 재시작 후에도 유지

### 2. 물흐름 화살표 별도 토글
- 신규 `GisFlowArrowLayer` — 옅은 회색 line + 정/역 색상 화살표만 (압력 색상 X)
- 기존 `GisEpanetSimLayer` 의 화살표 layer 제거 (분리 명확)
- GIS 페이지 [물흐름 표시] 토글 (cyan)
- **마스터 비활성에 영향 안 받음** — EPANET 메뉴와 별도의 단순 시각화

### 3. data-quality 캐시 → Zustand store
- `useStore` (byRegion / loadingRegions / fetched) + `invalidateEpanetDataQuality(region)` export
- 토글 변경 후 모든 mount 컴포넌트 즉시 갱신 (사이드바·탑바·GIS 토글)

### 4. AppTopbar statusOf 필터 추가
- 사이드바와 동일 로직 — 탑바 모드도 disabled 메뉴 hidden

### 5. M003-5 GIS 관망도 dataQualityKey 제거
- 기존 메뉴이므로 EPANET 마스터 토글 영향 안 받게
- GIS 페이지 안의 `gis-flow` 토글은 별도 (변경 X)

### 6. 자식 모두 disabled → 상위 그룹 hidden
- AppSidebar / AppTopbar 양쪽 적용
- 분석 그룹 (M008) 자식 3개 모두 EPANET → 마스터 비활성 시 그룹 사라짐
- 위기대응·모니터링은 EPANET 외 자식 있어 그룹 유지 (자식만 hidden)

## 검증 (Playwright)

| 상태 | 표시 그룹 |
|------|----------|
| 마스터 비활성 | 위기대응 / 모니터링 / 트렌드 / 보고서 / 관리 / 구축 (6 — **분석 사라짐**) |
| 마스터 활성 | + 분석 (7) |

**GIS 페이지**:
- 마스터 OFF → [물흐름 표시] 1개만
- 마스터 ON → 7개 토글 (물흐름 / 시뮬 / 누수 / 헤드손실 / 밸브 / 파손 / 교체)

**DB 영구성**:
- backend 컨테이너 재시작 → tb_epanet_menu_setting.enabled 'N' 그대로 유지
- API 응답 `menus_disabled: 10` 정상

**콘솔 에러** 0.

## dialog_log 롤링 (3일)
- 05-08, 05-09 유지 (D-2, D-1)
- 05-10 신규 (오늘)
- 05-07 삭제 (D-3)

## 후속 — 성능 분리 검토 (다음 세션)
사용자 질문: "EPANET 시뮬 별도 프로그램 분리되어 성능 영향 없는 구성? 비활성화만으로 성능 영향 없는지?"
- 현재 구성: 같은 backend 컨테이너 내부 모듈 (import 시 메모리 ~100MB 영구 점유)
- 비활성화 시: CPU 비용 0, 메모리 점유는 유지
- 진정한 분리 옵션: Lazy import / 사이드카 마이크로서비스 / Worker 풀
