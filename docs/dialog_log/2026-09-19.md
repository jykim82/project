# 2026-09-19 세션 로그

## 서비스 기동 + duckdns 진단

**사용자**: "서비스 기동해줘" → Docker 스택은 이미 가동, 호스트 네이티브
2종(Ollama·Vision Agent 8100)만 다운 → brew services + venv 백그라운드 기동.
**duckdns 접속 불가**: 서버측 전부 정상(Caddy 443·LE 인증서·DNS 일치)인데
외부 체커 3개국 전부 타임아웃 → **공유기 포워딩이 현 Mac LAN IP
(192.168.219.136)를 가리키지 않음** (IP 고정 미완료 상태에서 DHCP 변경 추정).
임시로 cloudflared 터널 제공, 공유기 포워딩 수정 + IP 고정은 사용자 조치
항목으로 안내.

## 캔버스 정렬 + 키오스크 P2 + 테스트 4회

**사용자**: "1순위(캔버스 정렬) 진행하되, 키오스크 P2 기능 테스트를 4회 수행"
→ 범위 확인 질문에 "P2 구현 + 4회 검증" 선택.

- **캔버스 정렬**: 박스 선택(좌드래그)·우클릭 팬·정렬 5종 — 양 레이어 공용
  (align-utils + AlignButtons). 계통도 undo 통합, 통신망 즉시 저장.
  Playwright 실검증 통과. 테스트로 흐트러진 통신망 배치는 계층 정렬 복원
- **키오스크 P2 (Migration 0142)**: KIOSK_VIEWS(목록·순서)/KIOSK_NIGHT_DIM
  (야간 밝기)/KIOSK_KPI_STRIP + 관리자 카드 + 키오스크 페이지 키 기반 순환
  재작성 (alarm 항상 상주). 백엔드 GET/PUT + 검증 400
- **부수 수정**: admin.py site-settings PUT — HTTPException 삼킴(400→200)
  재전파 수정
- **기능 테스트 4회 전부 통과** (기본/목록·순서 UI/야간 창 내/KPI 끔+자동
  순환), 콘솔 에러 0. 테스트 후 설정 기본값 원복
