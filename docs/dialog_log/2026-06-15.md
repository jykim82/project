# 2026-06-15 세션 대화 로그

## 1. 네트워크 장비 테이블 행 클릭 → 상세 패널 + 영향 측정 태그 연결

### 배경
- 직전 작업(L1→L3 역추적 "영향 측정 태그" 섹션)은 토폴로지 그래프 노드 클릭으로만
  열려, 운영자가 `/network` 하단 **장비 상태 테이블**에서 ping-down 장비를 봐도
  바로 영향 태그를 확인할 동선이 없었음.
- 178개 장비가 모두 토폴로지 노드(295개)에 포함 확인 → 행 클릭 시 항상 노드 resolve.

### 변경 파일
- 프론트 `src/components/network/DeviceTable.tsx` — `selectedId`/`onSelectDevice`
  prop 추가. `DeviceRow`·`FacilityGroup`(그룹 뷰)에 전파. 행 클릭 가능 +
  선택 행 `bg-primary/10` 하이라이트 + 안내 title.
- 프론트 `src/app/(dashboard)/network/page.tsx` — `handleDeviceSelect`
  (노드 선택 + 토폴로지 카드로 smooth scroll + 접힌 섹션 자동 펴기) + 토폴로지
  카드 ref. `<DeviceTable>` 에 `selectedId`/`onSelectDevice` 연결.
- `docs/tag-monitoring-spec.md` — §3.0.1 L1→L3 역추적 위치에 "장비 테이블 행
  클릭으로도 진입" 명시.

### 검증
- tsc: 본 변경 신규 에러 0 (baseline 172건 동일, 무관 line 59 pre-existing).
- 프론트(Playwright 로그인 후 `/network`): 장비 테이블 `plc_37` 행 클릭 →
  상세 패널 열림, 행 `bg-primary/10` 하이라이트, "영향 측정 태그 (13)" 신선
  값(압력 0.62, 순시유량 182.6, 적산 2,381,155, "방금 전") 렌더. 콘솔 에러 0.
  tmp 스크린샷 저장.
