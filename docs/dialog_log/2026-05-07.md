# 2026-05-07 세션 로그

## 요청
- "실제 메뉴가 구현되었는지 확인해줘" → 0건 구현 확인
- "진행해줘" → Phase 2.7 착수

## 변경 요지 — EPANET Phase 2.7 (메뉴 뼈대 + 데이터 품질 게이트)

### Migration 0066
- tb_menu 10건: M003-9/10 (모니터링 하위), M006-4~7 (위기대응 하위), M008 분석 그룹 + M008-1~3
- tb_auth_menu MASTER/ADMIN 권한 20건

### 백엔드 (slm)
- `endpoints/epanet.py` — `GET /admin/epanet/data-quality?region=R01`
  · 9 항목 체크: PIPE_NETWORK / RESERVOIR_HEAD / ELEVATION / DEMAND / TIME_PATTERN / METER_MAP / VALVE / PUMP / WATER_QUALITY_MODEL
  · ELEVATION 판정: junction.head_m - pressure_m 의 stddev > 0.5
  · DEMAND 판정: 양수 demand 의 distinct 수 > 1
  · VALVE/PUMP 판정: INP 파일의 [VALVES]/[PUMPS] 섹션에 데이터 있는지 직접 파싱
  · 메뉴별 ready/warning/blocked 매트릭스 (`_MENU_REQUIREMENTS` 10 메뉴)

### 프런트 (slm-dashboard)
- `components/epanet/DataQualityCard.tsx` 신규 — Ready/Warning/Blocked 3 단계
  · ✅ Ready: 작은 emerald 배지
  · 🟡 Warning: amber 카드 + 결과는 [참고용] 워터마크
  · 🔴 Blocked: destructive 카드 + [관리 페이지로 이동]
- `components/epanet/EpanetMenuPlaceholder.tsx` 신규 — 메뉴별 공통 placeholder (제공 예정 기능 + DataQualityCard)
- 9 placeholder 페이지:
  · /monitoring/leak-suspicious, /monitoring/headloss-anomaly
  · /crisis/valve-impact, /crisis/pipe-break, /crisis/pump-control, /crisis/scenario-diff
  · /analysis/replacement-candidates, /analysis/network-aging, /analysis/water-quality
- `hooks/use-epanet-data-quality.ts` 신규 — region 별 1회 fetch + 캐시
- `components/layout/AppSidebar.tsx` — 자식 메뉴에 dataQualityKey 점 (amber/red)
- `lib/config/sidebar-menus.ts` — SidebarMenuChildDef 분리 + 9 신규 메뉴 fallback + dataQualityKey 필드
- `hooks/use-sidebar-menus.ts` — `MENU_DATA_QUALITY_KEY` 매핑 (API 응답에 dataQualityKey 주입)

### Playwright 검증
- /crisis/valve-impact (Blocked) — "필수 데이터 부족" + 밸브 SHP 미반영 안내 + [관리 페이지] 버튼
- /crisis/pipe-break (Warning) — 일부 데이터 부족 안내 + [참고용] 워터마크 + 제공 예정 기능 미리보기
- 탑바에 "분석" 그룹 신규 노출
- 콘솔 에러 0

### 메뉴 분류 결과 (현재 데이터 상태 기준)
- ready: (없음 — 모두 권장 데이터 부족)
- warning: gis-flow, headloss-anomaly, pipe-break, scenario-diff, replacement-candidates
- blocked: leak-suspicious, valve-impact, pump-control, network-aging, water-quality

### 사양 갱신
- `docs/epanet-menu-spec.md` §6 변경 이력에 Phase 2.7 완료 항목 추가
- 사양상 임시 코드 (M030-/M040-/M050-) 가 실제로 M003-/M006-/M008- 로 등록됐음을 명시

## 다음 단계 (Phase 3 — 표고/수요 입력)
- DEM 매핑 또는 사용자 수동 표고 입력 UI
- 계량기 → EPANET 노드 매핑 (`tb_epanet_meter_map`) + 시간대별 수요
- 활성화 즉시 메뉴 1~3 (gis-flow / leak-suspicious / headloss-anomaly) 가치 있는 결과

## 추가 — Phase 3.1 (표고 입력) 구현 완료
사용자: "이어서 진행"

### 현황 분석
- SHP 속성·시설 마스터에 직접 표고 컬럼 없음
- SHP PolyLineZ 의 z 값이 모두 0
- → 운영자 입력 경로 필요

### 변경 요지
- Migration 0067 — `tb_epanet_elevation_point` 신규 (region/x/y/elevation_m/source[manual,csv,facility]/label/notes)
- 백엔드:
  · `endpoints/epanet.py` — `/admin/epanet/elevations` GET/POST/DELETE + bulk-csv
  · `inp_converter.py` — `_idw_elevation()` 헬퍼 + 통합 (k=4, power=2)
  · `GenerateRequest.use_elevation_points` (default true) + `use_synthetic_elevation` (default false)
  · 합성 표고: NW 30m → SE 5m 그라디언트
- 프런트:
  · `EpanetElevationInput` 컴포넌트 신규 (단건 입력 + CSV 업로드 + 표 + 삭제)
  · `/admin/epanet` 페이지 변환 카드에 표고 옵션 체크박스 추가
  · 페이지 하단에 표고 입력 카드 통합

### 검증
- 단건 입력 / 조회: 동작
- 합성 표고 ON → 시뮬 #9 — 압력 20.01~41.67m (음수 없음, 의미 있는 분포) / flow ±3.4 LPS
- data-quality: HAS_ELEVATION ok=true (분포 36.7~80m 초기 / 5~30m 조정 후)
- 메뉴 분류: warning 5 → 1 (gis-flow/pipe-break/scenario-diff/replacement-candidates 가 ready)

### Phase 3.2 / 3.3 (다음)
- 3.2 수요 입력 — 노드별 수요 분포 (계량기 또는 운영자 입력)
- 3.3 센서 매핑 (`tb_epanet_meter_map`) — leak-suspicious / network-aging 활성

## 추가 — Phase 3.2 (수요 입력) 구현 완료
사용자: "응 진행해줘"

### 변경 요지
- Migration 0068 — `tb_epanet_demand_point` 신규 (Phase 3.1 표고와 동일 패턴)
- 백엔드:
  · `/admin/epanet/demands` GET/POST/DELETE + bulk-csv
  · `inp_converter.py` — `demand_points` + `use_synthetic_demand` 옵션
  · 합성 수요: bbox 중심 1.0 LPS → 외곽 0.05 LPS 그라디언트 (도심 집중 흉내)
  · 우선순위: **합성 ON 이면 운영자 입력 무시** (시연 모드 명시적 의도) — 표고도 동일하게 갱신
- simulator.py: 시뮬 응답에 `junction.elevation_m` 포함 (data-quality 검증 정확도 향상)
- 프런트:
  · `EpanetDemandInput` 컴포넌트 신규
  · admin/epanet 변환 카드에 수요 옵션 체크박스
  · 페이지 하단에 수요 입력 카드 통합

### 검증 (artifact #14, 합성 표고+수요 ON)
- 시뮬 #12: 압력 20.01~41.66m / flow -11.36~+20.24 LPS
- elevation distinct 96, demand distinct 47 — 노드별 다양성 확보
- HAS_ELEVATION ok=true (σ=4.46), HAS_DEMAND_PROFILE ok=true
- 메뉴 분류: **ready 4** (gis-flow/pipe-break/scenario-diff/replacement-candidates) / warning 1 / blocked 5

### Phase 3.3 (다음)
- 센서 ↔ EPANET 노드 매핑 (`tb_epanet_meter_map`)
- 활성 시 leak-suspicious / network-aging 메뉴 ready

## dialog_log 롤링 (3일)
- 05-05, 05-06 유지 (D-2, D-1)
- 05-07 신규 (오늘)
- 05-04 삭제 (D-3 → D-4 경계)
