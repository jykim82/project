# 2026-07-16 세션 대화 로그

## IForest P1.5 — 알람-일치율(weak-label) 평가 (사용자: "현재 실제 데이터가 올라오고 있는데?" → "응")

### 배경
대기 3항목(벡터 임계값/few-shot/IForest P2)의 착수 시점 질문 → 실측(피드백 4건,
현장 판정 0건)으로 "실운영 투입이 트리거"라 답변. 사용자가 "실데이터는 올라오고
있다"고 지적 → 구분 정리(센서·알람 실데이터 ✅ / 사람 판정 ❌) 후, 알람 이벤트를
약한 레이블로 쓰는 **P1.5 평가는 지금 가능**으로 재검토·승인.

### 변경
- Migration 0098: tb_iforest_model_run 에 alarm_* 5컬럼
- `anomaly_iforest._evaluate_alarm_agreement`: 7일 아날로그 알람(수위/압력/유량)
  이벤트 ±60분을 Tier-2 모델로 판정 (경보 태그 → 시설+카테고리 아날로그 매핑)
- 학습 플로우 통합(실패 무해) + persist 확장 + admin API + UI KPI 행
  (/admin/model-eval?model=iforest)

### 첫 실측
- 이벤트 84/156 평가, **recall(proxy) 46.4%, lift ×1.48** (알람 구간 이상률이
  평상시 1.48배 — 모델이 실제 알람과 정렬)
- 스모크 16/16, UI 표출 확인. 한계 명시: 알람 오탐 포함 → 정밀도 아님, P2 승격
  조건은 review-items 트리거 절.

## 시계열 파운데이션 모델 도입 (사용자: "응 하는데 중국 모델은 제외해야되")

### 배경
"AI 알고리즘 새로 넣을 요소/새 모델?" 질문 → 인벤토리 진단 후 1순위로 향후
전망 교체 제안 (선형회귀가 최약 고리 — 7월 급락 문제의 근본 원인). 중국 모델
제외 조건 → Chronos(Amazon)/TimesFM(Google)만 후보 (영향 없음).

### PoC 백테스트 (8태그×10컷, 클램프 동일 적용)
- 선형(현행) MAE 1.2047 → **Chronos-Bolt 0.6391 (+46.9%)** / TimesFM 0.6263 (+48.0%)
- 패턴 신호 +38~81%, 추론 Chronos 24ms vs TimesFM 148ms → **Chronos-Bolt 선정**(사용자)

### 통합 (`slm@…`)
- trend_forecast.py (lazy 싱글턴·로컬 웨이트 783MB git 제외·선형 폴백)
- trend_comparison: median 교체 + 10/90% 밴드 응답 포함, 클램프·임계 스캔 유지
- 검증: method=chronos_bolt 확인, 웜 2.8s, 스모크 16/16, UI에서 사이클 위상
  추종(하강→재상승 곡선) 확인 — "내려갔다 올라갔다" 요구 충족

## 전망 불확실성 밴드 렌더 + 3회 테스트 (사용자: "응 진행해주고 테스트 3회 수행")

- comparison-overlay.ts(채팅·트렌드·GIS 공용 빌더)에 forecast band_upper/lower
  음영(ECharts stack) + chronos_bolt 라벨. 타입 확장.
- 3회 테스트: ① 채팅 남산 수위 ② 채팅 갈산 압력(다중 태그) ③ 트렌드 페이지
  남산 수위 — 3곳 모두 주황 예측 곡선 주위 10~90% 음영 렌더 확인
- 회귀: 백엔드 스모크 16/16 + Playwright e2e 5/5

## 음성 입력 (로컬 Whisper STT) — 완료 (사용자: EPANET 후순위 결정과 함께 승인)

- EPANET 활용 메뉴: **정말 후순위, 할지 안 할지 미정** (사용자 결정 — roadmap_status 메모리 기록)
- 구현: endpoints/stt.py(faster-whisper large-v3-turbo CPU int8, 도메인 프롬프트)
  + use-voice-input 훅 + ChatInput 마이크 버튼. 웨이트 1.5GB 로컬 번들(git 제외).
- 검증: 한국어 3발화 정확(탁도계 오인식 → 프롬프트로 해소), 전체 경로(브라우저→
  프록시→Whisper) 200, 인증 게이트 확인, 스모크 16/16
- 부수: watch-frontend-prod.sh '빌드 중 변경 유실' 버그 실재현·수정

## 장애 조치 보고서 목록 상단 통계 (사용자: "통계도 상단에 배치해서 구성하도록 검토해줘")

- 구성안 2안 제시 → A안(KPI 4카드+분류 칩+기간 필터) 선택
- GET /reports/stats + ReportStatsHeader + 목록 페이지 배치 (fault_action 전용)
- 검증: 전체 기간 실데이터 일치(10건/17항목/17.6%/1.9h), 기간 전환, 스모크 16/16

## resolved_at 기록률 개선 (사용자: "1")

- 원인 추적: 시각만 없는 항목 14건 = 전부 미완료 task 연결 — 조치 내용을
  보고서에 먼저 쓰고 task 완료가 늦는 흐름에서 시각 영구 누락
- 해결: task 완료 2경로에 초안 보고서 항목 역전파 (`slm@HEAD`). 확정본 불변,
  기존 내용 보존, 시각 지어내지 않음(백필 없음 — task 완료 시 자동 회복)
- 검증: direct resolve E2E(항목 시각 채워짐) 후 dev 데이터 원복, 스모크 16/16

## VLM 게이지 판독 PoC — 1차 실측 (진행 중, 사용자 "1 진행")

- 합성 게이지 5종(정답 통제: 아날로그 압력계 3 + 디지털 표시 2) 생성 (tmp/gauge_*.png)
- gemma4 비전 판독: **4/4 정확** (MPa 1.6→1.6, 디지털 3.42/127.8 완벽,
  아날로그 7.5→7.3 오차 2.7%), 판독 2~11s. 1건은 첫 요청 타임아웃(콜드) — 재측 필요
- 다음: 타임아웃 건 재측 + 실물 사진 테스트 + vision_agent 통합 설계

## 대기 요청 (사용자, 순차 처리 예정)
1. 음성 입력 UX: 종료 버튼 없이 자동 인식 종료 (무음 감지 VAD 자동 정지)
2. 설비 건강성 개요: "어디의 어떤 설비를 교체해야 하는지" 직관 표현 검토

## 음성 입력 자동 종료 (사용자 피드백)

- 요청: "종료버튼까지 눌러야하는거면 좀 이상한데" — 현장 취지 위배
- 조치: use-voice-input.ts 에 AnalyserNode RMS VAD — 발화 후 무음 2초
  자동 정지·전사, 무발화 8초 취소, 수동 정지 폴백. 사양 §자동 종료 추가

## 게이지 PoC a1 재측 — 5/5 값 정확 확정
- a1 타임아웃 원인 = gemma4 JSON 반복 생성 루프 (num_predict 캡으로 해결,
  value 4.2 정확). 최종: 아날로그 3/3 (최대 오차 2.7%), 디지털 2/2 완벽

## 설비 교체 우선순위 Top 5 카드 (사용자: "교체 대상이 직관적으로 안 보여")

- 개요 탭 최상단 카드: 3신호 융합(내용연수 +3/+1.5, MTBF<30일 +3/<90일 +1.5,
  재발 지속 +3) → 장소+설비+레벨+사유 배지 랭킹. 행 클릭 → 상세 탭 이동
- 백엔드 `slm/endpoints/replacement_priority.py` (신규, P5-rev equipment_status
  직접 호출로 판정 이원화 방지) / 프런트 ReplacementPriorityCard + controlled Tabs
- 사양 docs/equipment-health-priority-spec.md + CLAUDE.md 인벤토리 등록
- Playwright 검증: 카드 렌더 + 행 클릭 탭 이동 정상 (현재 dev 데이터는
  MTBF 신호 2건 — 내용연수는 commissioned_at no_data 293건이라 0, 입력 시 자동 반영)
