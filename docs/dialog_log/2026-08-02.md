# 2026-08-02 세션 로그

## 비전 입력 P2-a — 현장 모드 명판 스캔 진입

**사용자 요청**: "p2 진행해줘".
- `FieldNameplateSheet` 신설 — 현장 모드 시설 선택 작업에 "명판 스캔"
  액션. 시설 선택 → 설비 목록(기존 fetchEquipments) → NameplateScanDialog
  재사용
- 검증: 모바일 뷰포트(390px) 전 체인 — 신평 선택 → 명판 스캔 → plc_3
  선택 → 스캔 다이얼로그. 콘솔 0, tsc 0. 계기판(P2-b)은 실물 사진 확보 후

## 네트워크 계층 통일 (2026-08-03)

**사용자 요청**: "구축 네트워크 다이어그램이 모니터링과 구조가 다름 —
센터/유량계/가압장/배수지/정수장/감압설비/수압계실 구분 검토" → 권고
승인.

- 진단: 같은 데이터를 구축=자유 캔버스 / 모니터링=시설유형 5계층으로
  다르게 배치. 소블록 계열(119대)이 제안 목록에 누락, 정수장·감압설비는
  통신 장비 0대
- `network-layers.ts` 공용 단일 소스 신설 — 계층 선언(정수장·감압설비
  포함) + 동적 생성(빈 밴드 없음·미선언 유형 자동 밴드). 모니터링
  계층형·에디터 양쪽 소비
- 에디터 "계층 정렬" + 초기 기본 배치 계층화. 검증: 295대 재정렬 y 순서
  CORE→가압장→배수지→소블록→계실 확인, /network 회귀 없음(831 노드),
  콘솔 0, tsc 0. 기존 수동 배치는 tmp_canvas_layout_backup 보존

## 배경지도 출처 논의 (2026-08-03)

**사용자 질의**: 지도 데이터 출처(OSM/Protomaps 확인) → VWorld 정확도
비교 → QGIS 추출 가능성 → 공공 납품 무상 제공 맥락.

- 정리: 한국 정확도는 VWorld 우위이나 약관상 타일 내장·재배포 금지 —
  단, 공공 납품은 **발주처 명의 무상 제공** 경로가 정당 (권한 주체=발주처)
- 방침: 제품 기본 번들 OSM 유지 + 실 구축 시 발주처 데이터 → QGIS →
  MBTiles → pmtiles → 관리 UI 교체. offline-map-bundle.md 에 절차 문서화

## VWorld 변환 프로그램 — 계획 등재 (2026-08-03)

**사용자**: VWorld 작업은 재요청 시 진행. 별도 변환 프로그램을 계획에
등재 지시 → offline-map-bundle.md 에 CLI 사양(한도 관리·이어받기·
MBTiles→pmtiles) + 래스터 소스 지원 필요 항목 기록. 착수는 보류.
