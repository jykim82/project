# 2026-04-20 대화 로그

## 주요 요청 · 변경 요지

### 1. 멀티턴 follow-up 확장 (F1-ext)
- 사용자: "멀티턴은 10자 이내만 되나?" — 기존 10자 제한이 너무 좁다는 지적
- 추천 방향 협의 → **Option 2 (이어말 표지 + 20자 상한)** 채택
- 구현 (`slm@7705d59`):
  - `FOLLOWUP_MARKERS` 상수 신설 (그럼/그러면/같은/다른/어제/오늘/내일/이번/지난/최근)
  - `is_short_followup`: `<10자` OR `<20자 + 표지 포함` 복합 조건
  - "도" 조사는 "상수도"/"수도" 등 수도 도메인 용어와 형태 충돌로 제외
  - 빈 문자열 가드 추가
- 검증:
  - 유닛 테스트 15 케이스 중 14 통과 (1 실패는 "다른"의 본질적 중의성, 허용 한계)
  - E2E 20 케이스 (1턴 실존 시설명 → 2턴 다양한 follow-up) — **로직 기준 20/20 통과**
  - 전처리 "일주일→7일" 로 로그 문자열이 바뀌어 표면상 19/20 표시, 실제 상속 성공
- 관련 문서: `docs/review-items.md` §멀티턴 (a-ext) 섹션 추가

### 2. E2E 회귀 검증 — P5-rev/P6/P7/P8/F1-ext 통합 시나리오
- 사용자: "이어서 할일은?" → 추천 순서대로 #1 회귀 검증 진행
- Playwright MCP 로 5 경로 통합 검증:

| # | 시나리오 | 결과 | 스크린샷 |
|---|---------|-----|---------|
| A | 채팅 "대호 가압장 LTE 모뎀 통신이상 고장 등록" → fault_category 자동 "이상" 강제 (정책) → task#31 저장 | ✓ | `regression-A-fault-registration.png` |
| B | 채팅 "석문 가압장 유량 트렌드" (성공) → "그럼 지난 주 것도" (10자+표지) → **[SSE] 짧은 follow-up 상속: intent=FACILITY_TREND** 로그 확인 | ✓ | — |
| C | /monitoring/equipment-health → 교체/점검 KPI 클릭 → TaskListDialog → task#29 violet "LS · XGB-XBCH · 20240419-..." 노출 | ✓ | `regression-C-kpi-replace-dialog.png` |
| D | 설비 상세 "죽동 배수지 네트워크" 행 클릭 → EquipmentTimelineDialog → 알람 3,844건 시계열 병합 표시 | ✓ | `regression-D-equipment-timeline.png` |
| E | 진행중 KPI 클릭 → 신규 task#31 최상단 + task#29 violet 복합 표시 | ✓ | `regression-E-in-progress-task31.png` |

- 회귀 이슈 **없음** — 최근 3일간 대규모 변경(fault_category 정책/교체 메타/조치 사진/멀티턴)이 상호 충돌 없이 동작 확인

## 변경 파일
- `slm/session_manager.py` — FOLLOWUP_MARKERS + _has_followup_signal + is_short_followup 확장
- `docs/review-items.md` — (a-ext) 결과 추가, 히스토리 줄 추가
- `screenshots/regression-*.png` — E2E 회귀 증빙 5건 (로컬, .gitignore 제외)

### 3. 인과 규칙 할 일 정리
- work-history.md "즉시 진행 가능 #1 (가압장→소블록 cross-facility 규칙 추가)"
  을 진행하려다 `_CROSS_RULES[0]` 에 이미 구현됨을 확인 — 사용자도 "이미 완료"
  확인. work-history 에서 삭제, 2026-04-20 정리 주석 추가
- task #124 철회 (deleted)

### 4. 인과 규칙 엔진 고도화 (#2) 도 같은 패턴 — 이미 완료
- 사용자: "그것도 했는지 검토" + "완료한 항목은 항상 기록"
- 5개 하위 항목 (선행조건/안전연동/역방향/AND/다중홉) 전부 이미 구현됨 확인:
  - `_check_prerequisite` + `PREREQUISITE_FAILED` (anomaly_detector.py:1154)
  - `verify_safety_interlocks` + `SAFETY_INTERLOCK_VIOLATED`
  - 역방향 추적 (anomaly_detector.py:1319)
  - `verify_and_conditions` + `AND_CONDITION_VIOLATED` (anomaly_detector.py:1816)
  - 다홉 전파 (work-history 완료 이력 기재됨)
- work-history.md 의 "즉시 진행 가능" 섹션 비움 + 완료 이력에 2줄 추가
- task #125 deleted

### 5. Rule 9: 전체 네트워크 다운 시 UTM·관제 호스트 두 원인 병기
- 사용자: "호스트 PC, UTM 둘 다 원인일 수도 있자나"
- `network_upstream_explain.py` `_fallback_summary` + LLM 프롬프트에
  Rule 9 추가 — `down_utm==total_utm` AND `global_lte_down_pct>=90`
  이면 UTM 장애 / 관제 호스트 네트워크 문제 가능성 **병기**
- `allowed_numbers` 에 90.0 상수 추가 (할루시네이션 whitelist)
- E2E 3회 호출 안정성 검증 · 커밋 `slm@3ea6d02`

### 6. 채팅 응답 디자인 일관화 (대량 작업) — 27 인텐트 전부
- 사용자: "일반현황과 운영·압력 현황의 디자인 차이, 가독성 일관"
  → 일반현황 벤치마크(아이콘+라벨+값+pill) 로 통일 방향 합의
- 원칙: **내용(수치·고유명·시각) 100% 보존**, 조사·어미만 압축
  (`memory/feedback_preserve_answer_content.md` 신설)
- 단계별 변환 (각 커밋):
  - 압력현황 → 시각 복원 (`slm@ba632d1` + `slm@f532648`)
  - 가압장/배수지 일반·운영 4종 (`slm@85e5cf6/f11df0f/e282faf`)
  - 수위·알람 + 빌더 2종 (`slm@b4dca86`)
  - 소블록/감압변/주소 8종 (`slm@c06d565`)
  - 네트워크 통신 상태·구성 2종 (`slm@95e47c3`)
  - 평균사용량·야간최소유량 3종 (`slm@b7bc927`)
  - 적산·유출·공급시간·태그최신 4종 (`slm@eeb1573`)
  - 분석형 5종 마커→pill 승격 (`slm@2708600` + `slm-dashboard@60acbe0`
    bullet 분기 pill 렌더 추가)
- 부가 개선:
  - 숫자+단위 cyan regex 대폭 확장 (m³/h, kgf/㎠ 등 복합 단위,
    `slm-dashboard@5a76810`)
  - 분석형 응답 주 텍스트 foreground 색 (muted 회색 탈피,
    `slm-dashboard@21bf706`)
  - DetailLeaderRow 라벨 폭 `w-20 → min-w-24 max-w-48 break-keep`
    (`slm-dashboard@9444c02`)
  - 디자인 감사 25건 → 7건 (실질 해결) (`slm@b62dfe5`)
- E2E 감사: 일반/운영/압력/수위/알람 **5×4 = 20 테스트 100% compliant**

### 7. 네트워크 경로 일방향 경계 모델링 (migration 0052)
- 사용자: "업무망은 일방향 장비 거쳐가는데 사실상 네트워크 체킹 불가"
  → 잘못된 직결 링크 + 일방향 flag 모델링
- `tb_network_link.bidirectional BOOLEAN` 신설. 망간자료 전송장치
  포함·제어→업무망 경계 링크 `false`. 잘못된 `제어망 UTM → 업무망
  장비` 직결 3건 삭제
- view `v_network_path_trace_stop_local_with_status` 재귀 CTE 에
  `bidirectional=true` 필터 추가 — 단방향 이후 경로 자동 제외
- `build_network_status_block` 말미에 "이후 업무망 구간 — 일방향
  경계로 역방향 모니터링 불가" 안내 (`slm@6ff847b`)

### 8. 사진 첨부 기능 종합 점검 + 버그 수정
- 4 시나리오 테스트:
  - 1a 사진만 → IntentClarifyCard ✓
  - 1a → 메인 채팅 입력 → **photo_urls 유실** ✗ (DB photos=0)
  - 2 사진+텍스트 ✓ (DB photos=1)
  - 3 등록 후 사진 추가 (API·UI 존재, 미테스트)
  - 4 조치 완료 사진 ✓ (P8 기존)
- **버그 수정** (`slm-dashboard@d7a0673`):
  - `chat-store.pendingPhotoUrls` state 신설
  - IntentClarifyCard 마운트 시 store 보존, 전송·unmount 시 clear
  - `executeFaultDraft` 가 store 에서 photo_urls 가져와 draft 전달
  - E2E: task#34 난지마을 PLC · `photo_urls=1` 저장 확인
- **UI 추가** (`slm-dashboard@b527478`): TaskListDialog +
  EquipmentTimelineDialog 에 `photo_urls` 썸네일 렌더

### 9. 설비 건강성 대개편
- **장애·조치 이력 탭 신설** (`slm-dashboard@5a29192`)
  - 전체 이력 리스트 + 상태/분류/기간/키워드 필터
  - 분류별 분포 바 차트
  - `PhotoLightbox` 신규 (전체 화면, ←/→ 네비, ESC 닫기)
- **HH/LL 운영 이벤트 제외** (`slm@81c8005`) — 교체 후보 분석이
  수위계 HH 알람을 설비 교체 신호로 오탐하지 않도록. 분석 대상 13건
  → 5건 (실질 센서·설비 이상만)
- **내용연수 기반 교체 권고** (migration 0053 + endpoint +
  섹션 `slm-dashboard@c614e96`)
  - 기준 테이블 `tb_equipment_lifespan`: 통신 7년/보안 5년/서버 5년/
    유량계 8년/펌프 12년 + 국세청 연수 참고 + EOL 노트
  - 매핑 `tb_equipment_category_map`: 27 equipmenttype → 5 카테고리
  - UI: 카테고리 카드 + 상태 요약 필터 5개 + 설치일 입력 필터 +
    설비 리스트 (최대 200건)
  - 현 상태: 295개 설비 중 293 "설치일 미입력" — 데이터 입력 후 활성
- **4탭 구조 개편** (`slm-dashboard@7f43dd2`) — 스크롤 과다 해소
  - 개요 / 장애·조치 이력 / 내용연수 교체 권고 / 설비 교체 후보 분석

### 10. 메뉴 트리 개편 (migration 0054)
- 사용자 요청: 배수지/가압장/블록을 **트렌드** 하위로, 기존 트렌드는
  "사용자 트렌드" 로 이름 변경 + 하위로 편입
- DB + 정적 fallback 양쪽 동기화 (`slm-dashboard@617a132`)
- `tb_auth_menu` 에 M004-USR 권한을 M004 권한자 전원 자동 부여
- URL 경로 100% 유지 → 기존 즐겨찾기·링크 호환

## 커밋 시퀀스 (주요)

**web**: `6527275` → `4666b62` → `a91f685` → `ced9b5e` → `1b6115e`
  → `80bc7ac` → `de57193` → `412dc41` → `4ceb70e` → `431d9dd`
  → `61796d3` → `c4d287d` → `cbc8741` → `96129ca`

**slm**: `29420a9` → `7705d59` → `3ea6d02` → `ba632d1` → `f532648`
  → `85e5cf6` → `f11df0f` → `e282faf` → `b4dca86` → `c06d565`
  → `95e47c3` → `b7bc927` → `eeb1573` → `2708600` → `b62dfe5`
  → `6ff847b` → `81c8005` → `24bd2f9`

**slm-dashboard**: `60acbe0` → `5a76810` → `21bf706` → `9444c02`
  → `d7a0673` → `b527478` → `5a29192` → `48dd520` → `c614e96`
  → `c03fe14` → `7f43dd2` → `d6e3ad6` → `617a132`

## 설치일 데이터 상태

- **미조사** (2026-04-20 기준) — 295 설비 전부 `commissioned_at=NULL`
- 내용연수 교체 권고 기능은 구조만 준비 완료 (엔드포인트·UI 활성).
  데이터 입력 시 자동으로 overdue/approaching/normal 분류 활성화.
- 설치일 입력 UI 는 후속 작업(미착수)
