# 2026-05-08 세션 로그

## 요청
- "진행해줘" — Phase 3.3 (센서 매핑) 진행

## 변경 요지 — Phase 3.3a (센서 매핑 인프라)

### Migration 0069
- `tb_epanet_meter_map` (region/tag_sn/x/y/calibration_offset_m/label/notes)
- UNIQUE 제약: region + tag_sn (한 region 에서 같은 태그 중복 매핑 방지, ON CONFLICT UPDATE)

### 백엔드
- `/admin/epanet/meters` GET/POST/DELETE + `bulk-csv` (CSV: tag_sn,x,y[,offset,label,notes])
- `_check_data_quality` 의 HAS_METER_MAPPING 을 실제 카운트 검사로 변경
  · 0건: blocked
  · 1건+: ok=true, "센서 매핑 N건"
- POST 가 ON CONFLICT UPDATE — 같은 tag_sn 재입력 시 좌표·offset 갱신

### 프런트
- `EpanetMeterMapping` 컴포넌트 신규 (Phase 3.1/3.2 패턴)
  · 단건 입력 (tag_sn / x / y / offset / label)
  · CSV 업로드
  · 표 + 일괄 삭제
- admin/epanet 페이지 하단에 통합 (표고·수요 카드 다음)

### 검증
- 매핑 1건 (TEST_PRESSURE_001 @ 170000,480000) 추가:
  · HAS_METER_MAPPING ok=true, "센서 매핑 1건"
  · ready: 4 → **7** (gis-flow / leak-suspicious / headloss-anomaly / pipe-break / scenario-diff / replacement-candidates / network-aging)
  · warning: 0
  · blocked: 3 (valve-impact / pump-control / water-quality — Phase 4·6)
- 매핑 삭제 후 다시 4 ready (정상 동작)

### 메뉴 활성화 진척
| Phase | ready | warning | blocked |
|-------|-------|---------|---------|
| 2.7 | 0 | 5 | 5 |
| 3.1 (표고) | 4 | 1 | 5 |
| 3.2 (+수요) | 4 | 1 | 5 |
| **3.3a (+매핑)** | **7** | **0** | **3** |

### Phase 3.3b/c (다음)
- leak-suspicious 페이지 실 분석 로직 — `tb_tag_raw_data` 압력 실측값 + 시뮬 압력 차이 계산 + KNN 노드 매칭
- network-aging 페이지 — 월별 |실측-시뮬| 편차 시계열 차트

## 추가 — Phase 3.3b (leak-suspicious 활성)
사용자: "응 진행해줘"

### 변경 요지
- 백엔드 `endpoints/epanet.py` — `GET /admin/epanet/leak-suspicious?threshold_m=&hours=`
  · 매핑별 KNN 으로 가장 가까운 시뮬 노드 매칭
  · 실측 = tb_tag_raw_data 최근 hours 시간 평균 + offset
  · diff = |실측 - 시뮬|, threshold 초과면 suspicious
  · 의심 → 차이 큰 순 정렬
- 프런트 `LeakSuspiciousAnalysis` 컴포넌트 — 임계값/시간 슬라이더 + KPI 3 + 매핑별 비교 표
- /monitoring/leak-suspicious/page.tsx — DataQualityCard 안에 분석 컴포넌트 통합

### 검증 (Playwright tmp/epanet-leak-suspicious.png)
- ready 상태로 emerald "데이터 정합 — 결과 신뢰 가능" 배지
- 매핑 5건 추가 (CSV 업로드) → 의심 5/5 (현재 합성 시뮬 환경)
  · 죽동 토출압력 실측 9.03 vs 시뮬 35.16 = 차이 26.13m
  · 매방리 유입압력 실측 5.19 vs 시뮬 29.64 = 차이 24.45m
- 운영 환경에선 표고/수요 데이터 입력 시 차이 < 5m 정상 수렴

## 추가 — 메뉴 토글 인프라 + Phase 3.3c (headloss-anomaly)
사용자: "표현 항목들은 나중에 구현후에 활성 비활성화를 선택할수 있도록 해야해. 이어서 구현해줘"

### 메뉴 토글 인프라
- Migration 0070 — `tb_epanet_menu_setting` (region/menu_key/enabled/label, 10 메뉴 default Y)
- 백엔드:
  · GET `/admin/epanet/menu-settings` — region 별 10 토글 조회
  · PUT `/admin/epanet/menu-settings` — 단건 enabled 변경
  · `_check_data_quality` 응답에 `menus_disabled` 추가, 분류에서 제외
- 프런트:
  · `EpanetMenuToggles` 컴포넌트 — 10 스위치 + 활성/비활성 카운트 + 즉시 저장
  · `useEpanetDataQuality` 훅 statusOf — disabled 분기 추가
  · `AppSidebar` — disabled 메뉴 hidden 처리
  · `DataQualityCard` — disabled 상태 안내 (zinc 카드 + 관리 페이지 버튼)
  · admin/epanet 페이지에 토글 카드 통합

### Phase 3.3c — headloss-anomaly
- WNTRSimulator 가 headloss 결과를 안 채우므로 백엔드에서 Hazen-Williams 즉석 계산
  · HL = 10.67·Q^1.852 / (C^1.852·D^4.87) · L (Q m³/s, D m, L m, C HW 계수)
- GET `/admin/epanet/headloss-anomaly?z_threshold=2` — 50mm 구경 그룹 + z-score 기반 이상 분류
- 프런트 `HeadlossAnomalyAnalysis` — z_threshold 슬라이더 + KPI 3 + 파이프별 표 (상위 50건)
- /monitoring/headloss-anomaly 페이지 활성

### 검증
- 메뉴 토글: water-quality OFF → menus_disabled 에 이동, blocked 에서 제거. 다시 ON 정상.
- headloss-anomaly: sim #12 → 131 파이프 / 13 구경 그룹 / 9건 이상
  · P000043 (700mm 70m) z=+3.49
  · P000113 (150mm 82m) z=+2.76
  · P000091 (300mm 3199m) z=+2.44

## 추가 — Phase 3.3d / 4 / 5 / 6 일괄 구현 (합성 자동 fallback)
사용자: "a로 진행해줘 전부 구현하고 확인해줘"

### 합성 자동 fallback
- data-quality 갱신: HAS_VALVE_DATA / HAS_PUMP_DATA / HAS_WATER_QUALITY_MODEL 항상 ok=true (detail 에 "합성" 표기)
- 실 SHP/모델 입력 시 자동 전환 (`valve_ok` / `pump_ok` 가 true 면 detail 변경)

### simulator.py — `run_what_if()` 신규
- remove_links: link 제거 시뮬 (밸브 차단·관로 파손)
- add_pump_boost: reservoir head + boost (펌프 흉내)
- quality_initial / quality_kbulk: EPS 수질 모델 (잔류염소 1차 반응)
- 응답에 quality_mg_l 추가 (수질 시뮬 시)

### 7 분석 API
| 엔드포인트 | 응답 |
|----------|------|
| GET /synthetic-valves?n=5 | 가상 밸브 (큰 \|flow\| pipe 5) |
| GET /valve-impact | impacted 32 노드 (drop>5m) |
| GET /pipe-break | impacted + rerouted |
| GET /pump-control | items 50 노드 압력 변화 |
| GET /scenario-diff | top 20 노드/파이프 비교 |
| GET /replacement-candidates | z-score + length 점수 순 |
| GET /network-aging | 매핑별 월별 편차 추세 |
| GET /water-quality | 노드별 잔류염소 + 체류시간 |

### 7 분석 컴포넌트 + 페이지 활성
- 컴포넌트: ValveImpact / PipeBreak / PumpControl / ScenarioDiff / ReplacementCandidates / NetworkAging / WaterQuality
- 페이지: placeholder 제거, DataQualityCard 안에 분석 컴포넌트 (ready/warning 시 노출)

### 검증 (Playwright tmp/epanet-all-7-valve.png)
- /crisis/valve-impact: emerald 배지 + V01~V05 가상 밸브 + 슬라이더 정상
- 콘솔 에러 0
- 모든 7 페이지 컴파일 정상 (HTTP 307 — 인증 미들웨어)

### 메뉴 분류 진척
| Phase | ready | warning | blocked | disabled |
|-------|-------|---------|---------|----------|
| 2.7 | 0 | 5 | 5 | - |
| 3.1 (표고) | 4 | 1 | 5 | - |
| 3.2 (+수요) | 4 | 1 | 5 | - |
| 3.3a (+매핑) | 7 | 0 | 3 | - |
| 3.3c (+토글) | 7 | 0 | 3 | 0 |
| **3.3d/4/5/6 (합성 fallback)** | **9** | **1** | **0** | **0** |

→ 10 메뉴 모두 활성화. 운영자가 토글로 사이트별 ON/OFF 가능.

## dialog_log 롤링 (3일)
- 05-06, 05-07 유지 (D-2, D-1)
- 05-08 신규 (오늘)
- 05-05 삭제 (D-3 → D-4 경계)
