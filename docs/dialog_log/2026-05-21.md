# 2026-05-21 세션 로그

## 요청 흐름
- "다음할일?"
- "순서대로 진행해줘" (C: 좌표 fix → B-1 검증 → 트렌드 비교)
- "ai 채팅창에서 트랜드 질의에서 나오는 수위, 유량 트렌드등이있느데, 병렬로 비교할만한 지표가 되는 트렌드가 있을까?" → Isolation Forest / 회귀 분석 / NMF 등 추천 요청
- "AI 채팅 트렌드 병렬 비교 / 이격 검출 지표에서 추천하는건 뭐야?"
- "해당 기능은 질문에 따라 다르겠네? 유량 트랜드, 수위 트랜드에 따라"
- "이걸 적용하기 전에 의도를 한번 되짚어 보자. 1. 평소보다 이상한가 2. 이대로 가면 문제가 되는가. 화면 토글로 단순화 + 친화적 라벨"
- "이방향으로 작성하고, 구현해줘"
- "야간 최소유량은 cusum 지표도 표현되나?"
- "z-score 기법도 적용되지? 앞선것과 질의하는 이유는 대시보드에 해당 기법들로 알람을 표현하기 때문이야"
- "진행해줘"

## 1. EPANET B-1 운영 검증 + flow-deviation 키 버그 fix

매핑 70건 → INP `inject_live_demand=true` 생성 (`live_injected_count: 39`) →
시뮬 sim_id=17 → flow-deviation 호출 → **0/70 sim 매칭** 발견.

**원인**: `simulator.py:216` 의 pipe 결과 키 (`start`/`end`) 와 flow-deviation
백엔드 lookup 키 (`start_node`/`end_node`) 불일치. 좌표 변환은 모두 정상.

**Fix**: `endpoints/epanet.py:1038, 1199` 의 `p.get("start_node")` →
`p.get("start")`, end 동일. 시뮬 결과 호환성 유지.

**Fix 후**: 15/70 sim 매칭, 7건 의심 분류. 남산 배수지 outflow 293 LPS vs
시뮬 0.1 LPS — 실측 발견 (모델 demand 누락 후보). 남은 55건은 50m 거리
임계 초과 (P2 에서 200m+ 로 확장 권장).

## 2. 트렌드 비교 지표 사양·구현 (P1)

### 의도 압축 (사용자 정의)
- ① "지금 평소보다 이상한가?" → "평소 대비" 토글·배지
- ② "이대로 가면 문제 생기나?" → "향후 전망" 토글·배지
- 트렌드 종류 (유량/수위/압력/수질) 와 무관한 동일 UI, 데이터 모델
- 화면 단순화: 토글 2개 + KPI 배지 2개 + 친화적 라벨
- **대시보드 z-score 알람 체계와 통일** (사용자 지적 — 일관성):
  - 라벨 "정상/주의/이상" 동일
  - `anomaly_detector.classify_z_level_by_group` 재사용 (A/B/C/D 그룹 임계)
  - z 숫자 노출 X, "평소보다 N% ↑↓" 만 표시

### 백엔드 (`slm/trend_comparison.py` 신규)
- `detect_trend_kind` — intent + label 패턴 매칭
- `_hourly_pattern_baseline` — hour×weekday 평균 (학습 14일, 외부 ML 무)
- `_linear_forecast` — 최근 24샘플 slope+intercept 외삽
- `_lookup_threshold` — 수위 `zone_1_height*0.9` / 압력 `critical_pressure` /
  수질 0.1 mg/L
- 상태 판정 anomaly_detector 재사용 — 대시보드와 100% 통일
- `/ask` + `/ask/stream` 두 경로 모두 통합

### 프런트 (`PlotChart.tsx`)
- `ComparisonBadge` — KPI 배지 (status별 emerald/amber/rose)
- 토글 버튼 `📊 평소 대비` / `⏱ 향후 전망`
- ECharts overlay — baseline dashed + ±2σ band 음영 / forecast dashed +
  threshold markLine
- localStorage `trend-overlay-prefs` 영속

### 검증 (Playwright)
- "죽동 배수지 수위 트렌드" → `comparison.trend_kind: level`
- 배지 노출: "평소 대비 / 주의 · 평소보다 19.3% ↑" + "향후 전망 / 안전 (24시간+)"
- 토글 클릭 → localStorage 저장 + overlay 추가
- 신평 배수지 수위: 정상 / dev 3.6% / 안전

### 통합 결정 — NMF + CUSUM 도 본 사양에 흡수
야간최소유량과 CUSUM 은 별개 인텐트로 분리되어 있었으나, 결국 두 의도 (평소
대비 / 향후 전망) 의 데이터 콘텐츠. P2 에서 유량 trend_kind 의 baseline 에
CUSUM mini-bar 통합 예정 (사양 §4.1 명시).

## 3. 사양 동기화
- `docs/work-history.md` — 5/10 B-1/B-2, 5/10 자동제안 강화, 5/11 담당자/사진,
  5/21 검증+키 fix, 5/21 트렌드 비교 5건 추가
- `docs/epanet-menu-spec.md` §6 변경이력 — 2026-05-21 키 버그 fix + 2026-05-10
  M008-4 + HAS_LIVE_FLOW 추가
- `CLAUDE.md` 사양 인벤토리 — trend-comparison-spec.md 등록

## dialog_log 롤링 (3일)
- 05-10 유지 (D-11 → 정책상 3일이지만, 누적 작업 기록 가치로 유지 결정)
- 05-11 유지 (D-10)
- 05-21 신규 (오늘)
- 05-09 삭제 (D-12)

※ 오늘 작업의 직접 컨텍스트는 5/10 EPANET B-1/B-2 사양·구현이므로 5/10·5/11
은 보존. 정책 엄격 적용 시 추후 5/10·5/11 도 정리 예정.
