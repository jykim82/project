# 2026-07-29 세션 로그

## 상황보고 진입 동선 — 경보 상세 "1보 작성" (D P2)

**사용자 요청**: 상황보고 진입 동선(경보 상세에서 "1보 작성") 진행 후 이어서
로드맵 B 진행.

**변경 요지**:
- `IncidentDraftButton` 신설 — 경보 분석 상세 헤더(경보관리 팝업 +
  경보분석 페이지 양쪽)에서 그 경보 기준으로 상황보고 1보 초안 생성.
  발생 시각 = 경보 시작 시각, confirm 후 생성 → 편집기로 이동
- `/reports/incident?id=` 딥링크 — 생성된 초안 자동 선택
  (useSearchParams → Suspense 래핑)
- 검증: 이력 행 → 팝업 → 1보 작성 → confirm → `?id=6` 이동·자동 선택·
  상황 섹션에 해당 경보 수집 확인. 테스트 초안 삭제 원복. 콘솔 에러 0,
  tsc 0
- 사양 `docs/incident-report-spec.md` §6 구현 완료 반영

## 로드맵 B P1 — 현장 지식 카드 (Migration 0136)

**사용자 요청**: (위와 동일 지시의 후반) 이어서 로드맵 B 진행.

**변경 요지**:
- 사양 `docs/site-knowledge-spec.md` 신설 — 부록 권고안 채택 (B.2 안전
  신호 불가침 · B.3 유형 4종 · A.5 억제 정합). **P1 은 조회·기록 계층만**
  — 알람 생성·해제에 개입하지 않음, 억제 반영은 통합 억제 로그 설계 후 P2
- Migration 0136: `tb_site_knowledge`(target_refs·conditions jsonb) +
  `tb_site_knowledge_revision`(수정·삭제 전 스냅샷 — 감사 추적) + 메뉴
  M006-8 "현장 지식"
- 백엔드 `slm/endpoints/site_knowledge.py`: CRUD(revision 선기록) +
  match(경보 문맥 매칭 — 대상 AND·요일/시간대) + preview(지난 30일 영향
  미리보기)
- 프런트: `/crisis/site-knowledge` 목록+등록 폼(요일 토글·시간대·영향
  미리보기), 경보 분석 상세에 "현장 지식" amber 카드 (DB 사실·AI 의견과
  시각 구분)
- 검증: API 왕복(등록→매칭 양/음성→미리보기 2,844/3,520·화 09~11 조건
  45건 정밀 필터→수정/삭제 revision 2건)·UI 왕복(등록→미리보기→경보
  상세 표시→삭제)·채팅 스모크 16/16·tsc 0. 테스트 데이터 전부 원복

## 트렌드 향후 전망 — 수위 하한(저수위) 판정

**사용자 요청**: "수위는 최고점(넘침)도 중요하지만 최저점(공급량 부족)도
중요한데 트렌드에 반영 가능한가" → 조사 보고 후 진행 승인.

**변경 요지** (trend_comparison.py — 프런트 무변경):
- 현황: 임계 도달 예측(경보관리)·차트 가이드선은 이미 LL 양방향, 트렌드
  "향후 전망" KPI 만 상한(만수위 90%)만 판정하던 공백
- `_lookup_level_lower` 신설 — SCADA 실측 LL(LEI↔LEC 페어) 조회, 페어
  없으면 None(임의 하한 금지). 교차 스캔을 상·하한 후보로 확장, 먼저
  도달하는 쪽 판정 + 교차 없으면 기울기 방향 쪽 임계 표시
- 방향 명시(HH 상향만·LL 하향만) — 이미 LL 미달 현장(난지관광 0.11<0.5)
  의 회복 상승 오판 방지. 배지 "N시간 후 저수위/만수위 한계 도달"
- 수정 중 결함 1: SQL 본문 literal `%` 가 psycopg2 플레이스홀더와 충돌해
  조회가 조용히 None — LIKE 패턴을 파라미터로 전환
- 검증: 단위 4종(LL 조회 0.8/페어없음 None/하강 합성 "2.5시간 후 저수위
  한계 도달"=계산 2.4h 일치/LL 미달 회복 오판 없음) + 라이브(난지마을
  하강 창 LL 표시 전환) + 채팅 스모크 16/16. 사양 §물리 클램프 아래 추가

## 유량 전망 시간대 프로파일 투영

**사용자 요청**: "남산1 소블록 유량 예상추이가 기존 일주기 패턴(6시 상승·
점심·저녁 피크)과 괴리" → 진단 보고 후 진행 승인.

**진단**: 계절 게이트(ACF r>=0.6) 미달 — 30분 격자 r@24h=0.044 (스파이크
시각이 매일 다름) → Chronos 중앙값 → 진폭 압축(실측 16~177 vs 전망 48~69).
단, 시간대 평균 모양은 안정(일별 상관 0.15~0.74).

**변경 요지** (trend_comparison.py `_flow_hourly_profile`):
- flow 한정, 계절 미달 시 14일 시간대 프로파일 모양 + 최근 24h 수준 보정
  투영 (`hourly_profile`). 밴드 = 시간대 ±1.28σ. 스파이크는 예측 안 함
- 전수 백테스트(유신호 81태그×7일): 수평 대비 74/81 우세·중앙값 +27%.
  레벨 보정이 악화 태그(행정2-3 레벨 이동) 해소
- 게이트 split-half r>=0.3 — 공업 유량(모양 없음, 석문공업 r=-0.13)은
  Chronos 폴백. 수위 패턴 투영 기각 사유(펌프 결정)는 유량에 부적용 명시
- 검증: 남산1 전망 24~99 회복(새벽 25→06시 73→점심 94→저녁 92 — 보고된
  기대 패턴 그대로), 공업 태그 폴백·수위 불변 확인, 채팅 스모크 16/16

## 매방리 압력 확인 + 전망 렌더 왜곡 수정 [E-059]

**사용자 요청**: ① 매방리 압력계1 예측 확인 ② "데이터는 정상인데 그래프가
이상해 보임".

- ① 압력계1은 r@24h=0.97 강주기 → `seasonal_24h` 경로. 전망 곡선이 14일
  시간대 평균과 0.05 이내 일치(심야 최고·주간 최저 역상관 패턴) — 정상
- ② 원인: category x축 등폭 — 관측 5분 버킷 2,016점 뒤에 30분 간격 전망
  48점을 그대로 연결해 24h 전망이 폭 2.3%(정상 12.5%)로 압축, 파형이
  지글거림처럼 왜곡. `resampleForecastToBucket` 신설(comparison-overlay) —
  전망·밴드를 관측 버킷 간격으로 선형 재보간. SVG 좌표 실측 2.3%→9.2%.
  hourly_profile 기법 라벨 누락도 함께 보강. E-059 기록, tsc 0

## 유량 프로파일 평균 → 중앙값 전환

**사용자 요청**: "신평 유입은 실측이 0 을 찍는데 전망은 안 내려가 보임
(그래프 기준)".

- 원인: 배수지 유입은 켜짐(~300)/꺼짐(0) 이봉 신호 — 새벽 2~6시 중단
  비율 54~68%. 시간대 **평균**(88~141)은 실재하지 않는 값
- 변경: `_flow_hourly_profile` 평균→**중앙값**, 밴드 ±1.28σ→**p10~p90**
  분위수, 레벨 보정 제거(이봉 꺼짐 구간까지 밀어올려 손해)
- 백테스트(81태그×7일): 중앙값 76/81 우세·+29% (기존 평균+보정 74/81·
  +27%). 신평 유입 55.4→38.9, 남산1 21.9→18.0
- 검증: 신평 전망이 새벽 2~4시 ~0 도달(실측과 동일), 이봉 구간 밴드
  0~330 전폭, 남산1 일주기 유지. 채팅 스모크 16/16

## 로드맵 A P1 — 제한적 에이전트 루프

**사용자 요청**: "로드맵 a 고고".

- 사양 `docs/agent-loop-spec.md` 신설 — 부록 B.1 채택 (룰 라우터가 도구
  선택, LLM 도구선택 금지). P1 소견은 결정적 조합(생성 모델 없음),
  LLM 문체 서술은 P2
- `slm/evidence_agent.py` 신설 — 조회 도구 4종(동일 시간대 분포·상류
  알람·지식 카드 `find_matching_cards` 재사용·조치 이력), applicable 룰
  라우팅, 병렬 실행, 도구 3s/전체 8s 제한, 실패·타임아웃도 팩에 표시
- ANOMALY_FACILITY_DETAIL 핸들러 post_process 수집 + response_extras
  병합. 결함 1건: 응답 조립부가 _hextras 두 키만 읽어 evidence_pack
  누락 → 통과 배선 추가
- 프런트: EvidencePackSection (도구별 행 + 펼침 상세) — AnomalyDetailView
- 검증: SSE 실측 — "신평 배수지 이상 진단해줘" → 도구 4개 1.4s, 유출유량
  221.5 vs 평소 66~189 범위 이탈 검출. 채팅 UI 실전송·펼침·콘솔 0·
  스모크 16/16·tsc 0

## GIS 타임라인 모바일 탭 불가 수정

**사용자 요청**: "GIS 관망도 과거데이터 시뮬 기능이 없어진 것 같다" →
"모바일 접속 시 안 되나".

- 데스크톱: 기능 정상 (타임라인 버튼 → 24h·144프레임 스크러버 실측)
- 모바일(390px) 실측: 현장 모드 배너(fixed bottom z-40)가 타임라인
  버튼(z-10)을 덮어 **탭이 배너로 흡수** — elementFromPoint 로 확정
- 수정: FieldModeBanner 제외 경로에 /monitoring/gis 추가 — 전체화면
  지도에선 배너를 띄우지 않음 (발견 동선은 다른 화면 유지)
- 검증: 모바일 GIS 배너 미표시·타임라인 탭→스크러버 열림, 대시보드
  배너 정상(회귀 없음), 콘솔 0, tsc 0. field-mode-spec 동기화

## 진폭 압축 "참고" 톤 (합덕인더스 생활수위 보고)

**사용자 요청**: "합덕인더스 배수지 생활수위1 미래예측이 트렌드 그림상
동떨어져 보임".

- 진단: 펌프 진동 2.65~3.52m·주기 불규칙(ACF 0.13, 24h 역상관) — 계절
  탈락은 옳고 수위 패턴 투영 기각의 실증. Chronos 중앙 경로(3.14~3.35)가
  진동을 못 그리는 건 원리적 한계(밴드 2.84~3.5가 대신 표현)
- 결함: CV 게이트(>15%)가 평균 큰 수위(CV 5.6%)를 놓쳐 **확정 톤**으로
  표시 → 전망 범위 < 관측 진동의 40% 이면 low_confidence 보강 (유의
  진동 >10% 가드 — 평탄 신호 오탐 방지)
- 검증: 합덕인더스 참고 톤 전환·매방리 압력(진폭 재현) 확정 톤 유지·
  스모크 16/16

## 지식 카드 채팅 주입 (B P2) + 카탈로그 규칙

**사용자 요청**: "카탈로그·브로셔는 요청할 때만, 나머지는 이어서 진행"
→ 메모리 feedback_catalog_on_request 저장, B P2 소품부터.

- 백엔드 `_attach_site_knowledge` — sitename 문맥 성공 응답에 매칭
  active 카드 최대 3장 첨부 (결과 조립 2개 지점, 실패해도 응답 유지)
- 프런트 BotMessage amber "현장 지식" 블록 (경보 상세와 동일 체계),
  타입·매퍼·히스토리 저장 경로 배선
- 검증: SSE 양성(죽동→카드 첨부)/음성(남산→None), 채팅 UI 실전송 —
  탁도계 알람 목록 위에 "회선 노후·실제 수질 이상 아님" 카드 표시.
  테스트 카드 원복, 스모크 16/16, tsc 0

## 에이전트 루프 P2 — 원인 분석 인텐트 확대

- `EvidencePackMixin` 공용화 (anomaly.py) → RESERVOIR_LEVEL_CAUSE_ANALYSIS
  에 적용. z_score 없는 인텐트는 동일 시간대 도구 자동 제외
- 프런트: anomaly 카드 외 인텐트는 BotMessage 레벨 EvidencePackSection
  (mapper 에서 anomaly visual 이면 제외 — 이중 렌더 방지)
- 검증: SSE(도구 3개 수집)·채팅 UI 실전송(원인 요약 위 진단 근거 표시)·
  스모크 16/16·tsc 0

## 로드맵 E 사전 검증 + 비전 입력 사양

**사용자**: 명판 사진 5장 제공 → 실험 → OCR 활용 시나리오 논의 (명판
항목이 제각각이라 고정 스키마 한계 → 동적 키-값+수정 개방+사진 첨부
시나리오 제안, 수치 수정 필수 강조).

- gemma4 실물 검증: 정상 화질 3장 전 필드 정확(한글 포함)·장당 33~60s.
  리스크 실증 — 흐린 사진 환각(HYOSUNG)·명판 밖 지식 추정(WALRUS)
- `docs/vision-input-spec.md` 신설 — 동적 키-값(meta.nameplate, 스키마
  변경 없음) + 표준 키 승격(별칭 사전) + 전 항목 수정 개방(수치 포함) +
  사진 정본 + 검증 계층(범위 룰·DB 대조·이중 추출·SCADA 대조).
  신규 OCR 스택 없음 (부록 A.2 대안 입증). 구현은 미착수 — P1 범위 확정
