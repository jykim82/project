# 2026-04-25 세션 로그

## 요청
- 장애 조치 보고서 + 일 점검 보고서 사양 검토 (구현 전 결정 항목 정리)
- 추천안 채택 → 사양 문서 + migration 초안 작성

## 변경 요지
- `docs/report-spec.md` 신규 — P1 사양 확정 (D1~D9 추천안 모두 채택)
  - tb_report / tb_report_item 신규 테이블 모델
  - tb_task_master.inspection_type 컬럼 추가 (일상/정기/특별)
  - 채팅 점검 인텐트 분기 (`일상점검`/`정기점검`/`특별점검` 키워드)
  - 보고서 CRUD + 인라인 편집 + 확정 잠금 + A4 인쇄 (2-up 사진 부록)
  - AI 요약: Ollama 로컬, 항목 추가 즉시, 원문 보존 + 재요약 버튼
  - 권한: 작성자 본인만 편집·확정 (P2 RBAC 확장 예정)
- `db/migrations/0058_report.sql` 신규 — 위 모델 + 인덱스 + 코멘트 + 롤백 절차 명시
- `CLAUDE.md` 인벤토리에 보고서 사양 한 줄 등록

## 추가 — 사양 v2 정합성 보강 (10항목)
plan mode 재검토 결과 6가지 정합성 문제 발견 → 사양·migration v2 로 보강:
- region 멀티테넌시 (tb_task_master 미보유 → tb_equipment_info 조인 검증)
- 사진 JSONB 객체 배열 (`{url, source: fault/action/user, caption, taken_at}`) — 전/후 구분 보존
- 시점 메타 5컬럼 캐시 (`site_name`/`facility_type`/`equipment_name`/`fault_category`/`inspection_type`)
- 후보 필터 단순화 (`task_category` 단독)
- `tb_menu` INSERT (M005/M005-1/M005-2) + MASTER/ADMIN 권한 부여
- `sidebar-menus.ts` 정적 fallback 형식 명시
- `slm/report_summarizer.py` 신규 모듈 (ollama_client.generate 재사용)
- 부분 unique index (같은 task 중복 추가 방지)
- 정렬 API (`PATCH /reports/{id}/items/reorder`)
- 인쇄 라이트모드 강제 + 권한별 응답 코드 (403/405/422)

## 후속
- P1 구현 진행 (사용자 위임): migration 적용 → 백엔드 API + 요약 모듈 → 프런트 페이지·채팅 분기 → Playwright 화면 검증 (시나리오 5회씩)
