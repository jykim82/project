# 2026-07-24

## 카탈로그·브로셔 HTML 판 제작 (사용자 "html로, 임의 현장명 화면 삽입까지")

- md 초안의 `[화면]` 자리를 실제 캡처 대신 **임의 현장명(청수시·한빛배수지·
  동림가압장·매화소블록 등) HTML/CSS·SVG 목업**으로 직접 구성 — 실 현장명
  비노출 원칙
- `docs/catalog-draft.html` (16p) — 표지 히어로 대시보드·사진 진단 채팅·
  교체 우선순위·트렌드 전망 밴드·계통도·GIS 확장카드·경보 분석 2패널·
  위기 모달·장애 통계·보고서·EPANET 히트맵·실측 편차·AI 채팅 스캔·
  메신저·현장 모드·SKU 3단 카드·아키텍처 다이어그램·모듈 토글 목업 포함
- `docs/brochure-draft.html` (6p) — 요약판, 화면 6분할 그리드
- 자립형 단일 파일 + A4 인쇄 CSS(@page) — 브라우저 인쇄로 PDF 제작 가능
- 헤드리스 Chrome 렌더링 검증 후 파일 전송. CLAUDE.md 인벤토리 갱신
# 2026-07-24

## SCAN_ALL 성능 개선 — 선택지 a 구현 (사용자 "3 진행")

- Migration 0116: cagg_1h_raw_stats_ai (계층형 1h 사전집계 — nz 합·제곱합·
  건수로 5분 샘플 분산 보존, holding 판정 원본 동치. 85M→9.9M행 백필)
- SCAN_ALL SQL 재작성: 365일 통계만 1h 집계 사용, 최근 창(3h/1h/3d)은
  5분 유지. 실측 1,245초 → 40초 (31×)
- 전수 검증(289태그): 행 일치·등급 경계 교차 0·평균 차이 1건 후 교체
  (E-050 교훈 — 검증 선행). ANOMALY_FACILITY_DETAIL 은 태그 필터가 CTE
  초입이라 병목 아님 확인

## 품질 계층 P2 — 기준선 보호 + 판정 신뢰 게이트 (사용자 "이어서 진행")

- 기준선 학습(hourly_mean·GBT 프레임)에서 품질 불량 태그의 since 이후
  구간 제외 — 포화/고착 값의 오염 원천 차단 (복귀 시 자동 해제)
- comparison.quality_issue 전달 → ComparisonHeader(채팅·트렌드 공용)가
  판정 배지를 violet "계측 품질 {사유} · 판정 신뢰 불가"로 대체
- 검증: 송악2-1 압력(포화) 학습창 22:00<since / 신평 탁도(고착) 트렌드
  API 플래그 포함. tsc 0

## 회귀 검증 — 채팅 스모크 (규범: 인텐트 변경 후 필수)

- SCAN_ALL SQL 교체 + trend_comparison 품질 게이트 반영분 회귀 확인:
  16/16 통과 (8s). 이상 스캔·교차 검증·트렌드·야간최소유량 등 대표 인텐트
  구조 정상

## 품질 계층 P3 — 알람 게이트 + 점검 후보 (사용자 "추천안대로")

- 품질 이상 태그의 새 알람: 경고 모달·OS 알림 제외 → info toast "계측 품질
  의심" 완화 (알람 보존 — 자동 해제/억제 없음, 노출 단계 분리)
- 교체 우선순위 5번째 신호 quality_stuck (7일+ 지속 그룹, 계측기 점검 라벨)
- 검증: 삼화 통신이상 플래그 실동작, 신호 SQL 그룹 정상. 사양 3종 동기화

## 알람 임계 보유 현황 전수 조사 (사용자 "이어서 진행")

- 압력: 70곳 중 33곳 임계 전무 — **가압장 26곳 전원** (소블록 말단은 정비됨,
  성상1 사례의 일반형). 수위: 사실상 완비. 유량: 전면 부재 = 통계 감시
  소관 판정 (CUSUM·z-score·물수지)
- "설정압력"(제어 목표값)을 임계로 오인하는 함정 발견 → 정규식 매칭으로 구분
- docs/alarm-threshold-coverage.md 신설 (재조사 SQL·SCADA 보강 우선순위·
  셋업 검수 활용) + 인벤토리 등록. 실제 태그 신설은 SCADA/PLC 측 작업

## 구축 고도화 ① — DATAINFO 변환룰 (사용자 "datainfo 먼저")

- 검토: datadesc↔datainfo 2,700쌍 실측 — 동일 26%, 기본 12룰로 59% 재현
  → 룰 기반 자동화 타당 판정
- 구현: Migration 0117(tb_datainfo_rule+apply_log) + 4계층 엔진
  (regex/dict/context/override) + CRUD·미리보기·선별 적용(이력 롤백)·
  재현율 채점 API + /setup/datainfo-rules 페이지(M200-20, tb_menu·auth 등록)
- 시드 29룰 재현율 74.1%. E2E: 적용→이력→원복 사이클 + 화면 렌더 확인.
  원본 축약(RE 등) 유형은 diff 검토 큐 → override 룰 대상
- 다음: 구축 고도화 ② 계통도 엔지니어링화 (검토 완료 — 자동 레이아웃
  API화·diagram 편집기·정합 lint·CRUD 리로드 연결)

## 구축 고도화 ② — 계통도 엔지니어링화 (사용자 "응")

- relayout API (new_only=기존 배치 보존/full) + lint(누락·고아·순환·EPANET)
  + setup/diagram "노드 배치" 탭 (SVG 드래그 편집기, 정합 배지)
- flow-map CRUD→causal 부분 리로드 연결. 부수 fix: 리로드 함수 early-return
  이중 close (풀 'unkeyed connection')
- E2E: 관계 추가→lint 감지→자동 배치(상류 옆 삽입)→정합 통과 / 실마우스
  드래그 좌표 저장 확인 (합성 이벤트는 capture 한계로 미동작 — 실사용 무관)

## 삼화 DI 검증 → 통신두절 사유 구분 (사용자 "나머지 아날로그도 홀딩이야?")

- 사용자 의심 적중: 삼화(가)는 접점 반전이 아니라 실제 두절 — PLC ping
  매분 Timeout + 아날로그 3태그 30일 동결 (수집기가 stale 값 반복 송신)
- ping·아날로그 유동성 교차 판정 구현: 통신두절의심(복구 안내) vs
  DI상시ON의심(결선 점검). 재분류: 두절 8건(성북1리 배수지 신규 발견 —
  7태그 전면 동결) / 반전 24건
- 현장 조치 대상: 삼화 가압장·성북1리 배수지 통신망 복구 (30일+ 무감시)

## 데이터 정합 3종 + 구축 고도화 ③ (사용자 "추천순서대로")

- 정합 ①: 합덕일반 공업 수위 LL datainfo 오타 2건 정정 (3지→1지, 4지→2지
  — datadesc·알람 메시지 기준, apply_log 이력)
- 정합 ②: 블록 기초정보 미등록 7곳 등록(수청 계열·도비도·소난지도) +
  태그 sitename 오타 신펑1→신평1. facility_map 자동완성 반영 확인
- 정합 ③: ANOMALY_COMPARE tag_scale 적용 (E-051 계열 마감). 스모크 16/16
- 구축 고도화 ③: /setup/audit 검수 6종(기초정보/임계/datainfo/계통도/품질/
  EPANET) + M200-21 페이지 + delivery-checklist 인수 기준(warn 0) 연계.
  첫 실행에서 **가압장 기초정보 미등록 11곳 신규 검출**
