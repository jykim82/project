# 2026-06-29 세션 로그

## 수집 엔진 점검

### 사용자 요청
- "현재 수집엔진이 정상적으로 동작중인가? 데이터가 않쌓이는거같아"

### 결과
- 정상 확인: `slm-dev-tag-ingest` Up, 매분 "복제 완료", 최신 데이터 21초 전,
  24h 약 200만 행, 09~22시 매시간 2,700 태그 전량 적재. 태그당 ~2분 1회라
  단일 분에는 ~1,870 태그로 보일 뿐 1시간 누적 시 전량.
- 단 08:00 시간대만 2,490행/690태그(낮음) — 데몬 ~08:50 기동 정황. 현재 복구됨.

## 죽동 수위#1 HH 진단원인 오분류 분석

### 사용자 요청
- "죽동배수지 수위#1 HH 알람 진단이 '가동조건 설정 오류로 인한 펌프 미가동;
  배수지 유출량 감소추세로 유입량이 유출량보다 많음' — 2개 합쳐진 것 정상인지"
- → "가동조건 설정 오류가 문제인거같은데? HH이면 정상 멈춤인데"
- → "Node-RED 알람 파이프라인 알고리즘 분석·해결방안"
- → "가압장에 배수지 수위 연동 조건여부를 기록하도록 DB에 되어 있는데"

### 분석 결과
- `;` 합침은 버그 아님 — 랭킹 SQL `string_agg(DISTINCT diagnosed_cause)`. 한
  알람의 여러 진단원인을 모은 것(전 기간 수위#1 HH 5종).
- "가동조건 설정 오류로 인한 펌프 미가동"은 오분류. 인터록 사양(SI_RESERVOIR_01:
  "수위 HH → 상류 가압장 펌프 정지")상 HH 펌프 정지는 정상.
- **핵심**: `tb_service_booster_station_status.pump_control_mode`(23/23 기록)가
  "배수지 수위 제어 방식"(2) / "가압장 압력 제어 방식"(21)을 구분. **죽동 가압장은
  압력제어 + running_pump_count=1(실가동)** — "미가동"은 사실·제어모드 양쪽 오류.
- Node-RED 진단 파이프라인이 pump_control_mode·실가동을 무시하고 모든 가압장을
  배수지 수위 연동으로 가정.

## 건수 부기 (A안) — 랭킹 SQL

- `example3.json` 2개 인텐트(FACILITY_ALARM_TOP_COUNT / _CAUSE_DIAGNOSIS_RANK)
  의 diagnosed_causes 를 GROUP BY (alarm_msg, diagnosed_cause) 건수 집계 →
  `string_agg(… '건)' ORDER BY cause_count DESC)` 빈도순+건수 부기. alarm_count 보존.
- 검증: 죽동 수위#1 HH → "유출량 감소추세…(745건); 가동조건 설정 오류…(96건); …"
  총 850 일치. 빌더 무변경. (slm `d741338`)

## 제어모드 게이트 — 사양 + Node-RED 배포

- 사양 `docs/alarm-diagnosis-control-mode-spec.md` v1 작성 + CLAUDE.md 등록 (`d4bcfc4`).
- 배선 추적: "가동조건 설정 오류" 실제 생산 경로는 19단계 트리가 아니라
  펌프모드→가동조건 chain → **S2 writer(d5f8e543ad132d96)** 임을 확인(사양 §2 정정).
- **Phase B 1차 배포**: S2 writer UPDATE WHERE 에 fail-safe 게이트 SQL 추가 —
  전단 가압장이 압력제어 or 실가동이면 NOT EXISTS 로 "미가동" 오진 차단(매핑 없으면
  기존 동작 유지). 게이트 DB 검증(죽동 차단/행정·신평 유지), UPDATE 파싱(BEGIN/ROLLBACK),
  node-red 무중단 재기동 + Started flows + 노드 1040 보존 + 신규 에러 0.
- 백업: `dev-data/noderered-backups/flows.{live,patched}.20260629.json`(gitignore).
- 잔여: S1/P*/V*/원인10-11 확대, 임계 기반 계산, 경합 제거, 토폴로지 매핑 보강.
- (참고) node-red 로그의 "PLC통신 조회 ti.ip_address" 에러는 기존 문제(패치 무관).

## GIS 관망도 시설 메뉴 고도화 — 사양 기록 (1순위)

### 사용자 요청
- 향후 계획: GIS 관망도 시설 메뉴 고도화. (1) 시설정보 추가/삭제/수정(설치일자·
  제조사·제원; 펌프/유량계/수위계/PLC/통신장비 LTE·유선·DSU), (2) 계층=시설(배수지)
  →일반현황+설비들, (3) 고장이력·교체주기는 1차 이후 2차, (4) 사양작업 1순위.

### 결과
- `docs/gis-facility-menu-spec.md` v1 작성 + CLAUDE.md 등록.
- **기존 스키마 재사용 확인**: tb_equipment_info(commissioned_at=설치일자,
  meta.manufacturer/model=제조사/제원, 사진 URL), tb_equipment_lifespan(교체주기),
  tb_equipment_alarm_report/v_equipment_mtbf(고장이력), tb_service_reservoir_info
  (배수지 일반현황). → 신규 테이블 아닌 메뉴/CRUD/검증 계층 고도화로 정합.
- 1차(시설·설비 CRUD + GIS 인스펙터 연동 + 종류별 config 필드 스키마)/2차(고장이력·
  교체주기) 분리. 관련 사양(setup-phase 컬럼잠금·equipment-fault-tracking·gis_plan) 참조.

### 계층 구조 확정 (v1.1)
- 사용자: "계층구조는 배수지·가압장·블록 등 시설과 그 안의 펌프·밸브·수질·UPS 등
  설비로 계층화 하는걸 2번으로 생각하는거지?" → 확인.
- DB 실제 값 대조: 시설=배수지/가압장/소블록·소소블록(블록)/제어망, 설비=가압펌프·
  밸브(유입/유출/토출/송수/흡수정)·수위계·압력계·유량계·수질계·UPS·전원·침수센서·
  네트워크(통신). 사양 §3.1/§4/§5.2/§11 에 블록·밸브·수질·UPS 반영(v1.1).
- 블록은 전용 일반현황 _info 테이블 없음 → 토폴로지/태그 기반, 필요시 meta 확장.

## GIS 시설 메뉴 고도화 1차 구현

### 사용자 요청
- "1차 구현 진행해줘"

### 결과
- 탐색(Explore 에이전트 2개) 결과: Phase 1 백엔드 CRUD(/equipments·/reservoirs·
  /boosters·/pressure-reducing·CSV·매뉴얼) + 프런트 setup UI(/setup/equipments 등) +
  GIS 관망도(GisDetailPanel 읽기전용)가 이미 존재. 실제 갭 = GIS 인스펙터 CRUD 통합.
- 구현(프런트 submodule, slm-dashboard `26ea888`):
  - GisDetailPanel 설비 정보 섹션에 "추가" 버튼 + 행별 수정/삭제(hover) + 제조사·모델 표시.
  - EquipmentFormDialog 에 defaultSitename/defaultFacilitytype(add 모드 prefill+잠금) 추가.
  - 버그 수정: res.data?.items(오경로) → fetchEquipments(res.data), 설비목록 빈-표시 해소.
- 검증: tsc 내 파일 에러 0, /monitoring/gis 컴파일·200. WebGL 마커 클릭은 자동화 한계로
  수동 클릭 확인 권장. 사양 §10 구현 현황 + §11 이력 갱신.

### 수동 클릭 검증 + 인스펙터 배선 수정
- 검증 중 발견: detailFacility(인스펙터 오픈 상태)를 set 하는 코드가 없어 GisDetailPanel
  인스펙터가 애초에 열리지 않던 배선 누락 → handleMarkerClick·handleListSelect 에
  setDetailFacility(f) 추가(slm-dashboard `25e0072`).
- Playwright 검증: 시설 목록 행정배수지 클릭 → 인스펙터 렌더(설비 정보·추가/수정/삭제·
  Q/H/P 트렌드) + "설비 추가" 다이얼로그 현장명 "행정" prefill+잠금. 콘솔 에러 0.
