# 2026-05-04 세션 로그

## 요청
- "사양도 업데이트" — EPANET Phase 1 의 사양 문서 보강

## 변경 요지

### 사양 보강
- `docs/feature-spec.md`:
  · §18.4 EPANET 수리 시뮬레이션 — 관리 > 사이트 설정 토글 절 추가
  · §18-A 관리 > EPANET 시뮬레이션 신규 — 페이지 구성 / API 6종 / SHP 입력 자동 분류 / INP 출력 / Phase 1 한계
- `docs/work-history.md` 최상단 — Phase 1 완료 엔트리 (5섹션: 마이그레이션·백엔드·프런트·검증·사양)
- `docs/gis_plan.md` (어제 갱신, 유지)

### dialog_log 롤링 (3일)
- 04-30 삭제 (D-4)
- 05-03 남김 (D-1)
- 05-04 신규 (오늘)

## 추가 — EPANET Phase 2 (wntr 시뮬레이션 동작)
사용자: "다음 진행해줘"

### 변경 요지
- DB Migration 0065 (`db/migrations/0065_epanet_simulation_result.sql`):
  · `tb_epanet_simulation_result` (sim_id/artifact_id FK CASCADE/sim_type/status + 수치 요약 6 + result_data JSONB + duration_ms)
- 백엔드:
  · backend 이미지 재빌드 — wntr 1.x + pyshp 3.x 자동 설치 성공 (build-essential)
  · `slm/epanet/simulator.py` 신규 — `run_steady_state()` + ARM64 환경 자동 폴백 (EpanetSimulator → WNTRSimulator) + PDD 모드 + 가장 큰 CC 자동 추출
  · `slm/epanet/inp_converter.py` — 노드 좌표 정밀도 4자리 → 0자리 (1m 병합)
  · `slm/endpoints/epanet.py` — 3 엔드포인트 추가 (simulate/simulations/sim 상세)
- 프런트 (`admin/epanet/page.tsx`):
  · 산출물 표 행에 [시뮬] 버튼 (Activity icon, cyan)
  · 시뮬 결과 미리보기 카드 — 4 KPI (노드/링크/압력 범위/평균 압력) + 2 (유량 범위/실행 시간)

### 검증
- wntr_available: true (Docker 재빌드 후)
- artifact #3 (송수관 132건, 1m 병합) → 시뮬 #3 — 노드 128 / 링크 131 / 압력 50.0~50.0m / 유량 ±0.0205 LPS / 108ms
- ARM64 환경 EPANET .so 미포함 → WNTRSimulator 자동 폴백 동작

### Phase 2 한계
- 압력 50.0m 모두 동일 (표고 0 + 수요 0 LPS 환경의 정적 분포 — Phase 3 에서 표고·수요 입력 필요)
- 다중 vertex 미반영 (Phase 2.5)
- GIS 시각화 오버레이 미구현 (Phase 2.5)
- 펌프·밸브 SHP 미반영 (Phase 3)

## 추가 — 핫픽스 + GIS 밸브 SVG + EPANET Phase 2.5
사용자: "야간 최소유량 표준편차 무한 대기" / "밸브 시설이 점으로" / "이어서 진행해"

### 핫픽스 — 야간 최소유량 표준편차 채팅 무한 대기
- `slm/ai_server.py` import 목록에 `_extract_stddev_stats` 누락 (4 곳에서 호출, 매번 NameError → SSE close 안 됨)
- response_builder.py:1110 에 정의는 정상 존재 → import 한 줄 추가로 해결
- 커밋: `slm@8f9af36`

### GIS 밸브 SVG 심볼 적용
- `GisShpLayer.tsx` 가 `layerDef.icon` 필드를 무시하고 모든 시설 SHP 를 type:"circle" 로만 렌더 중
- ensureMapIcon() 신규 — SVG fetch → Canvas drawImage → ImageData → map.addImage
- icon-size 줌 보간 (10→0.45x, 18→1.4x)
- 점은 radius 2 / opacity 0 으로 hit-test 영역만 유지
- 5종 밸브 + 유량계/소화전/배수지/가압장 SVG 심볼 표출
- 커밋: `slm-dashboard@ce44b1a`, `web@cbafb63`

### EPANET Phase 2.5 — 다중 vertex + SVG 시각화
- **INP [VERTICES]**: `inp_converter.py` PolyLine 중간 점 보존 (첫·끝점은 노드, 중간은 vertex)
- **시뮬 응답 좌표**: `simulator.py` 가 wntr WaterNetworkModel 의 노드 좌표·파이프 vertex·배수지 좌표·bbox 응답에 포함
- **`EpanetSimulationCanvas`** 신규 (`components/epanet/`):
  · UTM-K 좌표 자체 SVG viewBox (proj4js 등 변환 라이브러리 의존 X)
  · 노드 압력 HSL 색상 히트맵 (240→0)
  · 파이프 정/역류 색상 (청록 / 주황) + 굵기 비례
  · 배수지 청록 사각형
  · 호버 툴팁 + 범례
- `/admin/epanet` 페이지 시뮬 결과 카드에 SVG 캔버스 통합
- 검증: artifact #4 → 시뮬 #4 — bbox UTM-K [156280, 465458, 194003, 494896], 좌표 응답 정상

### MapLibre GIS 페이지 오버레이는 Phase 3
- 이유: UTM-K (EPSG:5179) → WGS84 (EPSG:4326) 변환 필요 (proj4js)
- 현재는 `/admin/epanet` 자체 SVG 로 결과 즉시 확인 가능
