# 2026-07-11 세션 대화 로그

## 계통도(용수공급) 배수지 클릭 부드러운 확대 복원
- 증상: 계통도(MapLibre) 노드 클릭 시 부드러운 확대(flyTo)가 사라짐.
- 원인: MapLibre 는 `prefers-reduced-motion`(동작 줄이기) 시 flyTo 애니메이션을
  건너뛰고 즉시 점프 → "부드러움 없어짐"으로 인지. (흐름도는 custom rAF 라 무영향)
- 조치: `FlowDiagramMap.tsx` handleNodeClick 을 `getMap().flyTo({ essential: true,
  zoom: max(getZoom()+1,15), duration: 800 })` 로 변경. essential 로 reduce 예외,
  zoom+1 로 항상 확대 체감. 검증: 클릭 후 줌 Lv 15 확대·노드 포커스.

## 교차 검증 결과 — 카드 디자인 적용 + 진행 표시 상시화
- 요청: "교차 검증 결과" 응답을 목업(카드형)대로 개선. 대시보드 KPI(교차검증 이상)
  → AI 분석 팝업이 "멈추는 듯 보이다 결과가 나옴", 카드 디자인 미적용.
- 백엔드: `response_builder.py` ANOMALY_CROSS_FACILITY 분기에서 구조화 데이터
  (`cross_facility_mismatches`, `cross_anomaly_count`)를 top-level 응답으로 노출
  (기존 flat 텍스트 detail 은 프런트에서 억제). `build_success_response` 가
  `processed_data.get(...)` 를 kwarg 로 전달하는 기존 경로 재사용.
- 프런트 신규: `CrossValidationList.tsx` — 상류→하류 흐름(✕▶) + 가동률 비교 바
  (상류/하류 active_pct) + 진단 배지(공급 단절/인과 급락/방향 역전/가동률) +
  핵심 지표(상류 평균→0 등) + 판정 근거 칩. 다크/라이트 공용.
- 배선: `types/chat.ts`(bot.cross_facility_mismatches) → `chat-response-mapper.ts`
  → `ChatMessageArea` → `BotMessage`(intent=ANOMALY_CROSS_FACILITY 시 카드 렌더 +
  flat detail 억제). `AnomalyScanView` 의 기존 CrossFacilitySummary 도 신규
  컴포넌트로 통일(중복 제거).
- 진행 표시: `.slm-live-spin`/`.slm-live-pulse`(reduced-motion 전역 freeze 예외)를
  ChatMessageArea 컴포넌트-스코프 `<style jsx>` 에서 `globals.css` 전역으로 승격.
  `QuickAnalysisDialog` 의 로딩 스피너/스텝퍼에 적용 + 로딩 중 진행 스텝퍼 상시
  표시 → 병목 조회(교차검증 수초) 중에도 멈춘 것으로 오인되지 않음.
  겸사겸사 QuickAnalysisDialog 참고(ReferenceBlock) 렌더 잠재 버그 수정.
- 검증(Playwright, prod 빌드): /chat + 대시보드 KPI 팝업 모두 카드 렌더 확인,
  전역 `.slm-live-spin` 규칙 reduced-motion 에서 `infinite` 확인.

## 교차 검증 질의 분류 ~10초 지연 해결 (SSE 신뢰성 문의)
- 문의: "교차 검증 결과" 질의의 분류가 오래 걸리는 것처럼 보이는데 SSE 를 믿어도
  되나? 나머지 스텝은 순식간에 지나감.
- 실측(SSE 타임라인): classify@0.02s → **extract@9.8~10.7s** → query/render 즉시.
  즉 전체 ~10초가 분류 단계 소모. SSE 는 정확(진짜 분류가 느림).
- 원인: `intent_classifier._classify_category`(Stage1)의 `common_keywords` 에
  "교차 검증" 계열이 없어 keyword 단축 실패 → **SLM(gemma4:26b) 카테고리 분류
  폴백 ~10초**. 임베딩은 ~90ms 로 무관. 인텐트 단계(Stage2)엔 교차검증 키워드가
  있어 조회는 즉시였음(카테고리·인텐트 키워드 집합 불일치).
- 해결: `common_keywords` 에 교차 검증/교차검증/시설간 불일치/유량 불일치/흐름
  불일치/정합성 추가 → **분류 ~10초 → 0.1~0.4초**. intent=ANOMALY_CROSS_FACILITY
  결과 동일 검증.
- 일반 원칙(사양 기록): 새 인텐트 keyword 추가 시 Stage1(카테고리)+Stage2(인텐트)
  키워드를 함께 갱신해야 SLM 폴백 지연이 없음.

## 대시보드 KPI 팝업 카드 디자인 일관화 (물수지·경보이력·설비장애)
- 물 수지("물 수지 검증해줘"): FlowBalanceInfographic 카드는 이미 있으나 위에 flat
  47줄 텍스트가 중복돼 화면 가림 → BotMessage·QuickAnalysisDialog 에서 flat detail
  억제(카드만 표시).
- 경보 발생 이력("경보 발생 이력 보여줘"): AlarmHistoryList 카드 + flat 텍스트 중복
  → QuickAnalysisDialog flat detail 억제(BotMessage 는 기존 억제). 팝업
  VisualRenderer 에 intent 전달(이번 세션)로 카드 렌더 활성화됨.
- 설비 장애 카드 = 이상 감지와 **동일 질의("전체 센서 이상 스캔해줘")** 버그 →
  원인: dashboard/page.tsx 두 KPI onClick 문자열 동일. 사용자 선택: DI 설비 고장
  전용 뷰.
  - 신규 인텐트 EQUIPMENT_FAULT_STATUS: ANOMALY_SCAN_ALL 캐시의 equipment_failure
    _impacts(DI 통신/UPS/펌프/전원) 재사용. example3.json + intent_classifier
    (Stage1+Stage2) + ai_server(_DYNAMIC_SQL_INTENTS 2곳 + 2핸들러 분기,
    캐시 .processed_data 언랩) + response_builder(구조화 노출).
  - 프런트 EquipmentFaultList 카드(유형 칩+현장/설비+영향태그), BotMessage·
    QuickAnalysisDialog 배선, dashboard 질의 "설비 장애 현황 보여줘"로 교체.
  - 검증: 56건(4 DI 유형) KPI 카운트와 일치, 팝업 카드 렌더(Playwright).
  - 디버깅 이슈: ① _DYNAMIC_SQL_INTENTS 누락→분기 스킵 ② 캐시 언랩 키 오류
    (.processed_data) ③ response_builder _wrap_marker 미정의→평문. 모두 해결.

## 이상 감지 vs 설비 장애 KPI 완전 분리 (겹침 제거)
- 문의: 이상 감지·설비 장애 클릭이 결국 같은 대답 아니냐, 나눠야 하나 합쳐야 하나.
- 분석: 설비 장애(DI 확정 고장)는 이상 감지(전체 스캔)의 하위 섹션이라 겹침. 합치면
  '즉시 출동(DI 확정)' vs '검토(통계 이상)' 운영 구분 소실 → 비권장. 사용자 선택:
  완전 분리(각 KPI = 자기 항목만, 종합은 별도 버튼).
- 구현(프런트 전용, 백엔드/캐시 무변경):
  - QuickAnalysisContext.focus="value_deviation" 추가. 대시보드 '이상 감지' 클릭 시
    전체 스캔을 돌리되 QuickAnalysisDialog 가 anomaly visual 에서 겹치는 하위 데이터
    (crossFacilityMismatches·equipmentFailureImpacts·dataQualityIssues·
    flowBalanceSummary) 를 제거 + AnomalyData.focusMode 주입.
  - AnomalyScanView focusMode: 교차이상·설비장애·홀딩의심·유량불균형 요약 칩 + 통신장애
    배너 숨김(이상/주의/정상 + 편차율 차트만). 하위 섹션 카드는 데이터 제거로 자동 숨김.
  - QuickAnalysisDialog: focus 시 전체 카테고리 나열 summary 감추고 안내 배너 표시.
  - 대시보드: '이상 감지'→focus, 신규 '전체 이상 스캔 보기 (종합)' 버튼(focus 없이 전체).
  - 결과: 이상감지=값이탈만 / 교차검증=교차만 / 설비장애=DI만 / 유량불균형=물수지만.
    검증(Playwright): focus 뷰에 이상/주의/정상 칩만, 겹치는 섹션·배너 제거 확인.

## 설비 장애 영향/이상 카운트 의미 + 태그 드릴다운
- 문의: '영향 2개/영향 0개' 의미와, 영향 태그가 어느 것인지 정보 필요.
- 의미(_detect_equipment_failures): 영향=설비 연결 태그 수, 이상=그중 스캔에서 이상
  잡힌 수, 영향 0개=태그 매핑 없음(설비 자체 점검 필요).
- 조치(프런트): EquipmentFaultList 에 범례 + 행 펼치기(affected_tags 의 datainfo+tagsn
  목록). 이미 응답에 있던 affected_tags(최대 5)를 표출. 케이스별 안내 문구.

## 이상탐지 스캔 태그 필터 — 설정값 제외 (적산에 이어) + 분포 카운트 표시
- 문의: ① 시설별 이상 분포 '영향 센서 N개'와 표시 데이터 수 불일치 ② '설정값'은
  모니터링 값이 아닌데 이상센서로 잡히는 원인 ③ 적산도 실시간이 아닌 누적이라 제외 맞음.
- 원인: ANOMALY_SCAN_ALL SQL 은 tagtype='Analog Input' + datainfo NOT LIKE '%적산%'
  까지만 필터 → '설정값/설정압력'(알람 임계치, Analog Input 저장)이 z-score 대상에 포함.
  (적산은 이미 제외돼 있었음 — 사용자 지적대로 유지)
- 조치:
  - 백엔드: datainfo NOT LIKE '%설정%' 추가. z-score 5개 인텐트(SCAN_ALL/
    FACILITY_DETAIL/PREDICT/COMPARE/PATTERN) 공통. 설정 태그 34개 제외, 302→286행.
    검증: 설정값·적산 행 0.
  - 프런트: '시설별 이상 분포' 3개 초과 시 '외 M개 더' 표기(카운트-목록 일치).
  - 사양: slm-api-contract-final 스캔 대상 태그 필터 절 추가(적산·설정 제외 원칙).

## 트렌드 평소대비/향후전망 — 다중 tag 시 첫 tag 기준 오인 검토
- 문의: 트렌드 향후전망·평소대비가 모든 데이터가 아닌 첫 번째 데이터 기준으로
  추정하는 것 같다. 다수 데이터면 전부 보거나 일부 선택하게 해야.
- 검토 결과: 배지·overlay 는 실제로 tag별(comparisonMap[activeTagId])로 계산되고
  기본값=worst tag, ≥2개면 선택 드롭다운(각 tag '이름·상태' 표시)도 있음(스펙 §7.7).
  → "전부 상태 보기 + 일부 선택" 메커니즘은 이미 존재.
- 버그: TrendChart 제목 옆 라벨만 selectedTags[0](첫 tag) 하드코딩 → 실제 비교
  대상(worst/선택)과 어긋나 "첫 데이터 기준"으로 오인. (모든 tag 정상이면 worst
  동률→첫 tag tiebreak 이라 우연히 맞아 더 헷갈림. 첫 tag 가 skip 대상이면 완전 오표기)
- 수정: 라벨을 tagNameMap[activeTagId] 로 + comparison tag 2개↑면 '· 상단에서 변경'
  안내. 계산은 정상이라 프런트 표기만 수정. 사양 §7.7 버그수정 노트 추가.
  (참고: '전체 보기' 모드는 스펙 §7.7 P2 로 예정)
