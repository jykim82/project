# 2026-07-21 세션

## 공인 인증서 발급 완료

- 공유기 443 포워딩 후 TLS-ALPN 검증 통과 → Let's Encrypt 발급 성공
- https://dnhigh98.asuscomm.com 정식 인증서 접속 실측 (경고 없음, 자동 갱신)
- 80 미개방 구성 확인 — 443 단독 발급 경로 검증됨


## 통신 v2 소과제 4건

- 그룹 관리: 멤버 목록/추가 초대/이름 변경/나가기 (⚙, /userchat/group/*)
- 회의 중 추가 초대 (참가자 바 ➕, 상한 4, 재초대 지원)
- 메모 → 일정 등록 연계 (프리필 다이얼로그)
- 통화 이력 (🕘, /call/history — 방향·유형·결과·통화시간)


## GIS 타임라인 Phase 2

- /gis/timeline/pump (가압장 RUN 시계열, 27개소 실측 검증) + 패널 펌프
  컬럼(N/M) + 기간 24h/3일/7일 토글. 전 지표 서버 스냅샷 대신 부분
  서버화로 확정 (대표 태그 로직 이원화 방지 — 사양 갱신)

## "이 알람 중지" 헤더 실시간 알람 확장

- 사용자: 위기대응 모달의 "이 알람 중지"를 헤더 실시간 알람(벨 목록·주의
  toast)에도 적용 요청
- 프리필 URL 공용 헬퍼 `alarm-suppress.ts` 추출 (모달·벨·toast 3곳 일원화)
- 알림 벨 행별 "중지" 버튼 (controlled dropdown 전환), 주의 toast 에
  "알람 중지" 보조 버튼 (sonner cancel 슬롯)
- 실브라우저 검증: 벨 → 삼화(가) 통신이상 "중지" → 알람 제어 창 가압장/
  삼화/점검/통신 1개/[알람 중지] 내용 프리필 확인 (tmp/alarm-suppress-from-bell.png)
- alarm-popup-spec.md §신규-③ 확장 절 추가

## UI 효과 7건 검토 (magicui/motion/gsap)

- 각 페이지 확인 후 판정: 채택 3건(시작 진행 액션 배지·테마 토글
  View Transitions·우측 순차 등장 — 전부 신규 의존성 0, CSS/네이티브 API),
  제외 4건(Beam/Terminal/motion 버튼/Family Dialog — motion 제외 방침·톤
  불일치). GSAP 라이브러리 도입 반대(효과만 차용)

## UI 모션 채택 3종 구현 (사용자 승인 "응 진행해줘")

- ActionStateButton 공용 컴포넌트 (유휴→처리중→성공/오류, CSS transition,
  .slm-live-spin 스피너) → 보고서 생성 버튼 시범 적용. 실검증: 생성→상세
  이동 정상 (테스트 초안 #13 생성 후 삭제)
- 테마 토글 View Transition (theme-transition.ts, 클릭 지점 원형 확장 +
  flushSync, 미지원/reduce-motion 폴백). 라이트↔다크 실검증, 콘솔 오류 0
- 우측 등장: 네트워크 인스펙터에 기존 animate-in idiom 보강 + .slm-stagger-in
  자식 순차 등장 (일정 알림 팝업·위기대응 모달 카드)
- docs/ui-motion-policy.md 신설 (채택/배제 사유·사용 규칙) + CLAUDE.md 등록

## 이 알람 중지 — 개별 태그 자동 추가 + 모션 피드백 수정 + 4회씩 직접 테스트

- 사용자: ① 알람 중지 프리필에 태그명도 추가 (그 태그만 중지 대비)
  ② GIS 우측 패널 나타남 효과 미적용 ③ 테마 번짐 안 보임 → 각 효과 4회
  직접 테스트 지시
- 태그 프리필: suppress URL 에 tagsn 포함 → TaskFormDialog 가 시설 태그
  로드 후 datainfo 라벨 칩 자동 추가. 부수 버그 2건 수정 — 같은 페이지
  재진입 무시(1회성 가드→파라미터 키), 다이얼로그 이전 값 잔존(열릴 때
  defaults 재동기화)
- 테마 번짐: E-045 — next-themes effect 반영이 비동기라 스냅샷 무변화.
  toggleThemeWithCircle 로 class 동기 토글 + resolvedTheme (system 첫 클릭
  해소)
- GIS 패널: 시설 변경 시 key 리마운트 + GisDetailPanel 내부 slm-stagger-in
- 4회 테스트 결과 (Playwright 실측): 테마 4/4 (원형 애니메이션 감지+전환),
  태그 프리필 4/4 (죽동 탁도계/삼화/합덕일반/수청2지구2), GIS 패널 4/4
  (등장+스태거), 보고서 생성 버튼 4/4 (처리중 라벨+스피너→상세 이동,
  초안 #14~17 생성 후 삭제)
- 발견: 합덕일반 수위 태그 datainfo("공업 3지")와 알람 메시지("공업 1지")
  불일치 → 태그 칩은 tagsn 기준 정확, 메시지 부분일치 억제엔 미매칭 가능
  (tagsn 억제 P2). 수청2지구2 는 현장명 셀렉트 표시 공백 (facility_map 누락)

## 모션 체감 상향 — "실행됨 ≠ 보임"

- 사용자: 원형 번짐·GIS 패널 등장 여전히 안 보임 ("너무 빨라서 그런가")
- 조사: 파이프라인은 정상 (VT ready·클립 계산·애니메이션 실행). 원인은
  체감 — 구석 시작 450ms 원형, 360px 레이아웃 변화 옆 32px/300ms 슬라이드
- 조정: 테마 700ms ease-out / GIS·유량·네트워크 인스펙터 전체 폭 슬라이드
  400ms ease-out
- 재테스트 4회씩: 테마 4/4 (신규 파라미터 주입 확인), GIS 4/4 (남산→행정→
  합덕→송악1, 중간 정지 스크린샷으로 시각 확증)
- 한계 기록: 자동화 Chromium 은 VT 스냅샷 미합성 → VT 최종 체감은
  실브라우저 확인 필요 (ui-motion-policy 검증 노트 추가)
