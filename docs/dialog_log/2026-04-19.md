# 2026-04-19

## 비전 에이전트 사진 업로드 404 수정 [E-029]

### 사용자 요청
- "다음할일은??" → "1번부터 진행" (어제 기록한 "내일 할 일" — 비전 에이전트 오류)
- 배경: 채팅에서 사진 업로드하니 "비전 에이전트 오류가 발생했습니다" SSE error

### 진단
- backend(Docker, port 8000), vision_agent(host native, port 8100), Ollama(port 11434) 모두 정상 기동 확인
- 재현 테스트 (curl `/ask/multimodal/stream` + 이미지) → SSE event `error` + 404
- 근본 원인 2건:
  1. vision_proxy가 **컨테이너 내부 절대경로**를 `image_url`로 vision_agent(호스트)에 전달 → 호스트 FS에 해당 경로 없음 → `_load_image_base64` HTTPException(404)
  2. docker-compose.dev.yml backend 서비스에 `chat_attachments`/`facility` 호스트 바인드 마운트 자체가 없어 파일이 컨테이너 레이어에만 존재

### 변경 요지
- `slm/endpoints/vision_proxy.py` — `_save_upload` `(path, url)` 튜플 반환, vision_agent엔 URL 형식 `/api/files/chat_attachments/<name>` 만 전달 (vision_session 저장·`vision_advice.image_url` 도 URL로 통일)
- `slm/vision_agent.py` — `CHAT_ATTACHMENT_DIR` env 상수 추가, `_resolve_image_path`에 `/api/files/chat_attachments/` prefix 핸들러, 기본 경로 1레벨 오차 수정
- `web/docker-compose.dev.yml` backend — volumes `./files:/data/files` 추가, env `CHAT_ATTACHMENT_DIR=/data/files/chat_attachments`, `FACILITY_FILE_BASE_DIR=/data/files/facility`
- 호스트 `/Users/jykim/web/files/chat_attachments` 디렉터리 신규 생성

### 검증
- 실제 PNG(`anim-t1.png`, 170KB)로 `/ask/multimodal/stream` 재호출 → `vision_session_id=36`, VLM 46.9s 응답 정상 반환
- 호스트 `web/files/chat_attachments/` 와 컨테이너 `/data/files/chat_attachments/` 에서 동일 파일 가시

### 커밋
- slm@6093733 — vision_proxy + vision_agent
- web@704d699 — docker-compose + docs

---

## 사진 업로드 시나리오 4종 사양 정의 + P1 구현

### 사용자 요청
- AI 채팅 사진 업로드 시나리오 4종 정의 (사진만 / 사진+등록 / 등록만 / 멀티턴)
- "현재 SLM 언어모델에서 충분히 가능한 수준인지 검토해줘" → 검토 결과 P1 착수
- "진행해보자" → P1 구현 (시나리오 2/3)
- "실제로 테스트 5번해주고 다음으로 넘어가줘" → curl E2E 5회 검증

### 사양 결정
- `docs/chat-photo-upload-scenario-spec.md` 신규 작성
- D1 RAG 전략: A(매뉴얼 재청킹) + B(tb_fault_case) 병행 추천
- D2~D5 사용자 승인 하에 추천안 채택
- 구현 우선순위: P1(시나리오 2·3) → P2(1-a 재질의) → P3(1-b RAG) → P4(수질계/압력계)

### 모델 스택 검토
- `gemma4:26b-a4b-it-q4_K_M` + `snowflake-arctic-embed2` 현재 스택으로 4 시나리오 모두 처리 가능 (모델 업그레이드 불필요)
- 병목은 VLM 46초(설계상 호출 최소화) / 데이터(매뉴얼 고장 섹션) / Intent 복잡도(키워드 매칭 우선) 3가지

### P1 구현
- **백엔드 (slm@e344be0)**: `build_fault_draft()` 공용 헬퍼 추출, `DraftRequest.photo_urls[]`, `/confirm` 의 photo_urls 컬럼 매핑, `/chat/fault/attach-photo` 신규 (시나리오 3), `vision_proxy._detect_fault_intent()` + `_fault_branch()` (시나리오 2)
- **프런트 (slm-dashboard@5517ca9)**: `AiServerResponse.fault_draft`, `FaultDraft.draft.photo_urls[]`, `chat-response-mapper` 에 fault_draft 전달, `attachFaultPhotos()` API, `FaultRecordConfirmCard` 썸네일 그리드 + "사진 추가" 버튼 (최대 3장)
- **인프라 (web@a2cc44c)**: `docker-compose.dev.yml` frontend 에 `./files:/data/files` 바인드 + `FILE_STORAGE_PATH=/data/files` env (썸네일 서빙용)

### 검증 (curl E2E 5회)
1. ✅ "신평 배수지 PLC 고장 등록해줘" → FAULT_RECORD_DRAFT, PLC/고장, photo 1장, ready=true
2. ✅ "난지마을 배수지 판넬 전원이상 기록해줘" → 이상 카테고리
3. ✅ "행정 배수지 UPS 교체" → UPS/교체
4. ✅ "이 장비가 현재 정상이야?" → VISION_DIAGNOSE 정상 회귀 (FAULT 오탐 없음)
5. ✅ "난지관곡 가압장 가압펌프 이상 발생" → 가압펌프/이상

### 커밋
- slm@e344be0 — backend (chat_fault_record + vision_proxy)
- slm-dashboard@5517ca9 — frontend (types + mapper + API + card)
- web@a2cc44c — docker-compose + docs + submodule bump

---

## P2 구현 — 사진만 업로드 시 용도 재질의 (시나리오 1-a)

### 사용자 요청
- "커밋 푸쉬하고 다음으로 진행" → P2 착수

### 변경 요지
- **backend (slm@753d76c)**: vision_proxy 에 `is_photo_only` 판정 → PHOTO_CLARIFY 즉시 응답. 신규 `/chat/photo-action` (fault/diagnose/reference)
- **frontend (slm-dashboard@db447ff)**: `PhotoClarify` 타입 + `IntentClarifyCard` 신규 (썸네일 + 3버튼 + 자유 텍스트). 버튼 클릭 후 결과를 카드 내부에서 FaultRecordConfirmCard/VisionAdviceCard 로 치환 렌더
- Next.js 파서가 ternary JSX 에 unterminated string 에러 → parentheses 감싸서 해결

### 브라우저 E2E 검증 (스크린샷 2장)
1. `p2-intent-clarify.png` — 사진만 업로드 → IntentClarifyCard (3버튼) 즉시 표시
2. `p2-clarify-to-fault.png` — "난지마을 배수지 PLC 고장" + 고장 등록 버튼 → FaultRecordConfirmCard 로 치환 (equipment 자동 매칭)

### 커밋
- slm@753d76c — vision_proxy PHOTO_CLARIFY + /chat/photo-action
- slm-dashboard@db447ff — IntentClarifyCard + types + mapper + API

---

## P3 구현 — 고장 진단 케이스 DB + RAG 통합 (B+C 병행)

### 사용자 결정
- "추천대로 진행하고, C에 대한 고장 자료의 구조도 결정을하고, IMPORT, EXPORT 기능을 주고, 예제도 있어야할거같아"
- "일단 먼저 구현하고 향후 실행 사용하면서 개선 진행예정"

### 변경 요지
- **migration 0048** `tb_fault_case` — UNIQUE(equipment_type, brand, model, symptom), 심각도/설비유형 CHECK, updated_at 트리거
- **backend (slm@8b8cf8e)** endpoints/fault_case.py 신규 — CRUD + 임베딩(snowflake-arctic-embed2 + NPZ) + openpyxl 기반 IMPORT/EXPORT + 템플릿
- **vision_agent** `_FaultCaseIndex` + `_retrieve_fault_cases()` + `DiagnoseResponse.fault_cases[]` + `/vision/fault-cases/reload`
- **frontend (slm-dashboard@f29520e)** `/setup/fault-cases` 관리자 UI — 테이블/필터/검색/다이얼로그/엑셀 버튼 3개
- **예제** `docs/examples/fault_case_template.xlsx` (PLC/인버터/RTU/모뎀/UPS 5건)

### 검증
- 템플릿 → 5건 IMPORT → EXPORT → 재로드 OK
- 진단 E2E: "PLC ERR LED 빨강 점등" 쿼리 → 3 cases 매칭 (#1 PLC 0.571 최우선)
- 관리자 UI 스크린샷: `p3-fault-cases-list.png` 5건 테이블 정상

### 커밋
- slm@8b8cf8e — fault_case 엔드포인트 + vision_agent 통합
- slm-dashboard@f29520e — 관리자 UI + API client + 사이드바 메뉴
- web@ece49d4 — migration + docs + submodule bump

---

## P3 후속 — VisionAdviceCard fault_cases 섹션 렌더

### 사용자 요청
- "이어서 할만한 일은?" → 추천 3가지 제시 (1.VisionAdviceCard fault_cases / 2.#75 알람 연계 / 3.매뉴얼 PDF UI)
- "2번은 제외" — 알람 자동 해제는 설계 위배 (메모리 `feedback_no_auto_alarm_link.md` 저장, #75 삭제)
- "응 진행해" → 1번 착수

### 변경 요지
- **frontend (slm-dashboard@dad3e31)**
  - `VisionFaultCase` 타입 + `VisionAdvice.fault_cases[]`
  - VisionAdviceCard 에 "관련 고장 케이스 · N건" 섹션 (Wrench 아이콘, 매뉴얼 인용보다 앞)
  - 케이스당 badge (설비유형/심각도) + brand/model + score(3자리) + 증상/원인/조치 라벨드 3행

### 검증
- `p3-vision-card-fault-cases.png`: "PLC ERR LED 빨강 점등 상태 진단" 사진 질의 → 3건 정상 노출
- PLC 0.622 / 모뎀 0.478 / RTU 0.459 — 설비별 증상-원인-조치 완전 표시

### 커밋
- slm-dashboard@dad3e31 — VisionAdviceCard fault_cases 섹션

---

## P5 알람 ↔ 장애 매칭 분석

### 사용자 질문·결정
- "고장 등록 통계 현황은 어느 메뉴?" → `/monitoring/equipment-health` 존재하나 사이드바 미등록 확인, 등록
- "설비 신뢰성 메뉴랑 겹치는거야?" → 겹침 확인 (MTBF), 3옵션 제시
- "알람과 설비 건강성을 매칭하여 알람 통계 대비 실제 검사한 결과 통계를 유의미하게 제시할 수 있지 않나?" → 7개 핵심 지표 + 4 엔드포인트 + 섹션 스펙 제안
- "추천하는 방식대로 우선 구현" → 착수

### 변경 요지
- **backend (slm@40cd7f9)** — `endpoints/alarm_fault_correlation.py` 신규
  - /summary, /matrix, /lag, /unmatched 4 엔드포인트
  - 리포트 전용 (자동 해제·연계 금지 — `feedback_no_auto_alarm_link.md`)
- **frontend (slm-dashboard@fecad28)** — AlarmFaultCorrelationSection + equipment-health 하단 삽입 + 사이드바 "설비 건강성" 등록

### 실데이터 결과 (90일)
- 알람 5,738 vs 장애 14, 매칭 0 — 현장이 linked_alarm 미사용
- 설비유형별 편차 드러남: 네트워크(3924/0) 오탐 가능, PLC(0/5) 미검지
- 탐지 지연 히스토그램은 매칭 0이라 "기록 없음" 안내

### 커밋
- slm@40cd7f9 — alarm_fault_correlation 엔드포인트
- slm-dashboard@fecad28 — 섹션 + API client + equipment-health 통합 + 사이드바
- web@9856e3e — 사양 + 사이드바 설비 건강성 등록 + submodule bump

---

## P5-rev 관점 전환 — 설비 교체 후보 분석

### 사용자 피드백
"네트워크가 거의 4000개 태그이지만 현장에서 lte모뎀 고장이라고 고장 보고를 하고, 조치 완료 일자를 기록하면 그기간은 발생·해제·발생·해제가 반복될수도 있고 통신 불안정으로 그럴수 있다는말이지. 따라서 둘사이의 관계성을 찾기보다는 통신 알람이 발생 빈도가 높았고, 현장 검증을 했고, 조치가 되었고, 또는 조치가 되지 않아 아직도 빈번히 발생을 하고 잇다면 그것이 설비를 교체해야할 목록이라는 말이지"

### 변경 요지
- **backend (slm@b8d47f2)** — 기존 4 엔드포인트(summary/matrix/lag/unmatched) 폐기. `/equipment-status` 단일 엔드포인트로 교체. 설비 그룹 단위 집계 + 4상태 분류 (needs_action/in_progress/replacement_candidate/resolved) + 조치 후 재발 SQL 서브쿼리.
- **frontend (slm-dashboard@88e4f87)** — AlarmFaultCorrelationSection 전면 재작성. KPI 5개 + 설비 상세 테이블 + 필터 4종.
- 파라미터 기본값: days=90, min_alarm=10, recurrence_cnt=3, recurrence_rate=0.2

### 실데이터 결과
- 90d / min_alarm=10 기준 13 설비 → **전원 needs_action**
- 죽동 배수지 네트워크 3,806건/134일 지속 최다 → 교체/점검 1순위
- 현장이 아직 linked_alarm 을 쓰지 않음을 명확히 드러냄 (다음 개선 대상)

### 커밋
- slm@b8d47f2 — equipment-status 엔드포인트
- slm-dashboard@88e4f87 — 섹션 UI 전면 재작성
- web@bdec50e — 사양 + submodule bump

---

## P6 설비 타임라인 · KPI 드릴다운 · 채팅 조치완료

### 사용자 요청
- "설비의 조치 이력은 어떻게 확인해?" → 공백 지적
- "구현해주고, 조치완료 보고를 ai 채팅에서도 구현되어야 하고 (기존 고장 보고 참고), 설비 건강메뉴에서도 등록할수 있어야 하지 않을까?"
- "장애 건수나 진행중 과같은 상위 패널을 클릭하면 팝업으로 정보를 알려주는 기능도 고려해줘"

### 변경 요지
- **backend (slm@bf9884b)** — 3 엔드포인트 신규
  - `/chat/fault/resolve/draft`+`/confirm`+`/direct` (조치 완료)
  - `/monitoring/alarm-fault-correlation/equipment-timeline` (설비 이력)
  - `/monitoring/equipment-health/tasks` (KPI 드릴다운)
- **frontend (slm-dashboard@db0efbb)** — 3 Dialog + 1 카드 + 훅 분기
  - `EquipmentTimelineDialog` (교체 후보 행 클릭)
  - `TaskListDialog` (KPI 클릭)
  - `FaultResolveConfirmCard` + `isResolveIntent` (채팅 조치완료)
  - 인라인 조치 완료 입력·버튼 양쪽 Dialog 내부에 제공

### 검증
- KPI "진행중" 클릭 → 14건 목록 Dialog ✓
- 죽동 배수지 네트워크 행 클릭 → 알람 3,812건 시계열 ✓
- 채팅 "신평 배수지 PLC 조치 완료했어" → task #28 완료 카드 → DB status='완료' + resolved_at + resolution_note 확인 ✓

### 커밋
- slm@bf9884b — resolve/timeline/tasks 엔드포인트
- slm-dashboard@db0efbb — Dialog 3종 + 카드 + 훅
- web@TBD — 사양 + submodule bump
